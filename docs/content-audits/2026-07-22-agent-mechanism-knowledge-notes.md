# Agent 机制知识笔记内容审计

## Scope 与当前发布状态

本审计覆盖 `agent-mechanism` 的 8 节课、28 份原始基线资源与 1 份 Task 5 规格审查补充资源、24 道面试题和 3 个交互实验。当前 registry 因证据完整性修订共 29 份；来源正文核验与 resource evidence registry 已完成。`agent-01` 至 `agent-08` 的独立知识笔记文件均已达到 ready-for-integration，但尚未接入公开课程数据，八课仍待统一接线。

- `publicationReady: false`
- `status: in-progress`（不是 blocked；可访问核心证据和项目 registry 已具备，`agent-01..08` 均待集成）
- `brokenReferenceCount: 0`
- 原始来源审计覆盖 28 份基线；Task 5 规格审查后另行访问并新增 AWS official/core 来源，当前 29 项 registry metadata 均已核验并记录 `2026-07-22`。回归测试同时允许已知真实核验日期 `2026-07-20` 与 `2026-07-22`，避免把所有历史记录强制伪装成同一天
- 发布门槛：每课质量分至少 85/100、所有章节 `sourceIds` 同时解析到全局 registry、lesson `resourceIds` 和有效 evidence set。

## Source policy

课程 objectives、concepts、explanations、quiz、面试题和 exercise 只定义学习覆盖，不是外部证据。官方规范与厂商正文用于接口语义和工程经验，原始论文只支持其任务、模型、数据与评测设定，课程和综述负责路线与交叉核验。视频只有在取得字幕正文或明确等价文字材料时才可支持相应范围；metadata-only 视频只能作为 `extension`，标题、简介和现有 `value` 不得被反推为机制正文。

`authority` 表示已核验的来源身份，不等于普遍正确；`role` 表示本模块实际用途。时敏 API、持续更新仓库、在线厂商材料和等价文字材料记录语义核验日。下表的 coverage 均来自本轮实际访问，limitations 明示访问、版本与迁移边界。

## 来源访问表：28 份基线 + 1 份 Task 5 审查补充

### 2026-07-22 证据完整性修订

Task 2 按立项范围审计了 28 份基线资料。Task 5 规格审查发现，`agent-03` 的核心考核明确要求解释幂等请求标识、网络超时后的未知终态和安全重试，但基线资料没有一份可访问正文直接支持这组可靠执行语义。课程 objectives、quiz、面试题和 exercise 只能证明“课程要求覆盖”，不能证明机制本身；把课程字段或既有资源标题当 evidence 会违反本审计的 source policy。

因此审查例外新增第 29 份来源 `res-agent-aws-idempotent-apis`：实际访问 AWS Builders' Library 官方正文 `Making retries safe with idempotent APIs`，canonical URL 为 `https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/`，作者 Malcolm Featonby。该卡角色为 `official/core`，增加原因仅是补足 assessed outcome 的直接证据；coverage 限定为 client request identifier、幂等重试、超时不确定性、语义等价响应、晚到请求与同标识改参边界，不把订单审批、查询、对账或补偿归因给 AWS。下表列出当前最终 29 项，并以这条记录保留 28 份基线与 AWS 补充的历史区别。

| id | access type | 实际入口 | authority | role | coverage | limitations | 核验日 | broken |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `res-agent-anthropic-effective` | body | [Anthropic 正文的 Agents 段落](https://www.anthropic.com/engineering/building-effective-agents) | official | core | Agent/workflow 与编排模式；自治成本/工具设计；逐步环境 ground truth；阻塞时人工检查点；完成或最大迭代停止 | 厂商工程经验，非跨模型普适实验；不定义本课 task contract schema、未知项四路由或三层业务完成语义；在线内容可演进 | 2026-07-22 | 否 |
| `res-agent-openai-guide` | body | [OpenAI Business 指南正文](https://openai.com/business/guides-and-resources/a-practical-guide-to-building-ai-agents/)；[Developers track](https://developers.openai.com/tracks/ai-application-development) 经 OpenAI Docs MCP 交叉核验 | official | core | Agent 适用条件与 model/tools/instructions；单双编排；清晰步骤、动作、输出与缺失信息分支；run-loop 退出；工具风险、护栏、评测与人工介入 | 工程指南不是 API 规范；不定义本课精确字段、硬/软约束、四路由或三层业务完成语义；模型和实现会过时 | 2026-07-22 | 否 |
| `res-agent-berkeley-course` | body | [课程页与 syllabus](https://llmagents-learning.org/f24) | academic | extension | 推理、规划、工具、基础设施、评测、安全和多 Agent 的课程导航 | 未逐讲访问正文，不支持机制细节或性能结论 | 2026-07-22 | 否 |
| `res-agent-hf-course` | body | [GitHub 仓库](https://github.com/huggingface/agents-course)、[Unit 1](https://huggingface.co/learn/agents-course/en/unit1/introduction) | official | cross-check | Agent 基础；Think–Act–Observe；tools/actions；smolagents 路线 | main 持续更新；框架示例不证明一般可靠性 | 2026-07-22 | 否 |
| `res-agent-ms-course` | body | [GitHub 仓库](https://github.com/microsoft/ai-agents-for-beginners)、[01-intro](https://github.com/microsoft/ai-agents-for-beginners/tree/main/01-intro-to-ai-agents) | official | cross-check | Agent、环境、工具；适用/不适用；实现路线和框架入口 | 当前为 18 lessons 并转向 Agent Framework/Foundry V2；旧视频代码不可混用 | 2026-07-22 | 否 |
| `res-agent-hello-agents` | body | [chapter 1 正文](https://github.com/datawhalechina/hello-agents/blob/main/docs/chapter1/%E7%AC%AC%E4%B8%80%E7%AB%A0%20%E5%88%9D%E8%AF%86%E6%99%BA%E8%83%BD%E4%BD%93.md)；[chapter 7 正文](https://github.com/datawhalechina/hello-agents/blob/main/docs/chapter7/%E7%AC%AC%E4%B8%83%E7%AB%A0%20%E6%9E%84%E5%BB%BA%E4%BD%A0%E7%9A%84Agent%E6%A1%86%E6%9E%B6.md) | community | cross-check | 环境/内部状态/行动闭环；旅行目标分解、工具补缺、用户反馈成为新约束后的修正；消息时间/metadata 与最大迭代示例 | 社区教材与旅行案例不是 task contract 标准；不定义 state/transcript/event log 三分法或三层业务完成谓词；接口版本需复核 | 2026-07-22 | 否 |
| `res-agent-dlai-agentic` | body | [课程介绍与 outline](https://www.deeplearning.ai/courses/agentic-ai) | expert | extension | reflection、tool、planning、multi-agent；eval/error analysis 与部署路线 | 未取得完整讲义或字幕，不能承担效果主张 | 2026-07-22 | 否 |
| `res-agent-lilian-weng` | body | [综述正文](https://lilianweng.github.io/posts/2023-06-23-agent/) | expert | cross-check | planning、task decomposition、ReAct、短长记忆、工具与挑战 | 2023 二手综述；实验结论须回原论文 | 2026-07-22 | 否 |
| `res-agent-lihongyi` | body | [YouTube 视频](https://www.youtube.com/watch?v=M2Yg1kwPpts)、[第三方字幕正文](https://youtube-transcript.ai/transcript/M2Yg1kwPpts.txt) | expert | core | goal–observation–action；环境反馈；工具；规划 benchmark、树搜索、world model 与过度思考边界 | 第三方繁中字幕可能有转写误差；论文、模型和 benchmark 数字不可脱离年份 | 2026-07-22 | 否 |
| `res-agent-datawhale-bili` | metadata | [Bilibili 页面](https://www.bilibili.com/video/BV17i421Y7L6/)、[view API](https://api.bilibili.com/x/web-interface/view?bvid=BV17i421Y7L6) | community | extension | 标题、作者、时长、课程 season 等导航元数据 | 字幕数组为空；严禁从标题推断正文，不支持实质机制 | 2026-07-22 | 否 |
| `res-agent-disney-planner-bili` | metadata | [Bilibili 页面](https://www.bilibili.com/video/BV1ix4y117zo/)、[view API](https://api.bilibili.com/x/web-interface/view?bvid=BV1ix4y117zo) | community | extension | 标题、作者、时长、简介等导航元数据 | 字幕为空；简介不等于讲解，比赛合作语境可能过时 | 2026-07-22 | 否 |
| `res-agent-ms-tool-video` | body | [YouTube 视频](https://www.youtube.com/watch?v=vieRiPRx-gI)、[自动字幕正文](https://youtube-transcript.ai/transcript/vieRiPRx-gI.txt) | official | cross-check | 外部工具、工具串联、最小权限、错误处理、Semantic Kernel auto/required | 字幕有三重重复；实现行为依赖版本 | 2026-07-22 | 否 |
| `res-agent-ms-plan-video` | body | [YouTube 视频](https://www.youtube.com/watch?v=kPfJ2BrBCMY)、[自动字幕正文](https://youtube-transcript.ai/transcript/kPfJ2BrBCMY.txt) | official | cross-check | 子任务分解、多 Agent 分派、结构化计划、Pydantic 校验和执行 | 字幕有噪声；旧 Chapter 7/Pydantic 示例与当前 18 课版本分离 | 2026-07-22 | 否 |
| `res-agent-react-paper` | body | [arXiv PDF](https://arxiv.org/pdf/2210.03629) | academic | core | reason–act–observe；环境反馈；ReAct/CoT/Act 对照；QA 和交互环境实验 | PaLM-540B、少量人工轨迹、受限动作空间；非通用控制器或生产安全证明 | 2026-07-22 | 否 |
| `res-agent-tot-paper` | body | [arXiv PDF](https://arxiv.org/pdf/2305.10601) | academic | core | thought 单元；候选生成和状态评价；BFS/DFS；前瞻和回溯 | 仅三个特制任务；搜索成本高且依赖分解和评价器 | 2026-07-22 | 否 |
| `res-agent-plan-solve` | body | [arXiv PDF](https://arxiv.org/pdf/2305.04091) | academic | core | 先规划后求解；子任务；PS/PS+；错误分类 | 静态推理提示，不含环境、工具或动态重规划 | 2026-07-22 | 否 |
| `res-agent-rewoo` | body | [arXiv PDF](https://arxiv.org/pdf/2305.18323) | academic | core | Planner–Worker–Solver；证据占位符；推理/观察解耦；token 效率与工具失败 | 固定蓝图降低在线适应性，不适合强观察依赖 | 2026-07-22 | 否 |
| `res-agent-toolformer` | body | [arXiv PDF](https://arxiv.org/pdf/2302.04761) | academic | cross-check | 自监督 API 标注；调用时机和参数；结果并入生成；五类工具实验 | 不支持工具链、交互修订、权限和副作用；不等于生产 function calling | 2026-07-22 | 否 |
| `res-agent-openai-function` | body | [OpenAI 官方 Docs](https://developers.openai.com/api/docs/guides/function-calling) | official | core | 五步生命周期；JSON Schema；call_id 与 function_call_output；多调用、strict、工具建议 | 含 GPT-5.6、Responses、GPT-5.4+ tool_search 等强时效语义；结构化不保证参数或业务动作正确 | 2026-07-22 | 否 |
| `res-agent-anthropic-tools` | body | [Anthropic 正文](https://www.anthropic.com/engineering/writing-tools-for-agents) | official | core | 工具契约；少而高信号；命名、描述、schema；返回上下文、token 与 eval 迭代 | 2025-09-11 厂商经验；依赖模型与任务，在线标题有轻微变化 | 2026-07-22 | 否 |
| `res-agent-aws-idempotent-apis` | body | [AWS Builders' Library 正文](https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/) | official | core | 调用方唯一 client request identifier；幂等重试与无额外副作用；网络超时不确定性与 reconciliation；同一标识的语义等价响应；晚到请求与保留期；同标识改参校验错误 | AWS 分布式 API 工程经验；不证明具体订单权限、审批、补偿、错误码或保留期策略，严谨契约本身有服务端复杂度 | 2026-07-22 | 否 |
| `res-agent-coala` | body | [arXiv PDF](https://arxiv.org/pdf/2309.02427) 与 [完整 HTML](https://arxiv.org/html/2309.02427) 的 4.1、4.2、4.6 节 | academic | core | 跨调用工作记忆中的当前感知/目标/中间信息；情景/语义/程序记忆；环境反馈写回；提议、评价、选择、执行、观察循环 | 概念架构与综述，无新增 benchmark；不规定生产 state schema、三类记录职责、task contract 或业务完成谓词 | 2026-07-22 | 否 |
| `res-agent-reflexion` | body | [arXiv PDF](https://arxiv.org/pdf/2303.11366) | academic | core | Actor–Evaluator–Reflection；语言反馈；情景记忆；跨 trial 改进 | 依赖环境信号、启发式/LLM 评价和多次尝试，部分任务使用单测 | 2026-07-22 | 否 |
| `res-agent-self-refine` | body | [arXiv PDF](https://arxiv.org/pdf/2303.17651) | academic | cross-check | 同模型生成、反馈、修订；迭代停止；可操作反馈；七类任务 | 依赖任务化提示和评价标准，多为输出编辑，不等于可靠推理纠错 | 2026-07-22 | 否 |
| `res-agent-no-self-correct` | body | [arXiv PDF](https://arxiv.org/pdf/2310.01798) | academic | core | 内在自修正；无外部反馈退化；oracle 对照；等成本与提示公平 | 仅研究推理正确性，不外推所有风格、偏好或安全任务 | 2026-07-22 | 否 |
| `res-agent-critic` | body | [arXiv PDF](https://arxiv.org/pdf/2305.11738) | academic | core | 工具验证；外部 critique；verify–correct；搜索、代码、计算器、毒性 API | 工具可能错误、有偏、有延迟及隐私安全风险；效果依赖提示 | 2026-07-22 | 否 |
| `res-agent-agentbench` | equivalent | [arXiv 完整 HTML](https://arxiv.org/html/2308.03688) | academic | core | 八类环境；29 模型；完成格式、动作、超时、终止；重复与长程规划失败 | 简化提示；不测搜索、反思和多次尝试；环境/模型版本时效 | 2026-07-22 | 否 |
| `res-agent-tau-bench` | body | [arXiv PDF](https://arxiv.org/pdf/2406.12045) | academic | core | agent–tool–user 动态交互；数据库终态；领域政策；pass^k | 模拟用户、两个简化领域、唯一数据库结果假设；不代表真实单次服务 | 2026-07-22 | 否 |
| `res-agent-douyin-claude-code` | equivalent | [原视频](https://www.douyin.com/video/7529703060969508130)、[明确源自视频的修订文字版](https://hicsc.com/how-to-build-agent/) | community | extension | 间接材料中的 LLM 工具编排、ReAct、Plan-and-Execute/replan、状态与 LangGraph 示例 | 原视频防自动化且无字幕；文字版为间接修订材料，GPT-4o/Cursor/Manus/LangGraph 属 2025 语境 | 2026-07-22 | 否 |

## URL、版本与字幕限制

- 28 个基线 registry URL 与 1 个 Task 5 AWS 补充 URL 均可解析，`brokenReferenceCount: 0`；实际正文入口可能是同一来源的 PDF、完整 HTML、课程子页、字幕或等价文字版，已逐项列在上表。
- 两条 Bilibili 资源仅取得 metadata，字幕数组为空；它们只可用于资源导航，不能被章节 `sourceIds` 用来证明 Agent 机制。
- 李宏毅和 Microsoft 两条 YouTube 使用第三方/自动字幕，分别保留转写与重复噪声限制。抖音原视频没有可访问字幕，只能把明确源自该视频的文字版作为间接 extension。
- Berkeley 与 DeepLearning.AI 只访问课程页或 syllabus/outline；课程字段不等于逐讲正文。Microsoft 课程核验时已为 18 课，旧视频示例不得和当前仓库语义混用。
- OpenAI function calling 为强时敏官方语义，`evidence.verifiedAt` 固定记录本轮核验日；结构化工具调用仍只是动作提案，不保证业务参数、权限、副作用或执行结果正确。
- AWS Builders' Library 正文直接支撑分布式 API 的 client request identifier、语义等价响应、网络超时、reconciliation 与晚到请求边界；具体订单查询、审批和补偿保持课程综合，不归因给 AWS。

## `agent-02` 四来源重新访问与课程综合边界

2026-07-22 在规格复审后重新访问了本课全部四个正文，而不是复用 registry 的 `value` 或课程解释：OpenAI Business 指南读取了 Configuring instructions、单 Agent run、Guardrails 与 Plan for human intervention，并按 OpenAI Docs Skill 通过官方 Developers track 交叉核验 instructions/tools/guardrails、上下文/记忆和 eval/guardrail 边界；Anthropic 读取了 Agents 段落中逐步环境 ground truth、人工检查点、阻塞和最大迭代停止；Hello-Agents 读取 chapter 1 的旅行目标分解、工具补缺、反馈变约束与动态修正，以及 chapter 7 的消息时间/metadata、历史和最大工具迭代示例；CoALA 同时读取 arXiv PDF 与完整 HTML 的 4.1、4.2、4.6 节，确认跨调用工作记忆、环境反馈和决策循环。

上述材料共同支撑“指令要清晰、缺失信息要显式处理、环境反馈应进入后续决策、当前信息需跨调用保存、运行需有停止与人工介入”的一般机制，但没有任何一份把本课的精确结构定义为标准。因此 `task contract` 的字段清单、硬约束/软偏好二分、查询/澄清/可撤销假设/blocked 四路由、state/transcript/event log 三分职责，以及 submitted/action-succeeded/business-goal-achieved 三层验收，均被正文明确标为本课程为了把一般机制落成可审查设计而采用的工程工作模型。它们属于透明的 `course synthesis`，不得反向写入四张 evidence card，亦不得声称由四份来源共同提出。

本课没有 publication blocker：一般机制、风险边界、状态与环境反馈均有可访问正文，精确课程结构被清楚标为综合推导而非外部事实；coverage gap 为“尚无单一来源定义完整工程检查表”，已通过限制声明而不是伪造来源处理。若未来要把该检查表宣称为行业标准，必须补充对应规范；当前发布只把它作为可审查课程产出。

## `agent-03` 六来源重访与接口边界

2026-07-22 重新访问本课六个来源正文：按 OpenAI Docs Skill 搜索并完整读取当前 Function Calling 官方指南，核验五步 tool-call 生命周期、JSON Schema 定义、`strict` 与 `additionalProperties: false` 的约束、多个调用以及 `call_id` / `function_call_output` 回填；读取 Anthropic 工具工程正文与既有访问记录，保留少而高信号、命名、描述、返回上下文和评测迭代的厂商经验边界；用 PDF Skill 下载并提取 Toolformer 17 页原论文，实际阅读方法、五类工具、实验、分析与 Limitations；同时沿用已核验的 Hugging Face Unit 1 正文和 Microsoft 自动字幕正文，后者保留三重重复及 Semantic Kernel 版本限制。规格复审后又实际搜索并完整读取 AWS Builders' Library 正文，canonical URL 与页面标题分别确认为 `https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/` 和 `Making retries safe with idempotent APIs`，正文作者为 Malcolm Featonby。

OpenAI 文档直接支撑本章的接口生命周期和当前 strict 语义，但不证明业务参数、权限、副作用或执行结果正确，全部时敏表述均限定为本轮核验日。AWS 正文直接支撑调用方请求标识、幂等重试、网络超时不确定性、reconciliation、语义等价响应、晚到请求与同标识改参的边界；它不规定订单审批或补偿。Toolformer 支撑“调用时机、工具选择、参数和结果利用可成为自监督训练目标”，论文同时明确当前方法不能链式或交互式用工具、对措辞敏感且不考虑工具成本；因此正文没有把它描述成生产 function calling 协议。课程据此明确 AWS caller 是宿主 API client，而非语言模型：cancel_order 的模型 schema 只提交 order_id、reason、reason_detail 业务意图；actor、tenant、稳定 intent/idempotency key 和 approval reference 由宿主从可信系统注入并持久化映射。统一结果、recoveryAction 枚举、错误码集合、五类验收表以及订单状态查询与补偿策略仍是课程综合，不反向伪装成六份来源共同定义的标准。

本课没有 publication blocker：五步生命周期、schema、调用关联、工具设计和训练研究边界均有可访问正文；精确订单字段与业务策略被清楚标为工程示例。剩余限制是供应商接口和框架会演进，真实订单权限、审批阈值、错误码与幂等存储必须由具体系统重新设计和测试，教学检查器也不构成真实模型或生产安全评测。

## `agent-04` 七来源复核与控制循环边界

2026-07-22 重新访问本课五个实质来源，并复核两个 extension 的既有访问记录，而不是从课程解释或 registry `value` 推断正文：下载并核验 ReAct 33 页原论文 PDF，同时用 arXiv 可检索正文核对其摘要、方法与实验设定；读取 Lilian Weng 综述的 ReAct、工具、挑战和连续相同动作/相同 observation 段落；读取 OpenAI Business Agent 指南中 single-agent run、exit conditions 与 human intervention，并按 OpenAI Docs Skill 读取 Developers AI application track 的 instructions、tools、guardrails、eval 与结构化输出边界；读取 Anthropic 正文 Agents 段落的逐步环境 ground truth、人工检查点、阻塞和最大迭代；读取 AgentBench 完整 arXiv HTML 的 outcome 分类、Task Limit Exceeded 与重复轨迹分析。Berkeley 的既有核验范围只有课程页和 syllabus，Datawhale Bilibili 的既有核验范围仍只有 metadata 与空字幕，二者均未进入任何实质章节 `sourceIds`。

ReAct 只直接支撑论文设定中的自然语言 reasoning traces、任务特定 actions 与 observations 交错，以及 HotpotQA、FEVER、ALFWorld、WebShop 的对照研究；正文没有把它写成通用生产控制器，更没有提出现代隐藏推理政策。课程因此明确区分：普通 CoT 可以只展开文本推理；ReAct 的机制差异是行动连接外部来源或环境并让 observation 进入后续步骤；生产日志只记录可观察 decision summary、action、validation、observation、state diff、预算与 terminal decision，不获取或依赖隐藏思维链。后一个日志约定明确标为课程工程综合。

OpenAI 与 Anthropic 共同支撑 run loop、环境反馈、停止条件和人工介入的一般原则；AgentBench 只在其八环境、29 个旧模型与具体 TLE 定义下支撑“重复、无完成与轮次终止是可观察失败轨迹”，不提供通用发生率。failed → done → handoff → blocked → budget-exhausted 的安全优先级、指纹 `tool + canonical params + relevant state/version + result class`、无进展阈值与可照抄伪代码均属于课程综合：fatal 与完成证据冲突时先返回 failed；只有完整指纹重复、相关状态无进展且达到阈值才阻止，相关 state/version 改变会产生新指纹或清零计数。页面 `agent-loop` 没有 failed/handoff，只模拟 goalSatisfied、blocked 与步数预算的本地确定性子集，因此其 goalSatisfied-first 子集不与完整控制器冲突，也不构成真实模型、工具或 ReAct 评测。

本课没有 publication blocker：两个 objectives、五 concepts、两道 quiz、三道面试题全字段、exercise 两步与 deliverable、两个 completion criteria 均有可访问核心机制证据和清晰课程综合边界。剩余限制是具体系统必须自行定义完成谓词、致命不变量、人工交接政策、状态字段、预算与重复阈值；供应商在线指南和 AgentBench 模型/环境具有版本边界。

## `agent-05` 规划论文边界与 AWS 来源复用

本课绑定的正文证据按条件化谱系使用：ReAct 只支撑行动与环境 observation 交错；Plan-and-Solve 只支撑论文静态推理设定中的先规划后求解与错误分析；ReWOO 只支撑 Planner–Worker–Solver、证据占位符、规划/观察解耦以及其任务和模型口径中的 token 效率；Tree of Thoughts 只支撑三个特制任务中的 thought 候选、状态评价、BFS/DFS、前瞻和回溯。Microsoft 自动字幕只交叉核验旧 Chapter 7 的复杂任务拆分、结构化计划、Pydantic 校验和执行示例，保留转写噪声和版本边界；`res-agent-disney-planner-bili` 仍只有 metadata 和空字幕，不进入任何章节 `sourceIds`。

Task 7 的 timeout 注入是 assessed exercise outcome，而且要求学习者不能把响应超时解释为副作用必然未发生。本课原有六个 `resourceIds` 没有直接支撑该可靠执行语义，因此复用 Task 5 已实际核验、已存在于最终 29 项 registry 的 `res-agent-aws-idempotent-apis`，仅把它加入 `agent-05.resourceIds`，不新增全局来源或改写 evidence。正文只用 AWS 支撑 API caller 的稳定 client request identifier、网络超时后的未知终态、幂等语义和 retry 前 reconciliation；不把供应商研究计划结构、查询替换、约束传播或业务 handoff 归因给 AWS。最终资源总数仍为 28 份原始基线加 1 份 AWS 审查补充，共 29 份。

步骤字段、工具能力/上下文预算/错误隔离共同决定粒度的操作规则、按失败轨迹合并或继续拆分的准则、planVersion、权限变化导致步骤/依赖/artifact 失效及其 blocked/handoff 出口、版本化步骤生命周期与 claim/CAS/fencing、三份供应商策略卡、本地教学预算和三类恢复记录均明确属于 `course synthesis`。三策略 JSON 的 actions、artifacts、dependencies、checkpoints、budgets 现在都以同一组 step ID 建立可追踪映射；原子 claim 绑定 stepId、planVersion、stepVersion、attempt、stableIntent 和 fencingToken，完成 CAS 也同时比较这六项。旧 plan、旧 stepVersion、旧 attempt、错误 stableIntent 或 stale fencing 的迟到完成只记 observation，不发布 artifact、满足 checkpoint 或解锁新依赖；只有当前六项完整 tuple 可以 commit。stable intent 明确定义为宿主生成并持久化的唯一 client request identifier/idempotency key。

页面 `plan-recovery` 仍是本地确定性教学状态机，不调用真实模型、数据源或论文实现，也不是任何规划方法或生产可靠性的评测。核心函数新增可选可信输入 `operationKind` 与 `reconciliationStatus`：默认 `read-only/not-required` 保持供应商查询教学；`side-effect + unknown` 返回 reconcile，`cannot-reconcile` 返回 handoff，只有 `confirmed-not-executed` 且预算仍有余量才 retry。UI 不增加控件，直接声明实验固定为只读供应商查询，默认 retry 不得外推到副作用。本课没有 publication blocker；剩余限制是生产系统必须自行实现持久化 claim、真实 CAS/fencing、评价器、依赖图、预算、幂等/审计和业务完成谓词。

## `agent-06` 反思证据边界与 AWS 来源复用

本课四项反思研究按反馈条件而不是按论文标题串联：Reflexion 的 Actor–Evaluator–Reflection 使用环境、启发式/LLM evaluator 或测试信号并跨 trial 保存语言经验；Self-Refine 在任务化评价维度下执行生成、反馈、修订，许多任务属于输出编辑；《Large Language Models Cannot Self-Correct Reasoning Yet》针对无外部反馈的内在推理自修正并保留 oracle、等成本与提示公平对照；CRITIC 在搜索、代码、计算器和毒性 API 等特定任务中引入工具 critique。正文没有把任一结果外推为生产可靠性保证，也没有把无反馈反证错误地扩大成“任何迭代都无效”。AgentBench 只支撑其环境中的可观察动作、重复、TLE 和终止失败；τ-bench 只支撑其模拟领域中政策与数据库终态共同判分。

写工单 timeout 是 assessed interview 与 exercise outcome，而原六个 `agent-06.resourceIds` 不直接支撑响应缺失后的副作用终态不确定性。因此本课复用已核验且存在于最终 29 项 registry 的 `res-agent-aws-idempotent-apis`，只支撑宿主调用方持久化稳定 client request identifier/idempotency key、网络 timeout 的 UNKNOWN_OUTCOME、同 intent 对账、确认未执行后才重放，以及晚到请求边界；不把工单状态机、权限、补偿、错误码、预算或六类决策表归因给 AWS。该变更只扩展 `agent-06.resourceIds`，没有新增或改写全局来源，总数仍为 28 份基线加 1 份 AWS 补充。

六类中的语义类现为 `semantic-mismatch`，覆盖结构合法但与目标、事实或 completion predicate 不一致的空与非空结果；`empty-result` 保留为子类。外部验证器显式返回 PASS、FAIL 或 VALIDATION_UNKNOWN：只有来源获准且版本匹配的 PASS 可发布/继续副作用；FAIL 产生结构化反馈并有限修订；UNKNOWN 不得提交，只能在同权限和数据最小化边界换独立 validator，否则 blocked/handoff。pre-act `actionKey = tool + canonical params + stableIntent + relevant state/version`，post-act `outcomeKey = actionKey + resultClass`；结果类别交替不重置 action 无进展或全局次数/时间/成本，只有真实前置条件或版本变化允许新 attempt，且硬预算不清零。这些状态机、预算、键与工单 JSON 均为 `course synthesis`。

## `agent-07` 工作记忆证据边界与课程综合

本课四个关联来源均沿用 Task 2 的真实正文访问记录。CoALA PDF/HTML 的 4.1、4.2 与 4.6 节直接支持跨模型调用维持当前感知、目标和中间信息的 working memory、环境反馈写回，以及情景/语义/程序记忆的概念分类；它是概念架构与综述，不是新增 benchmark，也不规定生产 event、snapshot、summary 或 context schema。Lilian Weng 综述只交叉说明短期/长期记忆、上下文窗口与 Agent 组件地图，具体研究结论仍须回原论文。Hello-Agents chapter 1/7 只交叉核验内部状态、环境反馈、消息时间/metadata 与历史的中文实践语境，不定义 transcript/event log/state 五层职责。Berkeley 只取得课程页与 syllabus，故仅在模块边界章节用作后续学习导航，不承担任何状态机制主张。

正文中的 transcript、append-only event log、derived working state、working memory、per-call context 与 artifact pointer 分层，认识分类、版本/来源/validator provenance、canonical event hash 与可信 prefix replay、summary coverage gate、reserve-aware P0–P3 packer，以及 20 轮 v2 授权写入→v3 撤权但保留 pending 的 schema 均明确为 `course synthesis`。正文内嵌 7 组 event、6 组 snapshot、13 组 gate、2 组 packing 与 9 组 pointer fixture/expected；20 轮 actual output 还直接嵌入可重算 manifest 与两条完整 pointer。前课字段被保留为 16 个真实可解析 path，没有反向归因给外部来源。

本课没有 publication blocker：working memory、环境 observation 和记忆组件的一般机制有可访问正文，精确生产结构透明标为课程综合；四个实际使用的 source ID 均在 lesson `resourceIds`、全局 registry 与 evidence set 三重解析。边界保持为单 Agent 单任务内的短期状态和本轮 context；跨会话长期记忆写入治理、检索排序、RAG、遗忘与删除政策不在本课展开。剩余限制是生产系统需自行选择事件存储、事务/哈希、token 估算、隐私保留、validator 治理和 snapshot 周期。

## `agent-08` 综合设计证据边界与课程综合

本课六个关联来源均沿用 Task 2 的真实访问记录，不新增来源。OpenAI 工程指南只支撑模型、工具、指令、单双 Agent 编排、run-loop 退出、工具风险、护栏和人工介入的一般组成；Microsoft 与 Hugging Face 课程只交叉核验 Agent、环境、工具、Think–Act–Observe 与框架实践入口。AgentBench 只用于说明其八环境中的动作、超时、重复、终止与长轨迹失败可被观察；τ-bench 只用于说明其两个模拟领域中规则遵循与数据库终态共同参与判分。两项 benchmark 均不证明只读仓库诊断 Agent 的生产可靠性。

Architecture、task contract、四个输入/输出 schema、候选 union、路径/secret/prompt-injection gate、working state、五步计划、pending/result ledger、事件化预算、三条主轨迹与 timeout/handoff 子轨迹、semantic probe 全部明确标为 `course synthesis`。模型只提交候选；真实 repo handle、tool capability、身份权限、glob/check allowlist、scanner/sandbox attestation、执行、状态、验证与终止都由宿主拥有。只读边界对每个 glob match 要求可信 resolution evidence，再做 openat-beneath、祖先 lstat、symlink/secret policy；任何百分号或缺证据都 fail closed。

Success 的四个 action 均带 canonical argsHash/actionKey、attempt 和带 state version/delta/deadline 的 reservation；observation 绑定 call/action/hash/run/tool/revision，data 通过 output schema 与参数回显，并带 scanner policy，check 另带 sandbox:// attestation。完成谓词验证 claim text、location/path+line、revision、类型匹配、备选解释和验证步骤；no-write/no-network 只接受 host readonly/network-off attestation。12 组 completion 中仅 valid 为真，null data、错误 path/checkId、stale revision、duplicate key 与 model:// 等均失败。

框架、Harness、RAG/Memory、MCP 和 multi-agent 的职责分层是本站模块路线约定：框架封装常见适配与生命周期接口；Harness 接管耐久调度、持久化、恢复、隔离、审批、可观测与部署；RAG/Memory 管上下文获取和治理；MCP 管互操作而不提供业务授权；multi-agent 只在分工、并发或隔离收益大于协调成本时引入。`res-agent-douyin-claude-code` 保留为 lesson extension，但没有出现在任何章节 `sourceIds`，不承担机制证据。本课无 publication blocker；生产限制仍包括真实文件 API 竞态、跨平台路径、secret/PII 检测、沙箱逃逸、持久事件、并发、告警与人工队列。

## 论文冲突与互补

Reflection 的正反证据必须按反馈条件分开：`res-agent-reflexion` 的闭环实际使用环境、评价器或测试信号并允许跨 trial 记忆；`res-agent-no-self-correct` 检验的是没有外部反馈时的内在自修正，发现部分推理设置会退化，因此两者并不直接矛盾。`res-agent-self-refine` 支持有明确评价维度的生成/编辑迭代，不能被扩大成任意推理自纠错；`res-agent-critic` 则说明在其特定任务中引入可查询工具的外部 critique 能提供不同于自评的证据，但工具自身仍可能错误或有偏。

规划材料形成条件化谱系：Plan-and-Solve 是低成本线性预规划，ReWOO 先形成蓝图后收集证据，ReAct 每轮吸收观察，Tree of Thoughts 显式生成、评价、搜索和回溯。课程必须根据观察依赖、分支数、延迟、搜索成本与错误代价选择，而不能把任一论文结果写成普适排名。

## 逐课 coverage matrix 骨架

以下矩阵由 Task 3–10 的单课作者按“courseFieldBasis → 教学章节 → resource evidence → 验收产出”逐项填充。目前 evidence set 与八课独立章节、覆盖和评分均已完成，但尚未接入公开课程数据。

| lesson | objectives / concepts | quiz | interview | exercise / completion | section mapping | evidence mapping | 质量分 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `agent-01` | objectives[0..1] 与 concepts[0..4] → `control-authority-spectrum`、`minimal-action-loop`、`action-space-and-environment`、`termination-evidence` | quiz[0] → `control-authority-spectrum`；quiz[1] → `minimal-action-loop`、`termination-evidence` | `iq-agent-01-1` short/deepDive/followUp → `control-authority-spectrum`；`iq-agent-01-2` 全字段 → `selection-thresholds`；`iq-agent-01-3` 全字段 → `minimal-action-loop`、`action-space-and-environment`、`termination-evidence` | exercise.steps[0..1]、deliverable → `three-case-decision-table`；completionCriteria[0] → 第 1/5/6 节，completionCriteria[1] → 第 2/3/4/6 节 | 六节按控制权连续谱→最小闭环→动作与环境→终止证据→选型门槛→三案例表递进；产出固定摘要/审批流/开放调查分别为普通调用/Workflow/Agent，并含 done、blocked、budget、handoff | 核心 `res-agent-anthropic-effective`、`res-agent-openai-guide`；闭环交叉核验 `res-agent-hf-course`、`res-agent-ms-course`、`res-agent-hello-agents`、`res-agent-lihongyi`；未使用 metadata-only `res-agent-datawhale-bili`，也未把课程目录 `res-agent-berkeley-course` 或 outline-only `res-agent-dlai-agentic` 用作机制主张 | 94（25/20/24/16/9） | ready-for-integration |
| `agent-02` | objectives[0..1] 与 concepts[0..4] → `request-to-task-contract`、`constraints-and-preferences`、`facts-assumptions-unknowns`、`state-transcript-event-log`、`completion-predicates-evidence` | quiz[0] → `facts-assumptions-unknowns`、`shanghai-travel-contract`；quiz[1] → `completion-predicates-evidence` | `iq-agent-02-1` short/deepDive/followUp → 第 1/2/3/6 节；`iq-agent-02-2` 全字段 → 第 5/6 节；`iq-agent-02-3` 全字段 → 第 3/4 节 | exercise.steps[0..1] 与 deliverable → `shanghai-travel-contract`；completionCriteria[0] → 第 1/2/3/5/6 节，completionCriteria[1] → 第 3/4 节 | 六节按请求→契约、约束、认识状态、运行状态、完成证据、差旅交付递进；上海案例逐项处理七个未知项，敏感资料明确查询授权系统→向用户/管理员澄清→仍缺失则 blocked，并禁止临时假设；同时区分 submitted/action-succeeded/business-goal-achieved | OpenAI 支撑清晰指令、缺失信息分支、退出与高风险人工介入；Anthropic 支撑环境 ground truth、阻塞检查点和停止；CoALA 支撑跨调用工作记忆、反馈与决策循环；Hello-Agents 交叉核验旅行目标分解、工具补缺和反馈修正。精确字段、四路由、三类记录与三层验收全部标为 `course synthesis` | 94（25/20/22/18/9） | ready-for-integration |
| `agent-03` | objectives[0..1] 与 concepts[0..4] → `tool-declaration-contract`、`model-proposes-function-call`、`syntax-and-schema-validation`、`business-permission-risk-validation`、`host-execution-boundary`、`structured-result-observation` | quiz[0] → 第 2/3/4 节；quiz[1] → `structured-result-observation` | `iq-agent-03-1` short/deepDive/misconception/followUp → 第 2/4/5/6 节；`iq-agent-03-2` 全字段 → 第 1/3/4/7 节；`iq-agent-03-3` 全字段 → 第 5/6 节 | exercise.steps[0..1]、deliverable → `order-tool-contract-lab`；completionCriteria[0] → 第 1/2/4/5/6 节，completionCriteria[1] → 第 1/4/6/7 节 | 七节按声明→模型提案→结构校验→业务/权限/风险→宿主 envelope→结果关联→订单实验递进；两份完整 Responses schema 仅接收业务意图，宿主注入可信身份/intent/幂等/审批字段；统一结果加入 recoveryAction，并给出五类完整可解析 JSON | OpenAI 核心支撑五步、schema、strict、nullable-required 与 call_id；Anthropic 核心支撑工具命名、边界、返回和评测；AWS 核心支撑宿主作为 API caller 使用 client request identifier、幂等重试、超时不确定性、语义等价响应与晚到请求；Toolformer 只交叉核验训练研究并保留边界；HF 与 Microsoft 仅作课程循环、最小权限和错误处理交叉核验 | 98（25/20/25/19/9） | ready-for-integration |
| `agent-04` | objectives[0..1] 与 concepts[0..4] → `state-read-and-termination-priority`、`decide-from-observable-state`、`validate-and-act-boundary`、`observe-and-update-state`、`progress-and-repetition-detection`、`react-boundary-and-observable-logging` | quiz[0] → `observe-and-update-state`；quiz[1] → `progress-and-repetition-detection` | `iq-agent-04-1` short/deepDive/misconception/followUp → 第 1/3/4/5/7 节；`iq-agent-04-2` 全字段 → 第 2/6 节；`iq-agent-04-3` 全字段 → 第 4/5/7 节 | exercise.steps[0..1]、deliverable → `minimal-loop-and-deterministic-lab`；completionCriteria[0] → 第 1–5/7 节，completionCriteria[1] → 第 2/6 节 | failed-first→一次性 reconciliation receipt→原子预算→全候选指纹→call-tool 校验→writer lease/CAS/唯一 claim→观察/action 指纹；重放 observation 只消费一次并进入正常控制流，winner 释放 lease 后 loser 仍被 claim 拦截，stale CAS 不可 act | ReAct 原论文支撑交错 reasoning/action/observation；OpenAI/Anthropic 支撑 run、环境反馈、最大迭代与人工介入；AgentBench 支撑其评测中的重复/终止失败；receipt、lease、CAS、claim、预算与阈值均为课程工程综合 | 97（25/20/25/18/9） | ready-for-integration |
| `agent-05` | objectives[0..1] 与 concepts[0..4] → `choose-planning-by-task-structure`、`decompose-into-verifiable-work`、`plans-as-executable-dataflow`、`search-planning-and-evaluation-cost`、`observation-driven-replanning` | quiz[0] → 第 1/2/6 节；quiz[1] → 第 5/7 节 | `iq-agent-05-1` 全字段 → 第 1/6 节；`iq-agent-05-2` 全字段 → 第 2/3 节；`iq-agent-05-3` 全字段 → 第 5/7 节 | exercise.steps[0]、deliverable 三策略 → `supplier-research-strategy-board`；steps[1]、修订记录 → `failure-injections-and-lab-boundary`；completionCriteria[0] → 第 1/6 节，completionCriteria[1] → 第 2/5/7 节 | 七节按策略选择→可验证分解→版本化数据流与迟到完成隔离→ToT 搜索成本→观察驱动修订→供应商三策略棋盘→三类注入递进；每张策略卡的五类字段共享 step ID；pending→ready→running→终态使用原子 claim 和 CAS/fencing；timeout 副作用按对账状态分流 | ReAct 支撑行动/观察交错；Plan-and-Solve、ReWOO、ToT 只支撑各论文机制与实验边界；Microsoft 字幕仅交叉核验结构化计划/校验；复用 Task 5 已核验 AWS 来源只支撑 client idempotency、timeout unknown outcome 与 retry 前 reconciliation。版本协议、触发分类、棋盘与预算均标为 `course synthesis`；metadata-only Bilibili 不作章节 evidence | 97（25/20/24/19/9） | ready-for-integration |
| `agent-06` | objectives[0..1] 与 concepts[0..4] → `observe-before-recover`、`retry-budget-and-unknown-outcome`、`reflection-needs-feedback`、`external-validation-stack`、`fingerprint-and-progress-detection` | quiz[0] → 第 1/2/6 节；quiz[1] → 第 3/4/7 节 | `iq-agent-06-1` 全字段 → 第 1/2/6 节；`iq-agent-06-2` 全字段 → 第 3/4/7 节；`iq-agent-06-3` 全字段 → 第 5/7 节 | exercise.steps[0] → 六类表，semantic-mismatch 同时含 empty 与 nonempty mismatch；steps[1]/deliverable → timeout 对账、双键预算、验证三态；completionCriteria[0] → 第 1/2/5/6 节，completionCriteria[1] → 第 3/4/7 节 | 七节按证据/六类→预算/幂等→反思证据→PASS/FAIL/UNKNOWN→actionKey/outcomeKey 与硬预算→JSON 表→可执行 probe 递进；UNKNOWN 不提交，结果类别交替仍到限停止 | 四项反思研究按反馈条件并列；AgentBench/τ-bench 只支撑可观察轨迹与终态；AWS 只支撑 caller intent、timeout unknown 与 retry 前 reconciliation。semantic-mismatch、验证三态、双键和预算均为 `course synthesis` | 98（25/20/25/19/9） | ready-for-integration |
| `agent-07` | objectives[0..1] 与 concepts[0..4] → `information-carriers-and-lifecycles`、`epistemic-records-and-provenance`、`event-replay-and-derived-state`、`context-assembly-and-invariant-gates` | quiz[0] → 第 1/3 节；quiz[1] → 第 4/5/6 节 | `iq-agent-07-1` 全字段 → 第 1/3/6 节；`iq-agent-07-2` 全字段 → 第 4/5/6 节；`iq-agent-07-3` 全字段 → 第 2/5/6 节 | exercise.steps[0] → 第 2/3/5 节的事件索引、state、工作记忆；steps[1]/deliverable → 第 4/5/6 节的 context、删减依据和 probe；completionCriteria[0] → 第 1/3/5 节，completionCriteria[1] → 第 2/5/6 节 | 六节按载体职责→认识状态→事件/snapshot 完整性→context gate→v2/v3 actual output→语义 probe 递进；16 path 与 7/6/13/2/9 组 event/snapshot/gate/packing/pointer fixture 覆盖可信前缀、撤权、summary 缺口与 pointer 边界；actual manifest/pointers 也由同一算法验收 | CoALA 核心支撑跨调用工作记忆、环境反馈与概念分类；Weng/Hello-Agents 只交叉核验组件地图、状态/消息实践；Berkeley 仅作后续导航。五层职责、生产 schema、重放/摘要/裁剪规则和 probe 均标 `course synthesis` | 97（25/20/24/19/9） | ready-for-integration |
| `agent-08` | objectives[0..1] 与 concepts[0..4] → `capstone-architecture-and-trust-boundaries`、`task-contract-and-evidence-predicate`、`trusted-readonly-tools-and-security-gates`、`state-plan-loop-and-recovery`、`handwritten-loop-framework-and-harness`、`module-boundaries-and-semantic-probes` | quiz[0] → 第 2/5/6 节的宿主重算完成谓词与框架边界；quiz[1] → 第 1/3/7 节的工具连接、宿主 loop 与 MCP 边界 | `iq-agent-08-1` short/deepDive/misconception/followUp → 第 1–5/7 节及 success/clarify/blocked/timeout/handoff；`iq-agent-08-2` 全字段 → 第 6 节；`iq-agent-08-3` 全字段 → 第 6/7 节 | exercise.steps[0] → architecture/task/tools/state JSON；steps[1] → `three-auditable-terminal-traces` 的三主轨与 timeout/handoff；steps[2] → 第 6/7 节边界；deliverable → 五份 contract JSON、五类轨迹和 semantic probe；completionCriteria[0] → 第 1–5 节，completionCriteria[1] → 第 6/7 节 | 七节按可信架构→任务/完成证据→工具/逐 match 安全→pending/ledger/事件预算→五类 terminal/recovery→框架/Harness→语义验收递进；结果 payloadHash 覆盖 success/data/error/contentTrust/resourceVersion；completion 遍历全部 claims/constraints；budget replay 绑定 phase/delta/deadline/action chain；handoff packet 与 TIMEOUT route 精确绑定；35 completion、29 recovery、11 reconcile、15 security 均由 probe 重算 | OpenAI/MS/HF 只支撑 Agent 组成、工具、循环、风险和框架路线；AgentBench/τ-bench 只支撑各自评测中的长轨迹、规则与终态边界。全部精确 input/output schema、候选 union、resolution/scanner/sandbox attestation、pending/ledger、reservation event、handoff 包和模块职责分层均标为 `course synthesis`；抖音 extension 不作章节 evidence | 98（25/20/24/20/9） | ready-for-integration |

## 当前测试审计

`tests.status: failed`，原因是当前公开课程数据与聚合入口仍未接入八课 `knowledgeNote`：`agent-01` 至 `agent-08` 均已有通过单课 probe 的独立文件，但尚未接入。evidence 与引用测试已通过，集成 RED 没有被改写为 `not applicable`。

| command | exit code | result |
| --- | ---: | --- |
| `node --test --test-name-pattern="resources\|evidence\|references" tests/agent-mechanism-data.test.js` | 0 | 3 项命中测试通过，9 项因名称不匹配跳过；资源数量、29 张 evidence 卡、双向引用和日期均通过；日期需真实、不晚于审计日且属于 `2026-07-20`/`2026-07-22` 允许集合，当前 registry 实值均为本轮重访的 `2026-07-22` |
| `node --test --test-name-pattern='contains eight\|29 fixed\|evidence cards\|references resolve\|interview bank\|identifiers' tests/agent-mechanism-data.test.js` | 0 | 6 项命中测试全部通过，6 项因名称不匹配跳过；29 项资源、evidence、双向引用、面试与 ID 完整性通过，新增的 agent-06 → AWS 关联可解析 |
| `npm test -- --test-name-pattern='Agent mechanism\|Agent'` | 1 | 共 261 项，11 通过、245 跳过、5 个预期 RED：公开课程仍未接八课 `knowledgeNote`、缺聚合 registry/冻结、Agent UI 笔记和 README 发布声明；agent-07 独立文件、资源 evidence 与 Agent 实验回归通过 |
| `npm test` | 1 | 共 261 项，256 通过、5 个预期 RED，均与上一行的 Agent 笔记集成缺口相同；新增副作用超时对账反例均通过，Agent 29 份资源与其他模块回归均通过 |
| `node --test tests/agent-mechanism.test.js` | 0 | 26/26 通过；包含 read-only 默认兼容、side-effect unknown→reconcile、cannot-reconcile→handoff、confirmed-not-executed 且有预算→retry、无重复 act、枚举校验和输入不突变 |
| `node --test tests/ui-interactions.test.js` | 0 | 43/43 通过；plan-recovery caveat 明示固定只读供应商查询，retry 仅适用于无副作用读操作，原交互分支与 reset 均保持通过 |
| `node --check src/data/agent-mechanism.js` | 0 | 语法通过 |
| `node --check src/core/agent-mechanism.js` | 0 | 语法通过 |
| `node --check src/ui/agent-experiments.js` | 0 | 语法通过 |
| `node --check tests/agent-mechanism.test.js` | 0 | 语法通过 |
| `node --check tests/ui-interactions.test.js` | 0 | 语法通过 |
| `node --check tests/agent-mechanism-data.test.js` | 0 | 语法通过 |
| `node --check src/data/agent-mechanism-notes/agent-02.js` | 0 | `agent-02` 独立笔记语法通过 |
| `node --check src/data/agent-mechanism-notes/agent-03.js` | 0 | `agent-03` 独立笔记语法通过 |
| `node --check src/data/agent-mechanism-notes/agent-04.js` | 0 | `agent-04` 独立笔记语法通过 |
| `node --check src/data/agent-mechanism-notes/agent-05.js` | 0 | `agent-05` 独立笔记语法通过 |
| `node --check src/data/agent-mechanism-notes/agent-06.js` | 0 | `agent-06` 独立笔记语法通过 |
| `node --check src/data/agent-mechanism-notes/agent-07.js` | 0 | `agent-07` 独立笔记语法通过 |
| `git diff --check` | 0 | 本轮至 `agent-07` 独立笔记与审计差异无空白错误 |
| 下方“`agent-02` 可复制 contract/source/freeze probe”完整 one-liner | 0 | 29 分钟；6 个唯一 section ID；测试口径（introduction + paragraphs + nextStep）正文 3935 字符；每节 3–4 段、每段最短 146 字符、至少 2 个要点、非空 source 数组；5 个误区、8 个 recap；所有嵌套值冻结；4 个来源均通过全局 registry、lesson resourceIds 和 evidence set 三重解析，`brokenReferenceCount: 0` |
| 下方“`agent-03` 可复制 contract/source/freeze probe”完整 one-liner | 0 | 36 分钟；7 个唯一 section ID；正文 7715 字符且不超过 9000；每节 3 段、每段最短 196 字符、至少 2 个要点、非空且无重复 source 数组；6 个误区、11 个 recap；所有嵌套值冻结；6 个来源均三重解析；两份 Responses schema 与五类 result JSON 均实际 parse，cancel_order 只含 order_id/reason/reason_detail，validation 为 false+repair_input、审批为 false+request_approval，AWS caller/UNKNOWN_OUTCOME reconcile 断言通过，`brokenReferenceCount: 0` |
| 下方“`agent-04` 可复制 contract/source/freeze probe”完整 one-liner | 0 | 37 分钟；7 个唯一 section ID；正文 8985 字符且不超过 9000；每节 3 段、每段最短 140 字符、至少 2 个要点；6 个误区、11 个 recap；深层冻结；5 个来源三重解析，`brokenReferenceCount: 0` |
| 下方“`agent-04` 控制语义 probe”完整 one-liner | 0 | 除 receipt 重放、唯一 claim 与 stale CAS 外，新增恶意候选自报 `supports_idempotency:true`、可信 registry 为 false 的反例；宿主仍调用串行对账门一次，确认安全前 `actCount=0`，并断言正文不存在 `candidate.tool_supports_idempotency` |
| 下方“`agent-05` 可复制 contract/source/freeze probe”完整 one-liner | 0 | 38 分钟；7 个唯一 section ID；正文 8330 字符且不超过 9000；每节 3–4 段、每段最短 181 字符、至少 2 个要点；6 个误区、10 个 recap；深层冻结；6 个来源三重解析，`brokenReferenceCount: 0` |
| 下方“`agent-05` 策略棋盘与恢复语义 probe”完整 one-liner | 0 | 三策略的 actions/artifacts/dependencies/checkpoints/budgets 共享并解析同一组 step ID；三类恢复记录、timeout 对账、分解粒度、权限传播与实验边界断言通过 |
| 下方“`agent-05` 版本化步骤与迟到完成 probe”完整 one-liner | 0 | 解析七状态与 stepId、planVersion、stepVersion、attempt、stableIntent、fencingToken 六元 claim key；old plan、same-plan old stepVersion、same-step old attempt、wrong stableIntent、stale fencing 五反例均 observation-only，不能发布 artifact、满足 checkpoint 或解锁依赖；只有当前六项完整 tuple 可发布；副作用反例保持 `actCount: 1` |
| 下方“`agent-06` 可复制 contract/source/freeze probe”完整 one-liner | 0 | 39 分钟；正文 8806；7 节；最短段 126；7 个来源三重解析；6 个误区、11 recap；深层冻结；`brokenReferenceCount: 0` |
| 下方 `agent-06` 三条语义 probe | 0 | 确定性 classifier 的 empty/nonempty-irrelevant/matched 三案例；PASS/FAIL/UNKNOWN 与 stale PASS/FAIL；actionKey/outcomeKey、结果交替到限、真实版本只清局部且硬预算不清零均通过 |
| 下方“`agent-07` 可复制 contract/source/freeze probe”完整 one-liner | 0 | 34 分钟；正文 8930；6 节；最短段 119；4 个来源三重解析；observation provenance 与 state pointer 嵌套契约通过；6 个误区、11 recap；深层冻结；`brokenReferenceCount: 0` |
| 下方 `agent-07` 四条语义 probe | 0 | 7 event/6 snapshot expected 匹配；13 gate 含 reconcileBeforeAct=false 与 partial range；2 个 fixture packer 匹配，20 轮 actual manifest 由其 blocks 重算后 deepEqual；9 pointer fixture 匹配，actual event-range/artifact 两指针也通过同一 resolver 且仅 observation |

### `agent-02` 可复制 contract/source/freeze probe

以下命令不是注释或占位符；它实际导入 `agent02Note` 与公开课程 registry，断言阅读量、测试口径长度、六节结构与唯一 ID、段落与要点、非空 source 数组及三重解析、误区、recap 和深层冻结，并输出本轮记录的摘要：

```bash
node --input-type=module -e 'import assert from "node:assert/strict"; const [{agent02Note:n},{agentMechanism:c}]=await Promise.all([import("./src/data/agent-mechanism-notes/agent-02.js"),import("./src/data/agent-mechanism.js")]); const lesson=c.lessons.find(({id})=>id==="agent-02"); const globalIds=new Set(c.resources.map(({id})=>id)); const evidenceIds=new Set(c.resources.filter(({evidence})=>evidence).map(({id})=>id)); const body=[n.introduction,...n.sections.flatMap(({paragraphs})=>paragraphs),n.nextStep].reduce((sum,value)=>sum+value.trim().length,0); const sectionIds=n.sections.map(({id})=>id); const deepFrozen=(value)=>{if(value&&typeof value==="object"){assert.equal(Object.isFrozen(value),true); Object.values(value).forEach(deepFrozen)}}; assert.equal(n.readingMinutes,29); assert.equal(n.sections.length,6); assert.equal(new Set(sectionIds).size,sectionIds.length); assert.ok(body>=3600); assert.ok(n.sections.every(({id,paragraphs,keyPoints,sourceIds})=>/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(id)&&paragraphs.length>=2&&paragraphs.length<=4&&paragraphs.every((p)=>p.trim().length>=60)&&keyPoints.length>=2&&Array.isArray(sourceIds)&&sourceIds.length>=1&&sourceIds.every((sourceId)=>globalIds.has(sourceId)&&lesson.resourceIds.includes(sourceId)&&evidenceIds.has(sourceId)))); assert.ok(n.misconceptions.length>=4&&n.misconceptions.length<=6); assert.ok(n.recap.length>=5); deepFrozen(n); console.log(JSON.stringify({body,sections:n.sections.length,uniqueSectionIds:true,nonEmptySourceArrays:true,minParagraph:Math.min(...n.sections.flatMap(({paragraphs})=>paragraphs.map((p)=>p.trim().length))),sources:[...new Set(n.sections.flatMap(({sourceIds})=>sourceIds))],brokenReferenceCount:0,deepFrozen:true}))'
```

本轮输出：`{"body":3935,"sections":6,"uniqueSectionIds":true,"nonEmptySourceArrays":true,"minParagraph":146,"sources":["res-agent-openai-guide","res-agent-anthropic-effective","res-agent-hello-agents","res-agent-coala"],"brokenReferenceCount":0,"deepFrozen":true}`。

### `agent-03` 可复制 contract/source/freeze probe

以下命令实际导入 `agent03Note` 与公开课程 registry；它 `JSON.parse` 两份工具定义和五类结果数组，断言 cancel_order 的模型字段只含 order_id、reason、reason_detail，严格排除 idempotency_key 与 approval_token；再断言 validation error 为 false+repair_input、审批为 false+request_approval、稳定 recoveryAction 枚举、AWS caller 是宿主、UNKNOWN_OUTCOME 走 false+reconcile、正文在 4500–9000 字符、source 三重解析和深层冻结：

```bash
node --input-type=module -e 'import assert from "node:assert/strict"; const [{agent03Note:n},{agentMechanism:c}]=await Promise.all([import("./src/data/agent-mechanism-notes/agent-03.js"),import("./src/data/agent-mechanism.js")]); const parseObject=p=>JSON.parse(p.slice(p.indexOf("{"),p.lastIndexOf("}")+1)); const parseArray=p=>JSON.parse(p.slice(p.indexOf("["),p.lastIndexOf("]")+1)); const get=parseObject(n.sections[0].paragraphs[2]); const cancel=parseObject(n.sections[3].paragraphs[1]); const results=parseArray(n.sections[6].paragraphs[1]); for(const tool of [get,cancel]){assert.equal(tool.type,"function");assert.equal(tool.strict,true);assert.equal(tool.parameters.type,"object");assert.equal(tool.parameters.additionalProperties,false);assert.deepEqual(new Set(tool.parameters.required),new Set(Object.keys(tool.parameters.properties)));} assert.deepEqual(Object.keys(cancel.parameters.properties),["order_id","reason","reason_detail"]);assert.deepEqual(cancel.parameters.properties.reason_detail.type,["string","null"]);assert.equal("idempotency_key" in cancel.parameters.properties,false);assert.equal("approval_token" in cancel.parameters.properties,false);assert.equal(results.length,5);const byScenario=Object.fromEntries(results.map(x=>[x.scenario,x.result]));assert.deepEqual(byScenario.valid_query,{success:true,data:{order_id:"ord_2048",status:"processing",cancellable:true},error:null,recoveryAction:"none",evidenceRef:"evt_order_2048"});for(const key of ["missing_order_id","invalid_view_enum","unexpected_debug_property"]){assert.equal(byScenario[key].success,false);assert.equal(byScenario[key].error.retryable,false);assert.equal(byScenario[key].recoveryAction,"repair_input");}assert.equal(byScenario.high_risk_cancel.error.retryable,false);assert.equal(byScenario.high_risk_cancel.recoveryAction,"request_approval");const allowed=new Set(["none","repair_input","request_approval","retry","reconcile","blocked"]);assert.ok(results.every(({result})=>allowed.has(result.recoveryAction)&&Object.keys(result).sort().join(",")==="data,error,evidenceRef,recoveryAction,success"));const host=n.sections[4].paragraphs.join(" ");assert.match(host,/caller 是作为 API client 的宿主/);assert.match(host,/UNKNOWN_OUTCOME、retryable 为 false、recoveryAction 为 reconcile/);const body=[n.introduction,...n.sections.flatMap(({paragraphs})=>paragraphs),n.nextStep].reduce((sum,v)=>sum+v.trim().length,0);assert.ok(body>=4500&&body<=9000);const lesson=c.lessons.find(({id})=>id==="agent-03");const globalIds=new Set(c.resources.map(({id})=>id));const evidenceIds=new Set(c.resources.filter(({evidence})=>evidence).map(({id})=>id));const sources=[...new Set(n.sections.flatMap(({sourceIds})=>sourceIds))];assert.ok(sources.every(id=>globalIds.has(id)&&lesson.resourceIds.includes(id)&&evidenceIds.has(id)));const deepFrozen=v=>{if(v&&typeof v==="object"){assert.equal(Object.isFrozen(v),true);Object.values(v).forEach(deepFrozen)}};deepFrozen(n);console.log(JSON.stringify({body,readingMinutes:n.readingMinutes,sections:n.sections.length,minParagraph:Math.min(...n.sections.flatMap(({paragraphs})=>paragraphs.map(p=>p.trim().length))),sources,misconceptions:n.misconceptions.length,recap:n.recap.length,jsonDefinitions:[get.name,cancel.name],resultExamples:results.length,cancelModelProperties:Object.keys(cancel.parameters.properties),recoveryActions:[...new Set(results.map(({result})=>result.recoveryAction))],brokenReferenceCount:0,deepFrozen:true}))'
```

本轮输出：`{"body":7715,"readingMinutes":36,"sections":7,"minParagraph":196,"sources":["res-agent-openai-function","res-agent-anthropic-tools","res-agent-ms-tool-video","res-agent-toolformer","res-agent-hf-course","res-agent-aws-idempotent-apis"],"misconceptions":6,"recap":11,"jsonDefinitions":["get_order","cancel_order"],"resultExamples":5,"cancelModelProperties":["order_id","reason","reason_detail"],"recoveryActions":["none","repair_input","request_approval"],"brokenReferenceCount":0,"deepFrozen":true}`。

### `agent-04` 可复制 contract/source/freeze probe

以下命令实际导入 `agent04Note` 与课程 registry，断言阅读量、正文、七节、kebab-case 唯一 ID、段落与要点、source 去重及三重解析、误区、recap 和全部嵌套值冻结：

```bash
node --input-type=module -e 'import assert from "node:assert/strict"; const [{agent04Note:n},{agentMechanism:c}]=await Promise.all([import("./src/data/agent-mechanism-notes/agent-04.js"),import("./src/data/agent-mechanism.js")]); const lesson=c.lessons.find(({id})=>id==="agent-04"); const registry=new Map(c.resources.map(r=>[r.id,r])); const body=[n.introduction,...n.sections.flatMap(s=>s.paragraphs),n.nextStep].reduce((a,s)=>a+s.trim().length,0); const ids=n.sections.map(s=>s.id); const allSources=n.sections.flatMap(s=>s.sourceIds); const deepFrozen=v=>{if(v&&typeof v==="object"){assert.equal(Object.isFrozen(v),true);Object.values(v).forEach(deepFrozen)}}; assert.ok(n.readingMinutes>=30&&n.readingMinutes<=40);assert.ok(body>=4500&&body<=9000);assert.equal(n.sections.length,7);assert.equal(new Set(ids).size,ids.length);assert.ok(n.sections.every(s=>/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(s.id)&&s.paragraphs.length>=2&&s.paragraphs.length<=4&&s.paragraphs.every(p=>p.trim().length>=60)&&s.keyPoints.length>=2&&s.sourceIds.length>=1&&new Set(s.sourceIds).size===s.sourceIds.length&&s.sourceIds.every(id=>lesson.resourceIds.includes(id)&&registry.has(id)&&registry.get(id).evidence)));assert.ok(n.misconceptions.length>=4&&n.misconceptions.length<=6);assert.ok(n.recap.length>=5);deepFrozen(n);console.log(JSON.stringify({readingMinutes:n.readingMinutes,body,sections:n.sections.length,minParagraph:Math.min(...n.sections.flatMap(s=>s.paragraphs.map(p=>p.trim().length))),sources:[...new Set(allSources)],misconceptions:n.misconceptions.length,recap:n.recap.length,deepFrozen:true,brokenReferenceCount:0}))'
```

本轮输出：`{"readingMinutes":37,"body":8985,"sections":7,"minParagraph":140,"sources":["res-agent-anthropic-effective","res-agent-openai-guide","res-agent-agentbench","res-agent-react-paper","res-agent-lilian-weng"],"misconceptions":6,"recap":11,"deepFrozen":true,"brokenReferenceCount":0}`。

### `agent-04` 可复制控制语义 probe

以下命令还验证幂等策略的可信边界：candidate 即使自报支持幂等，也只能提供 tool/params；宿主必须查询 trusted registry。registry 为 false 时仍进入串行对账门，未确认安全前不得 act。其余 unknown、receipt 重放、预算拒绝、双 worker、CAS 与指纹状态机继续执行：

```bash
node --input-type=module -e 'import assert from "node:assert/strict"; const {agent04Note:n}=await import("./src/data/agent-mechanism-notes/agent-04.js"); const body=[n.introduction,...n.sections.flatMap(s=>s.paragraphs),n.nextStep].reduce((a,s)=>a+s.trim().length,0); const loop=n.sections.find(s=>s.id==="minimal-loop-and-deterministic-lab").paragraphs.join(" "); const repeat=n.sections.find(s=>s.id==="progress-and-repetition-detection").paragraphs.join(" "); for(const token of ["atomic_reserve_and_charge_budget","record_budget_exhausted","update_decision_progress","no_progress_threshold","acquire_task_writer_lease","get_or_create_intent(task, candidate)","claim_and_persist_pending_action","unique_key:[task.id,stable_intent]","expected_state_version","idempotency_mapping","serialize_and_reconcile_before_act","holds_writer_lease","cas_task_state_version","reconcile_or_handoff_existing_claim","validate(candidate","act(candidate","normalize_observation","update_state","mark_pending_reconciled_and_record_observation","already_consumed","apply_reconciliation_receipt","recovery_threshold","trusted_tool_registry.require(candidate.tool)","tool.supports_idempotency"])assert.ok(loop.includes(token),token); assert.match(loop,/priority=\[failed, done, handoff, blocked, budget-exhausted\]/); assert.equal(loop.match(/reconcile_pending/g).length,1); const unknown=loop.slice(loop.indexOf("if(recovery.unknown)"),loop.indexOf("view=read_state_view")); for(const token of ["mark_pending_requires_human","auto_reconcile:false","evidenceRef:recovery.evidenceRef","record_handoff_request","return terminal_result(\"handoff\""])assert.ok(unknown.includes(token),token); assert.doesNotMatch(unknown,/continue|act\(/); let reconcileCount=0,unknownActCount=0; const simulateUnknown=()=>{reconcileCount+=1;const recovery={unknown:true,evidenceRef:"ev-1"};if(recovery.unknown)return {status:"handoff",evidenceRef:recovery.evidenceRef};unknownActCount+=1}; assert.deepEqual(simulateUnknown(),{status:"handoff",evidenceRef:"ev-1"}); assert.deepEqual({reconcileCount,unknownActCount},{reconcileCount:1,unknownActCount:0}); const consumed=new Set();let appliedObservations=0,nextPhase=null;const ingestObservation=key=>{const already_consumed=consumed.has(key);if(!already_consumed){consumed.add(key);appliedObservations+=1;}nextPhase="terminal-or-decision";return {already_consumed}};const firstReceipt=ingestObservation("pending-1/call-1"),secondReceipt=ingestObservation("pending-1/call-1");assert.deepEqual({first:firstReceipt.already_consumed,second:secondReceipt.already_consumed,appliedObservations,nextPhase},{first:false,second:true,appliedObservations:1,nextPhase:"terminal-or-decision"}); const simulateRejected=kind=>{const s={turns:5,noProgress:0,status:null};let decisions=0;while(!s.status){if(s.turns===0){s.status="budget-exhausted";break;}s.turns-=1;decisions+=1;s.noProgress+=1;if(s.noProgress>=3)s.status="handoff";}return {kind,status:s.status,decisions,turns:s.turns}}; const rejected=Object.fromEntries(["invalid","validation-reject","finish-reject"].map(k=>[k,simulateRejected(k)])); for(const r of Object.values(rejected))assert.deepEqual({status:r.status,decisions:r.decisions,turns:r.turns},{status:"handoff",decisions:3,turns:2}); const shared={lease:null,claim:null,actCount:0,reconciles:0,stateVersion:7};const winner=()=>{shared.lease="w1";shared.claim=["task-1","intent-1"];shared.actCount+=1;shared.lease=null;return "acted"};const loserAfterRelease=()=>{assert.equal(shared.lease,null);shared.lease="w2";if(shared.claim){shared.reconciles+=1;shared.lease=null;return "reconcile";}shared.actCount+=1;return "acted"};const staleCasWorker=()=>{shared.lease="w3";const expectedStateVersion=6;if(expectedStateVersion!==shared.stateVersion){shared.reconciles+=1;shared.lease=null;return "handoff";}shared.actCount+=1;return "acted"};const winnerOutcome=winner(),claimLoserOutcome=loserAfterRelease(),staleCasOutcome=staleCasWorker();assert.deepEqual({winnerOutcome,claimLoserOutcome,staleCasOutcome,actCount:shared.actCount,reconciles:shared.reconciles,lease:shared.lease},{winnerOutcome:"acted",claimLoserOutcome:"reconcile",staleCasOutcome:"handoff",actCount:1,reconciles:2,lease:null}); const trustedRegistry={risky_write:{supports_idempotency:false}};const modelCandidate={tool:"risky_write",params:{value:1},tool_supports_idempotency:true};const policyTool=trustedRegistry[modelCandidate.tool];let policyGateCalls=0,policyActCount=0;if(!policyTool.supports_idempotency){policyGateCalls+=1;const safe_to_act=false;if(safe_to_act)policyActCount+=1;}else policyActCount+=1;assert.deepEqual({candidateClaim:modelCandidate.tool_supports_idempotency,registryValue:policyTool.supports_idempotency,policyGateCalls,policyActCount},{candidateClaim:true,registryValue:false,policyGateCalls:1,policyActCount:0});assert.doesNotMatch(loop,/candidate\.tool_supports_idempotency/); assert.ok(loop.indexOf("candidate=decide")<loop.indexOf("validation=validate")&&loop.indexOf("validation=validate")<loop.indexOf("raw=act")&&loop.indexOf("raw=act")<loop.indexOf("obs=normalize_observation")&&loop.indexOf("obs=normalize_observation")<loop.lastIndexOf("next=update_state")); assert.match(repeat,/state\/version 改变也会形成新完整指纹/); assert.ok(body>=4500&&body<=9000); console.log(JSON.stringify({body,unknown:{reconcileCount,actCount:unknownActCount,status:"handoff"},rejected,receiptReplay:{firstAlreadyConsumed:firstReceipt.already_consumed,secondAlreadyConsumed:secondReceipt.already_consumed,appliedObservations,nextPhase},idempotencyPolicy:{candidateClaim:modelCandidate.tool_supports_idempotency,registryValue:policyTool.supports_idempotency,gateCalls:policyGateCalls,actCount:policyActCount},workers:{winnerOutcome,claimLoserOutcome,staleCasOutcome,actCount:shared.actCount,reconciles:shared.reconciles},budgetChargedBeforeDecision:loop.indexOf("atomic_reserve_and_charge_budget")<loop.indexOf("candidate=decide"),decisionFingerprint:loop.includes("update_decision_progress"),actionFingerprint:loop.includes("update_progress"),uniqueClaimBlocked:claimLoserOutcome==="reconcile",staleCasBlocked:staleCasOutcome==="handoff",toolOrder:true}))'
```

本轮输出：`{"body":8985,"unknown":{"reconcileCount":1,"actCount":0,"status":"handoff"},"rejected":{"invalid":{"kind":"invalid","status":"handoff","decisions":3,"turns":2},"validation-reject":{"kind":"validation-reject","status":"handoff","decisions":3,"turns":2},"finish-reject":{"kind":"finish-reject","status":"handoff","decisions":3,"turns":2}},"receiptReplay":{"firstAlreadyConsumed":false,"secondAlreadyConsumed":true,"appliedObservations":1,"nextPhase":"terminal-or-decision"},"idempotencyPolicy":{"candidateClaim":true,"registryValue":false,"gateCalls":1,"actCount":0},"workers":{"winnerOutcome":"acted","claimLoserOutcome":"reconcile","staleCasOutcome":"handoff","actCount":1,"reconciles":2},"budgetChargedBeforeDecision":true,"decisionFingerprint":true,"actionFingerprint":true,"uniqueClaimBlocked":true,"staleCasBlocked":true,"toolOrder":true}`。

### `agent-05` 可复制 contract/source/freeze probe

以下命令实际导入 `agent05Note` 与课程 registry，断言阅读量、正文、七节、kebab-case 唯一 ID、段落、要点、source 去重与三重解析、误区、recap 及全部嵌套值冻结：

```bash
node --input-type=module -e 'import assert from "node:assert/strict";const [{agent05Note:n},{agentMechanism:c}]=await Promise.all([import("./src/data/agent-mechanism-notes/agent-05.js"),import("./src/data/agent-mechanism.js")]);const lesson=c.lessons.find(x=>x.id==="agent-05");const reg=new Map(c.resources.map(x=>[x.id,x]));const body=[n.introduction,...n.sections.flatMap(s=>s.paragraphs),n.nextStep].reduce((a,s)=>a+s.trim().length,0);const ids=n.sections.map(s=>s.id);const deep=v=>{if(v&&typeof v==="object"){assert.equal(Object.isFrozen(v),true);Object.values(v).forEach(deep)}};assert.ok(n.readingMinutes>=30&&n.readingMinutes<=40);assert.equal(n.sections.length,7);assert.equal(new Set(ids).size,ids.length);assert.ok(body>=4800&&body<=9000);assert.ok(n.sections.every(s=>/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(s.id)&&s.paragraphs.length>=2&&s.paragraphs.length<=4&&s.paragraphs.every(p=>p.trim().length>=60)&&s.keyPoints.length>=2&&s.sourceIds.length>=1&&new Set(s.sourceIds).size===s.sourceIds.length&&s.sourceIds.every(id=>lesson.resourceIds.includes(id)&&reg.has(id)&&reg.get(id).evidence)));assert.ok(n.misconceptions.length>=4&&n.misconceptions.length<=6);assert.ok(n.recap.length>=5);deep(n);console.log(JSON.stringify({readingMinutes:n.readingMinutes,body,sections:n.sections.length,minParagraph:Math.min(...n.sections.flatMap(s=>s.paragraphs.map(p=>p.trim().length))),sources:[...new Set(n.sections.flatMap(s=>s.sourceIds))],misconceptions:n.misconceptions.length,recap:n.recap.length,deepFrozen:true,brokenReferenceCount:0}))'
```

本轮输出：`{"readingMinutes":38,"body":8330,"sections":7,"minParagraph":181,"sources":["res-agent-react-paper","res-agent-plan-solve","res-agent-ms-plan-video","res-agent-rewoo","res-agent-tot-paper","res-agent-aws-idempotent-apis"],"misconceptions":6,"recap":10,"deepFrozen":true,"brokenReferenceCount":0}`。

### `agent-05` 可复制策略棋盘与恢复语义 probe

以下命令实际解析正文中的三份策略 JSON 和三类恢复 JSON；每份策略的 actions、artifacts、dependencies、checkpoints、budgets 必须共享相同 step ID，依赖只能指向本策略步骤。它还断言 timeout 对账、分解粒度、权限传播、course synthesis 与本地实验边界：

```bash
node --input-type=module -e 'import assert from "node:assert/strict";const {agent05Note:n}=await import("./src/data/agent-mechanism-notes/agent-05.js");const parseArray=s=>JSON.parse(s.slice(s.indexOf("["),s.lastIndexOf("]")+1));const board=parseArray(n.sections.find(s=>s.id==="supplier-research-strategy-board").paragraphs[1]);assert.deepEqual(board.map(x=>x.strategy),["reactive","plan-and-execute","hybrid"]);for(const x of board){const steps=Object.keys(x.actions).sort();for(const k of ["artifacts","dependencies","checkpoints","budgets"])assert.deepEqual(Object.keys(x[k]).sort(),steps,`${x.strategy}/${k}`);for(const step of steps){assert.ok(x.artifacts[step].length);assert.ok(x.dependencies[step].every(dep=>steps.includes(dep)));assert.ok(x.checkpoints[step]);assert.ok(Number.isInteger(x.budgets[step].calls)&&Number.isInteger(x.budgets[step].minutes));}}const recovery=parseArray(n.sections.find(s=>s.id==="failure-injections-and-lab-boundary").paragraphs[1]);assert.deepEqual(recovery.map(x=>x.injection),["empty-result","timeout","new-constraint"]);const timeout=recovery.find(x=>x.injection==="timeout");assert.match(timeout.reason,/终态可能未知/);assert.match(timeout.recovery,/reconcile-by-stable-intent/);assert.match(timeout.recovery,/确认未执行/);assert.match(timeout.exit,/handoff/);const lab=n.sections.find(s=>s.id==="failure-injections-and-lab-boundary").paragraphs.join(" ");for(const token of ["宿主生成并持久化","唯一 client request identifier/idempotency key","只读查询","不调用真实模型","不是模型、论文方法或生产可靠性的评测"])assert.ok(lab.includes(token),token);const decomposition=n.sections.find(s=>s.id==="decompose-into-verifiable-work").paragraphs.join(" ");for(const token of ["工具能力","上下文预算","错误隔离","按失败轨迹迭代粒度","应继续拆","应合并"])assert.ok(decomposition.includes(token),token);const replan=n.sections.find(s=>s.id==="observation-driven-replanning").paragraphs.join(" ");for(const token of ["权限变化","artifact 失效","局部修订","整体重规划","blocked","handoff","整组规则都是课程工程综合","不归因于 ReAct 或 ReWOO"])assert.ok(replan.includes(token),token);console.log(JSON.stringify({strategies:board.map(x=>({strategy:x.strategy,stepIds:Object.keys(x.actions),traceable:true})),injections:recovery.map(x=>x.injection),timeoutUnknownSafe:true,interviewCoverage:true,labBoundary:true}))'
```

本轮输出：`{"strategies":[{"strategy":"reactive","stepIds":["r1","r2"],"traceable":true},{"strategy":"plan-and-execute","stepIds":["p1","p2"],"traceable":true},{"strategy":"hybrid","stepIds":["h1","h2"],"traceable":true}],"injections":["empty-result","timeout","new-constraint"],"timeoutUnknownSafe":true,"interviewCoverage":true,"labBoundary":true}`。

### `agent-05` 可复制版本化步骤与迟到完成 probe

以下命令解析正文运行约束，并建立当前六项 expected tuple。它分别注入 old plan、同 plan 的 old stepVersion、同 step 的 old attempt、wrong stableIntent 和 stale fencing，五种完成均只能 observation-only；随后证明只有六项都匹配的当前 tuple 能发布 artifact、满足 checkpoint 与解锁依赖。它还调用真实 `decidePlanRecovery`，确认副作用在 unknown/cannot-reconcile 下不会第二次 act：

```bash
node --input-type=module -e 'import assert from "node:assert/strict";const [{agent05Note:n},{decidePlanRecovery}]=await Promise.all([import("./src/data/agent-mechanism-notes/agent-05.js"),import("./src/core/agent-mechanism.js")]);const parseObject=s=>JSON.parse(s.slice(s.indexOf("{"),s.lastIndexOf("}")+1));const runtime=parseObject(n.sections.find(s=>s.id==="plans-as-executable-dataflow").paragraphs[3]);assert.deepEqual(runtime.states,["pending","ready","running","passed","failed","unknown","invalidated"]);assert.deepEqual(runtime.claimKey,["stepId","planVersion","stepVersion","attempt","stableIntent","fencingToken"]);assert.equal(runtime.dispatch,"atomic-claim-expected-tuple");assert.equal(runtime.completion,"cas-full-claim-tuple-with-valid-fencing");const current={stepId:"s1",planVersion:2,stepVersion:3,attempt:2,stableIntent:"intent-1",fencingToken:12};const observations=[];const complete=claim=>{const matches=["stepId","planVersion","stepVersion","attempt","stableIntent","fencingToken"].every(k=>claim[k]===current[k]);observations.push({claim,observationOnly:!matches});return matches?{publishedArtifact:true,checkpoint:true,dependencyUnlocked:true,observationOnly:false}:{publishedArtifact:false,checkpoint:false,dependencyUnlocked:false,observationOnly:true}};const base={...current};const cases={oldPlan:{...base,planVersion:1},oldStepVersion:{...base,stepVersion:2},oldAttempt:{...base,attempt:1},wrongStableIntent:{...base,stableIntent:"intent-2"},staleFencing:{...base,fencingToken:11}};for(const claim of Object.values(cases))assert.deepEqual(complete(claim),{publishedArtifact:false,checkpoint:false,dependencyUnlocked:false,observationOnly:true});assert.deepEqual(complete(base),{publishedArtifact:true,checkpoint:true,dependencyUnlocked:true,observationOnly:false});let actCount=1;for(const reconciliationStatus of ["unknown","cannot-reconcile"]){const decision=decidePlanRecovery({strategy:"hybrid",observation:"timeout",retriesUsed:0,maxRetries:2,operationKind:"side-effect",reconciliationStatus});if(decision.action==="retry")actCount+=1;}assert.equal(actCount,1);console.log(JSON.stringify({claimKey:runtime.claimKey,staleCases:Object.keys(cases),staleResult:{publishedArtifact:false,checkpoint:false,dependencyUnlocked:false,observationOnly:true},currentResult:{publishedArtifact:true,checkpoint:true,dependencyUnlocked:true,observationOnly:false},sideEffectActCount:actCount}))'
```

本轮输出：`{"claimKey":["stepId","planVersion","stepVersion","attempt","stableIntent","fencingToken"],"staleCases":["oldPlan","oldStepVersion","oldAttempt","wrongStableIntent","staleFencing"],"staleResult":{"publishedArtifact":false,"checkpoint":false,"dependencyUnlocked":false,"observationOnly":true},"currentResult":{"publishedArtifact":true,"checkpoint":true,"dependencyUnlocked":true,"observationOnly":false},"sideEffectActCount":1}`。

当前没有把 metadata-only 材料当作机制 evidence，也没有把 extension 升级为 core。最终测试状态、coverage、评分和 `publicationReady` 必须在八篇笔记接线、完整自动化与浏览器验收完成后重写，不能沿用本阶段的中间结果。

### `agent-01` 单课质量与可追溯性

- `courseFieldBasis`：`lesson.objectives[0..1]`、`lesson.concepts[0..4]`、`lesson.explanations[0..1]`、`lesson.quiz[0..1]`、`iq-agent-01-1..3` 的 `shortAnswer`、全部 `deepDive` 与 `followUps`、`lesson.exercise.steps[0..1]`、`lesson.exercise.deliverable`、`lesson.completionCriteria[0..1]` 均已进入上表所列实质章节；这些路径只定义覆盖，不被伪装成来源 ID。
- `sections → evidence → 产出`：第 1/5 节用 Anthropic 与 OpenAI 厂商正文支撑控制权和适用门槛，并明确其工程经验不构成跨任务定律；第 2/3/4 节以 OpenAI、Hugging Face、Microsoft、Hello-Agents 与李宏毅字幕正文交叉说明闭环、环境和退出，始终区分模型决策与宿主执行；第 6 节把相同标准应用到三案例选型表。两条 Bilibili metadata-only 资源没有出现在任何 `sourceIds` 中。
- `质量自评`：覆盖 25/25（两个 objectives、五 concepts、两题 quiz 推理、三题面试全字段、exercise 两步与 deliverable、两个完成标准均可追踪）；结构 20/20（六节按依赖递进并连接 agent-02）；来源 24/25（ID 全部属于本课 registry 与 evidence set，厂商/字幕/课程边界已显式保留）；教学 16/20（固定三案例完整走查，但尚待集成后的真实页面可读性验收）；数据契约 9/10（纯数据、稳定 ID、无 HTML，最终递归冻结由聚合入口与集成测试共同验收）。总分 `94/100`，`brokenReferenceCount: 0`，coverage gap：无；状态 `ready-for-integration`，全模块仍为 `publicationReady: false`。

### `agent-02` 单课质量与可追溯性

- `courseFieldBasis`：`lesson.objectives[0..1]`、`lesson.concepts[0..4]`、`lesson.explanations[0..1]`、`lesson.quiz[0..1]`、`iq-agent-02-1..3` 的 `shortAnswer`、全部 `deepDive`、`misconceptions` 与 `followUps`、`lesson.exercise.steps[0..1]`、`lesson.exercise.deliverable`、`lesson.completionCriteria[0..1]` 均映射到上表所列六节；这些字段只规定覆盖与验收，不进入 `sourceIds` 或 evidence map。
- `sections → evidence → 产出`：第 1/2/5/6 节只用 OpenAI 支撑清晰步骤、缺失信息分支、run 退出、工具风险与人工介入，用 Anthropic 支撑环境 ground truth、阻塞检查点和停止；第 3/4/6 节只用 Hello-Agents 交叉核验旅行目标分解、工具补缺和反馈修正，用 CoALA 解释跨调用工作记忆、环境反馈与决策循环。正文明确说明四份来源都没有定义完整 task contract；最终上海差旅逐项处理七个未知项，其中敏感资料只可查授权系统或澄清、执行前仍缺失即 blocked、禁止临时假设；全部字段、四路由、三类记录和三层验收是课程综合出的可审查交付物。
- `完成语义`：正文明确区分 state、transcript 与 event log，区分事实、假设与 observation，关键状态带来源、时间或版本；完成检查依次区分“动作已提交、动作成功、业务目标达成”，异步 queued/pending/processing 均保持 waiting 或 in-progress，并依赖工具终态、产物校验或测试证据，而不采信模型自述。
- `质量自评`：覆盖 25/25（两个 objectives、五 concepts、两题 quiz 推理、三题面试 short/deepDive/misconception/follow-up、exercise 全步骤与 deliverable、两个完成标准均可追踪）；结构 20/20（六节严格按任务定义到案例交付递进并承接 agent-01、连接 agent-03）；来源 22/25（四份正文重新实际访问且 ID 三重解析，精确课程结构均标注为综合推导；扣分项是没有单一外部规范定义完整检查表）；教学 18/20（上海案例给出七个未知项的逐项策略与三层完成证据，仍待集成后的真实页面验收）；数据契约 9/10（纯数据、稳定 kebab-case ID、无 HTML、全部嵌套值已冻结，最终对象身份由聚合测试验收）。总分 `94/100`，`brokenReferenceCount: 0`；coverage gap：没有阻塞性缺口，限制为“完整 schema 仅作为 course synthesis 而非外部标准”；状态 `ready-for-integration`，全模块仍为 `publicationReady: false`。

### `agent-03` 单课质量与可追溯性

- `courseFieldBasis`：`lesson.objectives[0..1]`、`lesson.concepts[0..4]`、`lesson.explanations[0..1]`、`lesson.quiz[0..1]`、`iq-agent-03-1..3` 的 `shortAnswer`、全部 `deepDive`、`misconceptions` 与 `followUps`、`lesson.exercise.steps[0..1]`、`lesson.exercise.deliverable`、`lesson.completionCriteria[0..1]` 均映射到矩阵所列七节；这些字段只规定教学覆盖与验收，不进入 `sourceIds` 或 evidence map。
- `sections → evidence → 产出`：第 1/2/3/6 节用 OpenAI 当前官方正文支撑工具定义、五步生命周期、strict/nullable-required 边界和 call_id 回填；第 1/3/4/5/6/7 节用 Anthropic 与 Microsoft 支撑工具命名、最小权限、错误和返回设计，并保留厂商与字幕版本限制；AWS 直接支撑作为 API caller 的宿主使用 client request identifier、幂等重试、网络超时不确定性、reconciliation、语义等价响应与晚到请求；Toolformer 只用于解释训练目标及其无工具链、无交互修订、无权限治理的边界；HF 只交叉核验 Think–Act–Observe 路线。最终订单案例给出两份完整 Responses JSON 工具定义，其中 cancel_order 模型输入只有 order_id/reason/reason_detail；宿主 envelope 注入 actor/tenant/intent/idempotency/approval。五类完整结果统一包含 recoveryAction，订单查询、审批和补偿仍标为 course synthesis。
- `接口与实验边界`：正文把 OpenAI strict、Responses 与 call_id 等语义限定为 `2026-07-22` 核验；明确合法 JSON 之后仍需 schema、存在性、跨字段、权限、风险、幂等与审批；明确模型不能生成可信幂等键或审批凭证。统一结果为 success/data/error/recoveryAction/evidenceRef，失败 error 含 code/retryable/message；retryable 仅表示原参数原意图可安全且有意义地自动重放，validation、审批和副作用 UNKNOWN_OUTCOME 分别走 repair_input、request_approval、reconcile。页面 `tool-contract` 仍是确定性教学检查器，不是真实模型调用、生产安全验证或能力评测。
- `质量自评`：覆盖 25/25（两个 objectives、五 concepts、两题 quiz 推理、三题面试 short/deepDive/misconception/follow-up、exercise 两步与 deliverable、两个完成标准均可追踪）；结构 20/20（恰七节从 agent-02 任务契约推进至动作契约并连接 agent-04 loop）；来源 25/25（六份正文真实核验且全部 ID 三重解析，幂等/超时获得 AWS 直接证据，Toolformer 和时敏接口边界清晰）；教学 19/20（两份完整 schema、可信宿主 envelope、稳定恢复枚举与五类完整 JSON 可直接完成练习，仍待集成页面验收）；数据契约 9/10（正文 7715 字符且不超过 9000，纯数据、稳定 kebab-case ID、无 HTML、所有嵌套值冻结，最终对象身份由聚合测试验收）。总分 `98/100`，`brokenReferenceCount: 0`；coverage gap：没有阻塞性缺口，真实业务字段、审批阈值、订单对账/补偿和错误码需按具体系统再设计；状态 `ready-for-integration`，全模块仍为 `publicationReady: false`。

### `agent-04` 单课质量与可追溯性

- `courseFieldBasis`：`lesson.objectives[0..1]`、`lesson.concepts[0..4]`、`lesson.explanations[0..1]`、`lesson.quiz[0..1]`、`iq-agent-04-1..3` 的 `shortAnswer`、全部 `deepDive`、`misconceptions` 与 `followUps`、`lesson.exercise.steps[0..1]`、`lesson.exercise.deliverable`、`lesson.completionCriteria[0..1]` 均映射到矩阵所列七节；课程字段只规定覆盖和交付，不被转换为 source ID。
- `sections → evidence → 产出`：ReAct 原论文只支撑其设定中的 reasoning/action/observation 交错；OpenAI/Anthropic 正文支撑 run、环境 ground truth、最大迭代、失败阈值与人工介入；AgentBench 只支撑其评测中的 invalid action、TLE、重复和终止失败。课程另行综合确定 failed-first、全候选原子预算计费、decision/action 双指纹、unknown 直接 handoff、writer lease、state CAS、唯一 claim 与非幂等串行对账；没有把这些并发控制结构伪装成论文或厂商标准。Berkeley 和 metadata-only Bilibili 均未作机制引用。
- `ReAct 与实验边界`：正文明确普通 CoT 未必执行外部动作，ReAct 原论文在具体设定中让 reasoning traces、task-specific actions 与 observations 交错；生产系统不获取或依赖隐藏思维链，只记录可观察 decision summary、action、validation、observation、state diff、预算和 terminal decision，且明确这是课程工程综合。页面 `agent-loop` 只运行本地 goalSatisfied/blocked/步数预算优先级，不调用真实模型、工具或 ReAct 环境。
- `质量自评`：覆盖 25/25；结构 20/20；来源 25/25；教学 18/20（伪代码覆盖可信工具策略、receipt、预算、指纹、unknown 与单写者协议，并有反例状态机，仍待真实持久化与页面集成）；数据契约 9/10（正文 8985 字符且不超过 9000、纯数据、稳定 ID、无 HTML、深层冻结）。总分 `97/100`，`brokenReferenceCount: 0`；coverage gap：真实系统仍需选择 registry 治理、事务隔离、lease fencing/续期、预算结算与 handoff 策略；状态 `ready-for-integration`。

### `agent-05` 单课质量与可追溯性

- `courseFieldBasis`：`lesson.objectives[0..1]`、`lesson.concepts[0..4]`、`lesson.explanations[0..1]`、`lesson.quiz[0..1]`、`iq-agent-05-1..3` 的 `shortAnswer`、全部 `deepDive`、`misconceptions` 与 `followUps`、`lesson.exercise.steps[0..1]`、`lesson.exercise.deliverable`、`lesson.completionCriteria[0..1]` 均映射到矩阵所列七节；课程字段只规定覆盖和交付，不被转换为 source ID。
- `sections → evidence → 产出`：第 1 节比较 reactive、plan-and-execute 与混合策略；第 2 节把模糊动作转为输入/动作/artifact/dependency/checkpoint/failure exit/budget，明确工具能力、上下文预算、错误隔离与失败轨迹决定何时拆分或合并；第 3 节分别保留 Plan-and-Solve 静态推理与 ReWOO 固定蓝图的边界，并把版本化步骤落实为七状态、原子 claim、CAS/fencing 和迟到完成隔离；第 4 节只在 ToT 三个特制任务范围内解释候选、评价、BFS/DFS 与搜索成本；第 5 节将 observation 差异映射为局部修订或整体重规划，并明确整组规则均属 course synthesis；第 6 节三份策略卡的五类字段全部按 step ID 对齐；第 7 节给出三类恢复记录与 stable intent 定义，并复用 AWS 只支撑 timeout unknown outcome 的安全对账。
- `论文与实验边界`：没有把论文收益外推为“规划必然更好”；ReAct、Plan-and-Solve、ReWOO、ToT 均保留任务、模型、工具和评测限制，Microsoft 字幕保留旧版本限制，metadata-only Bilibili 未进入任何章节。权限传播、重规划阈值、最小状态变化、冷却、最大次数、step 生命周期与并发协议全部明标为 course synthesis，不归因 ReAct/ReWOO。`plan-recovery` 固定为本地只读供应商查询；UI 的 retry 只适用于无副作用读操作，不测量方法能力或生产可靠性。
- `质量自评`：覆盖 25/25（两个 objectives、五 concepts、两题 quiz 推理、三题面试全字段、exercise 两步、deliverable 与两个完成标准均可追踪）；结构 20/20（七节由策略选择、分解、版本化数据流与搜索推进到动态修订和同一案例交付，并连接 agent-06）；来源 24/25（六个实际章节来源全部三重解析，AWS 复用边界清楚；具体计划 schema、版本协议与重规划分类没有单一行业规范，故明确作为课程综合）；教学 19/20（三策略 step ID、五类 stale tuple、六项全匹配的有效 tuple、恢复 JSON 和副作用反例均有可执行 probe，仍待页面集成验收）；数据契约 9/10（正文 8330 字符且不超过 9000，纯数据、稳定 ID、无 HTML、深层冻结）。总分 `97/100`，`brokenReferenceCount: 0`；coverage gap：无阻塞缺口，真实系统仍需实现持久化 claim、真实 CAS/fencing，并校准预算、评价器、对账与业务完成谓词；状态 `ready-for-integration`，全模块仍为 `publicationReady: false`。

### `agent-06` 单课质量与可追溯性

- `courseFieldBasis`：`lesson.objectives[0..1]`、`lesson.concepts[0..4]`、`lesson.explanations[0..1]`、`lesson.quiz[0..1]`、`iq-agent-06-1..3` 的 `shortAnswer`、全部 `deepDive`、`misconceptions` 与 `followUps`、`lesson.exercise.steps[0..1]`、`lesson.exercise.deliverable`、`lesson.completionCriteria[0..1]` 均映射到矩阵所列七节；课程字段只规定覆盖与交付，不作为资源证据。
- `sections → evidence → 产出`：第 1 节以 AgentBench/τ-bench 支撑可观察轨迹与数据库/政策终态，六类路由和 empty-result 子类明确为课程综合；第 2 节只用 AWS 支撑 caller intent、超时不确定性、幂等与对账；第 3 节比较四项反思研究；第 4 节将 CRITIC/τ-bench 限定到工具 critique 与终态验证，并给出 PASS/FAIL/VALIDATION_UNKNOWN；第 5 节只用 AgentBench/AWS 支撑问题背景，actionKey/outcomeKey 与全局预算仍为综合；第 6/7 节交付 JSON 与可执行 probe。source ID 全部三重解析。
- `恢复语义`：确定性 classifier 实际区分 empty、nonempty-irrelevant 与 provenance-valid matched；validator 先做 provenance/version gate，stale PASS/FAIL 都变为 UNKNOWN 且零提交；写 timeout 按 UNKNOWN_OUTCOME → reconcile → confirmed-not-executed 才重放。actionKey 相同且无真实进展就持续累计，resultClass 只进入 outcomeKey 诊断且不重置预算；真实版本变化只清局部计数。
- `质量自评`：覆盖 25/25；结构 20/20；来源 25/25；教学 19/20（六行 JSON、验证三态和双键/硬预算反例 probe 可复现，仍待页面验收）；数据契约 9/10（正文 8806 字符、39 分钟、纯数据、稳定 ID、无 HTML、深层冻结）。总分 `98/100`，`brokenReferenceCount: 0`；coverage gap：无阻塞缺口，真实系统仍需实现事件/intent 原子持久化、对账、鉴权、validator 治理与人工队列；状态 `ready-for-integration`，模块仍为 `publicationReady: false`。

### `agent-06` 可复制 contract 与语义 probe

以下命令实际导入笔记与课程 registry，检查 4800–9000 字正文、七节结构、source 三重解析、误区、recap 与全部嵌套值冻结：

```bash
node --input-type=module -e 'import assert from "node:assert/strict";const [{agent06Note:n},{agentMechanism:c}]=await Promise.all([import("./src/data/agent-mechanism-notes/agent-06.js"),import("./src/data/agent-mechanism.js")]);const lesson=c.lessons.find(x=>x.id==="agent-06");const reg=new Map(c.resources.map(x=>[x.id,x]));const body=[n.introduction,...n.sections.flatMap(s=>s.paragraphs),n.nextStep].reduce((a,s)=>a+s.trim().length,0);const ids=n.sections.map(s=>s.id);const deep=v=>{if(v&&typeof v==="object"){assert.equal(Object.isFrozen(v),true);Object.values(v).forEach(deep)}};assert.ok(n.readingMinutes>=35&&n.readingMinutes<=40);assert.equal(n.sections.length,7);assert.equal(new Set(ids).size,ids.length);assert.ok(body>=4800&&body<=9000);assert.ok(n.sections.every(s=>/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(s.id)&&s.paragraphs.length>=2&&s.paragraphs.length<=4&&s.paragraphs.every(p=>p.trim().length>=60)&&s.keyPoints.length>=2&&s.sourceIds.length>=1&&new Set(s.sourceIds).size===s.sourceIds.length&&s.sourceIds.every(id=>lesson.resourceIds.includes(id)&&reg.has(id)&&reg.get(id).evidence)));assert.ok(n.misconceptions.length>=4&&n.misconceptions.length<=6);assert.ok(n.recap.length>=5);deep(n);console.log(JSON.stringify({readingMinutes:n.readingMinutes,body,sections:n.sections.length,minParagraph:Math.min(...n.sections.flatMap(s=>s.paragraphs.map(p=>p.trim().length))),sources:[...new Set(n.sections.flatMap(s=>s.sourceIds))],misconceptions:n.misconceptions.length,recap:n.recap.length,deepFrozen:true,brokenReferenceCount:0}))'
```

本轮真实输出：`{"readingMinutes":39,"body":8806,"sections":7,"minParagraph":126,"sources":["res-agent-agentbench","res-agent-tau-bench","res-agent-aws-idempotent-apis","res-agent-reflexion","res-agent-self-refine","res-agent-no-self-correct","res-agent-critic"],"misconceptions":6,"recap":11,"deepFrozen":true,"brokenReferenceCount":0}`。

semantic-mismatch 使用最小确定性分类器实际运行三个案例，而不是把预期字符串直接当作布尔条件：

```bash
node --input-type=module -e 'import assert from "node:assert/strict";const classify=({items,goalPredicate,completionPredicate,provenanceValid})=>{if(items.length===0)return {class:"semantic-mismatch",subtype:"empty-result"};const relevant=items.some(item=>goalPredicate(item)&&completionPredicate(item));if(!relevant)return {class:"semantic-mismatch",subtype:"nonempty-irrelevant"};if(!provenanceValid)return {class:"semantic-mismatch",subtype:"invalid-provenance"};return {class:"matched",subtype:null}};const goal=item=>item.ticketId===42,complete=item=>item.status==="closed";const empty=classify({items:[],goalPredicate:goal,completionPredicate:complete,provenanceValid:true});const irrelevant=classify({items:[{ticketId:7,status:"open"}],goalPredicate:goal,completionPredicate:complete,provenanceValid:true});const matched=classify({items:[{ticketId:42,status:"closed"}],goalPredicate:goal,completionPredicate:complete,provenanceValid:true});assert.deepEqual(empty,{class:"semantic-mismatch",subtype:"empty-result"});assert.deepEqual(irrelevant,{class:"semantic-mismatch",subtype:"nonempty-irrelevant"});assert.deepEqual(matched,{class:"matched",subtype:null});console.log(JSON.stringify({empty,nonemptyIrrelevant:irrelevant,matched}))'
```

本轮真实输出：`{"empty":{"class":"semantic-mismatch","subtype":"empty-result"},"nonemptyIrrelevant":{"class":"semantic-mismatch","subtype":"nonempty-irrelevant"},"matched":{"class":"matched","subtype":null}}`。

以下命令解析验证状态机、控制约束和六类决策表；它验证 semantic-mismatch 的空/非空子类、UNKNOWN 零提交、结果类别交替仍触发上限，以及真实版本变化只清局部计数而不清全局硬预算：

```bash
node --input-type=module -e 'import assert from "node:assert/strict";const {agent06Note:n}=await import("./src/data/agent-mechanism-notes/agent-06.js");const obj=s=>JSON.parse(s.slice(s.indexOf("{"),s.lastIndexOf("}")+1));const arr=s=>JSON.parse(s.slice(s.indexOf("["),s.lastIndexOf("]")+1));const validation=obj(n.sections.find(s=>s.id==="external-validation-stack").paragraphs[1]);const control=obj(n.sections.find(s=>s.id==="fingerprint-and-progress-detection").paragraphs[1]);const table=arr(n.sections.find(s=>s.id==="ticket-recovery-decision-table").paragraphs[1]);assert.deepEqual(validation.states,["PASS","FAIL","VALIDATION_UNKNOWN"]);assert.deepEqual(validation.passGate,["approved-provenance","validator-and-resource-version-match"]);assert.match(validation.unknownAction,/no-publish-no-side-effect/);assert.match(validation.unknownAction,/same-permission-and-data-minimization/);assert.deepEqual(control.actionKey,["tool","canonicalParams","stableIntent","relevantStateVersion"]);assert.deepEqual(control.outcomeKey,["actionKey","resultClass"]);assert.equal(control.resultClassChangeResetsNoProgress,false);assert.equal(control.hardBudgetNeverResets,true);assert.deepEqual(table.map(x=>x.class),["transport","parameter","semantic-mismatch","business-conflict","permission","capability"]);for(const row of table)for(const k of ["observableEvidence","sideEffectRisk","allowedRecovery","budget","idempotency","terminalExit"])assert.ok(row[k]!==undefined,row.class+"/"+k);const semantic=table[2];for(const e of ["schema-valid","expected-vs-actual-mismatch","external-validator-mismatch","missing-or-conflicting-provenance","subtype:empty-result-or-nonempty-irrelevant"])assert.ok(semantic.observableEvidence.includes(e),e);for(const a of ["bounded-revise-query","change-approved-source","replan","external-validate"])assert.ok(semantic.allowedRecovery.includes(a),a);assert.equal(table[4].budget.automaticRetries,0);for(const x of ["clarify-goal","offer-approved-degraded-output","handoff"])assert.ok(table[5].allowedRecovery.includes(x));const canonical=x=>JSON.stringify(x,Object.keys(x).sort());const actionKey=version=>["update_ticket",canonical({ticketId:42,status:"closed"}),"ticket-42-close-v1",version].join("|");const global={attempts:0,time:0,cost:0};const noProgress=new Map();const outcomes=[];for(const resultClass of ["timeout","conflict","timeout"]){const key=actionKey("row=7;permission=v1");global.attempts+=1;global.time+=10;global.cost+=1;noProgress.set(key,(noProgress.get(key)||0)+1);outcomes.push(key+"|"+resultClass)}const keyV7=actionKey("row=7;permission=v1");assert.deepEqual({attempts:global.attempts,noProgress:noProgress.get(keyV7),terminal:global.attempts>=3?"blocked":null},{attempts:3,noProgress:3,terminal:"blocked"});assert.equal(new Set(outcomes).size,2);const keyV8=actionKey("row=8;permission=v1");assert.notEqual(keyV7,keyV8);noProgress.set(keyV8,0);assert.equal(noProgress.get(keyV8),0);assert.deepEqual(global,{attempts:3,time:30,cost:3});const unknown={commitCount:0,downstreamSideEffectCount:0};const apply=v=>v==="VALIDATION_UNKNOWN"?"blocked-or-handoff":"unexpected";assert.equal(apply("VALIDATION_UNKNOWN"),"blocked-or-handoff");assert.deepEqual(unknown,{commitCount:0,downstreamSideEffectCount:0});console.log(JSON.stringify({classes:table.map(x=>x.class),semanticSubtype:true,validatorStates:validation.states,validationUnknown:{...unknown,exit:"blocked-or-handoff"},alternatingOutcomes:{actionKeys:1,outcomeKeys:new Set(outcomes).size,globalRecoveryAttempts:global.attempts,noProgressOnActionKey:noProgress.get(keyV7),terminal:"blocked"},realVersionChangesActionKeyAndResetsLocal:true,hardBudgetAfterVersionChange:global}))'
```

本轮真实输出：`{"classes":["transport","parameter","semantic-mismatch","business-conflict","permission","capability"],"semanticSubtype":true,"validatorStates":["PASS","FAIL","VALIDATION_UNKNOWN"],"validationUnknown":{"commitCount":0,"downstreamSideEffectCount":0,"exit":"blocked-or-handoff"},"alternatingOutcomes":{"actionKeys":1,"outcomeKeys":2,"globalRecoveryAttempts":3,"noProgressOnActionKey":3,"terminal":"blocked"},"realVersionChangesActionKeyAndResetsLocal":true,"hardBudgetAfterVersionChange":{"attempts":3,"time":30,"cost":3}}`。

验证器转移函数先执行 provenance/version gate，再解释 PASS/FAIL；因此 stale PASS 与 stale FAIL 都归一化为 VALIDATION_UNKNOWN 且零提交：

```bash
node --input-type=module -e 'import assert from "node:assert/strict";const apply=({state,approvedProvenance=true,versionsMatch=true})=>{const runtime={commitCount:0,downstreamSideEffectCount:0};if(!approvedProvenance||!versionsMatch)return {normalizedState:"VALIDATION_UNKNOWN",decision:"blocked-or-handoff",runtime};if(state==="PASS"){runtime.commitCount+=1;runtime.downstreamSideEffectCount+=1;return {normalizedState:"PASS",decision:"publish",runtime}}if(state==="FAIL")return {normalizedState:"FAIL",decision:"structured-feedback-and-bounded-revision",runtime};return {normalizedState:"VALIDATION_UNKNOWN",decision:"blocked-or-handoff",runtime}};const pass=apply({state:"PASS"}),fail=apply({state:"FAIL"}),unknown=apply({state:"VALIDATION_UNKNOWN"}),stalePass=apply({state:"PASS",versionsMatch:false}),staleFail=apply({state:"FAIL",versionsMatch:false});assert.equal(pass.decision,"publish");assert.equal(fail.decision,"structured-feedback-and-bounded-revision");assert.deepEqual(unknown.runtime,{commitCount:0,downstreamSideEffectCount:0});assert.deepEqual(stalePass,unknown);assert.deepEqual(staleFail,unknown);console.log(JSON.stringify({pass:pass.decision,fail:fail.decision,unknown:{...unknown.runtime,exit:unknown.decision},stalePass:stalePass.normalizedState,staleFail:staleFail.normalizedState}))'
```

本轮真实输出：`{"pass":"publish","fail":"structured-feedback-and-bounded-revision","unknown":{"commitCount":0,"downstreamSideEffectCount":0,"exit":"blocked-or-handoff"},"stalePass":"VALIDATION_UNKNOWN","staleFail":"VALIDATION_UNKNOWN"}`。

### `agent-07` 单课质量与可追溯性

- `courseFieldBasis`：`lesson.objectives[0..1]`、`lesson.concepts[0..4]`、`lesson.explanations[0..1]`、`lesson.quiz[0..1]`、`iq-agent-07-1..3` 的 `shortAnswer`、全部 `deepDive`、`misconceptions` 与 `followUps`、`lesson.exercise.steps[0..1]`、`lesson.exercise.deliverable`、`lesson.completionCriteria[0..1]` 均映射到矩阵所列六节；这些字段只规定覆盖和交付，不进入 source ID 或 evidence map。
- `sections → evidence → 产出`：第 1/2/3/4 节只用 CoALA 支撑跨调用工作记忆与环境反馈的一般机制，以 Weng/Hello-Agents 交叉核验组件地图和消息/状态实践；第 5 节将同一边界落到 20 轮供应商/工单案例；第 6 节只在“后续学习导航”范围使用 Berkeley syllabus。正文不声称四个来源定义 transcript/event/state 五层职责、认识分类、生产 schema、摘要或裁剪算法；这些结构、required invariants 和 probe 全部明标 `course synthesis`。
- `状态与压缩语义`：v2 合同在 evt-16 前明确含一次 `ticket.update`，宿主持久化 stable intent 并发出请求后得到 UNKNOWN_OUTCOME；evt-17 的 v3 以 user provenance 撤销写权限并增加只读/禁止联系约束，current permissions 全为只读。旧 pending 仍绑定 authorizedByContractVersion=2，但只能走 request-status/rowVersion reconcile，`actAllowed:false`。State 与 context 共享 16 个真实 path，包含 contested belief、qualified observation、validator provenance 与 artifact pointers；pointer 解析只生成 observation。
- `语义验收`：7 组事件与 6 组可信 prefix snapshot 保持原结论。13 组 gate 新增 `reconcileBeforeAct=false`，即使其余权限字段自洽也返回 `REJECT_PERMISSION_OR_PENDING_SEMANTICS` 且零 act；event range coverage 仍逐项展开。两组 fixture packer 外，probe 还从 20 轮 compressed output 解析真实 manifest，忽略其自报理由后按 window/fixed/output/safety reserve 与 blocks 重算，结果 `deepEqual`。9 组 pointer fixture 外，actual output 的 event-range/artifact 两指针也经同一 resolver 绑定 type↔scheme、URI authority、声明 metadata、accessBoundary、caller ACL 与可信 store ACL/content hash，均只产生 observation。
- `质量自评`：覆盖 25/25；结构 20/20；来源 24/25（四个 ID 均三重解析，生产 schema 只作课程综合）；教学 19/20（合法版本历史、16 path、37 组 fixture、observation provenance/state pointer 嵌套断言与 actual output probe 可复现，仍待页面验收）；数据契约 9/10（正文 8930 字符、34 分钟、纯数据、稳定 ID、无 HTML、深层冻结）。总分 `97/100`，`brokenReferenceCount: 0`；coverage gap：无阻塞缺口，生产系统仍需实现真实事件存储、事务/MAC、隐私保留、token 估算、snapshot 和 validator 治理；状态 `ready-for-integration`，模块仍为 `publicationReady: false`。

### `agent-07` 可复制 contract/source/freeze probe

以下命令实际导入独立笔记与课程 registry，检查 4200–9000 字正文、六节结构、source 三重解析、误区、recap 与全部嵌套值冻结：

```bash
node --input-type=module -e 'import assert from"node:assert/strict";const[{agent07Note:n},{agentMechanism:c}]=await Promise.all([import("./src/data/agent-mechanism-notes/agent-07.js"),import("./src/data/agent-mechanism.js")]),lesson=c.lessons.find(x=>x.id==="agent-07"),reg=new Map(c.resources.map(x=>[x.id,x])),body=[n.introduction,...n.sections.flatMap(s=>s.paragraphs),n.nextStep].reduce((a,s)=>a+s.trim().length,0),ids=n.sections.map(s=>s.id),sec=id=>n.sections.find(s=>s.id===id),obj=t=>JSON.parse(t.slice(t.indexOf("{"),t.lastIndexOf("}")+1)),state=obj(sec("event-replay-and-derived-state").paragraphs[1]),ctx=obj(sec("twenty-turn-compression-walkthrough").paragraphs[1]),z=JSON.parse(sec("semantic-probes-and-module-boundary").callout.body),obsOk=x=>Array.isArray(x.knowledge.qualifiedObservations)&&x.knowledge.qualifiedObservations.length>0&&x.knowledge.qualifiedObservations.every(o=>typeof o.id==="string"&&typeof o.sourceRef==="string"&&o.sourceRef.length>0&&typeof o.sourceVersion==="string"&&o.sourceVersion.length>0&&typeof o.integrityStatus==="string"),deep=v=>{if(v&&typeof v==="object"){assert.equal(Object.isFrozen(v),true);Object.values(v).forEach(deep)}};assert.ok(n.readingMinutes>=30&&n.readingMinutes<=35);assert.equal(n.sections.length,6);assert.equal(new Set(ids).size,ids.length);assert.ok(body>=4200&&body<=9000);assert.ok(n.sections.every(s=>/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(s.id)&&s.paragraphs.length>=2&&s.paragraphs.length<=4&&s.paragraphs.every(p=>p.trim().length>=60)&&s.keyPoints.length>=2&&s.sourceIds.length>=1&&new Set(s.sourceIds).size===s.sourceIds.length&&s.sourceIds.every(id=>lesson.resourceIds.includes(id)&&reg.has(id)&&reg.get(id).evidence)));assert.ok(n.misconceptions.length>=4&&n.misconceptions.length<=6);assert.ok(n.recap.length>=5);for(const x of[state,ctx])assert.equal(obsOk(x),true);const p=state.artifactPointers[0],b=p.accessBoundary,caller=z.pointerPolicy.caller;assert.ok(Number.isInteger(p.version)&&p.version>0);assert.equal(p.runId,state.runId);assert.equal(p.tenantId,caller.tenantId);assert.deepEqual({scope:b.scope,tenantId:b.tenantId,runId:b.runId,permission:b.permission},{scope:"run",tenantId:p.tenantId,runId:p.runId,permission:z.pointerPolicy.typePermissions[p.type]});assert.ok(caller.acl.includes(b.permission));deep(n);console.log(JSON.stringify({readingMinutes:n.readingMinutes,body,sections:n.sections.length,minParagraph:Math.min(...n.sections.flatMap(s=>s.paragraphs.map(p=>p.trim().length))),sources:[...new Set(n.sections.flatMap(s=>s.sourceIds))],misconceptions:n.misconceptions.length,recap:n.recap.length,observationProvenance:true,statePointerContract:true,deepFrozen:true,brokenReferenceCount:0}))'
```

本轮真实输出：`{"readingMinutes":34,"body":8930,"sections":6,"minParagraph":119,"sources":["res-agent-coala","res-agent-lilian-weng","res-agent-hello-agents","res-agent-berkeley-course"],"misconceptions":6,"recap":11,"observationProvenance":true,"statePointerContract":true,"deepFrozen":true,"brokenReferenceCount":0}`。

### `agent-07` canonical event 与可信 snapshot prefix probe

以下命令重算 7 组 event 的 canonical SHA-256。Snapshot loader 不把自带 stateHash 当信任根：它从 canonical event store 取得声明 range、重新验链并用受支持的 reducerVersion 重放 prefix，再比较 sequence、lastHash、derivedState 与 stateHash；因此同步重算 checksum 的伪造状态仍会被拒绝：

```bash
node --input-type=module -e 'import assert from"node:assert/strict";import{createHash}from"node:crypto";const{agent07Note:n}=await import("./src/data/agent-mechanism-notes/agent-07.js"),z=JSON.parse(n.sections.find(x=>x.id==="event-replay-and-derived-state").callout.body),canon=x=>Array.isArray(x)?"["+x.map(canon).join(",")+"]":x&&typeof x==="object"?"{"+Object.keys(x).sort().map(k=>JSON.stringify(k)+":"+canon(x[k])).join(",")+"}":JSON.stringify(x),sha=x=>createHash("sha256").update(canon(x)).digest("hex"),eh=({eventId,sequence,previousHash,delta})=>sha({eventId,sequence,previousHash,delta}),set=(x,p,v)=>{const a=p.split("."),k=a.pop();a.reduce((o,q)=>o[q],x)[k]=v},events=c=>{if(c.events)return structuredClone(c.events);const e=structuredClone(z.baseEvents);if(c.mode==="base")return e;if(c.insertCopyOf){e.splice(e.findIndex(x=>x.eventId===c.insertAfter)+1,0,structuredClone(e.find(x=>x.eventId===c.insertCopyOf)));return e}if(c.replace){e.find(x=>x.eventId===c.replace.eventId).delta=structuredClone(c.replace.delta);return e}e.splice(e.findIndex(x=>x.eventId===c.insertAfter)+1,0,structuredClone(c.insertEvent));return e},run=(name,seed,es)=>{let sequence=seed.sequence,lastHash=seed.lastHash,derivedState=structuredClone(seed.derivedState),applied=0,duplicatesIgnored=0,result="OK",failureReason;const seen=new Map(seed.seenEntries||[]);for(const e of es){if(eh(e)!==e.hash){result="BLOCKED_EVENT_INTEGRITY";failureReason="HASH_MISMATCH";break}if(seen.has(e.eventId)){if(seen.get(e.eventId)!==e.hash){result="BLOCKED_EVENT_INTEGRITY";failureReason="DUPLICATE_CONFLICT";break}duplicatesIgnored++;continue}if(e.sequence!==sequence+1){result="BLOCKED_EVENT_INTEGRITY";failureReason="SEQUENCE_CONFLICT";break}if(e.previousHash!==lastHash){result="BLOCKED_EVENT_INTEGRITY";failureReason="PREVIOUS_HASH_MISMATCH";break}seen.set(e.eventId,e.hash);sequence=e.sequence;lastHash=e.hash;applied++;if("turnsRemaining"in e.delta)derivedState.budgets.turnsRemaining=e.delta.turnsRemaining;for(const k of["factVersion","pending","currentContractVersion"])if(k in e.delta)derivedState[k]=e.delta[k]}return{name,result,...failureReason&&{failureReason},sequence,lastHash,derivedState,applied,duplicatesIgnored,seenEntries:[...seen]}},runs=z.cases.map(c=>run(c.case,z.seed,events(c))),actual=runs.map(({name,seenEntries,...x})=>({case:name,...x}));assert.deepEqual(actual,z.expected);const t=z.snapshot.trusted,load=x=>{if(x.schemaVersion!==1||x.reducerVersion!=="agent07-reducer-v1")return"REJECT_SNAPSHOT_VERSION";const r=x.createdFromEventRange;if(!Number.isInteger(r?.from)||!Number.isInteger(r?.to)||r.from!==1||r.to!==x.lastSequence)return"REJECT_SNAPSHOT_PREFIX_BINDING";const p=z.baseEvents.slice(r.from-1,r.to),replay=run("prefix",z.seed,p);if(replay.result!=="OK"||replay.sequence!==x.lastSequence||replay.lastHash!==x.lastHash||r.prefixHash!==replay.lastHash||p.at(-1)?.eventId!==x.lastEventId)return"REJECT_SNAPSHOT_PREFIX_BINDING";if(x.stateHash!==sha(x.derivedState))return"REJECT_SNAPSHOT_STATE_HASH";if(canon(replay.derivedState)!==canon(x.derivedState)||sha(replay.derivedState)!==x.stateHash)return"REJECT_SNAPSHOT_PREFIX_STATE_MISMATCH";return"OK"},snap=z.snapshot.cases.map(c=>{const x=structuredClone(t);if(c.mutation)set(x,c.mutation.path,c.mutation.value);for(const m of c.mutations||[])set(x,m.path,m.value);return{case:c.case,result:load(x)}});assert.deepEqual(snap,z.snapshot.expected);const p=z.baseEvents.slice(0,t.lastSequence),tail=run("tail",{sequence:t.lastSequence,lastHash:t.lastHash,derivedState:t.derivedState,seenEntries:p.map(e=>[e.eventId,e.hash])},z.baseEvents.slice(t.lastSequence)),full=runs.find(x=>x.name==="normal"),norm=x=>({sequence:x.sequence,lastHash:x.lastHash,derivedState:x.derivedState});assert.deepEqual(norm(tail),norm(full));console.log(JSON.stringify({eventCases:actual.map(x=>[x.case,x.result,x.failureReason||null]),snapshotCases:snap,replayStateHash:sha(norm(full)),snapshotReplaySameState:true,duplicateDidNotReapply:runs.find(x=>x.name==="duplicate").duplicatesIgnored}))'
```

本轮真实输出：`{"eventCases":[["normal","OK",null],["duplicate","OK",null],["tampered-payload-old-hash","BLOCKED_EVENT_INTEGRITY","HASH_MISMATCH"],["conflicting-duplicate","BLOCKED_EVENT_INTEGRITY","DUPLICATE_CONFLICT"],["same-sequence-different-id","BLOCKED_EVENT_INTEGRITY","SEQUENCE_CONFLICT"],["sequence-gap","BLOCKED_EVENT_INTEGRITY","SEQUENCE_CONFLICT"],["wrong-previous-hash","BLOCKED_EVENT_INTEGRITY","PREVIOUS_HASH_MISMATCH"]],"snapshotCases":[{"case":"trusted","result":"OK"},{"case":"forged-derived-state","result":"REJECT_SNAPSHOT_STATE_HASH"},{"case":"rehashed-forged-derived-state","result":"REJECT_SNAPSHOT_PREFIX_STATE_MISMATCH"},{"case":"wrong-schema-version","result":"REJECT_SNAPSHOT_VERSION"},{"case":"wrong-reducer-version","result":"REJECT_SNAPSHOT_VERSION"},{"case":"wrong-prefix-binding","result":"REJECT_SNAPSHOT_PREFIX_BINDING"}],"replayStateHash":"c2c57e62ed0ef82183c666944e2c6c22c964dff71729dd1158e53b9c221cd7e7","snapshotReplaySameState":true,"duplicateDidNotReapply":1}`。

### `agent-07` summary/gate 与 actual manifest probe

以下命令展开 summary range、重算 allowedMode，并拒绝 `reconcileBeforeAct=false` 的权限/pending 语义冲突。除验证 fixture packer 外，它直接解析 20 轮 compressed output，忽略 manifest 自报理由后按其 reserve 与 blocks 重算，再与 actual manifest `deepEqual`：

```bash
node --input-type=module -e 'import assert from"node:assert/strict";const{agent07Note:n}=await import("./src/data/agent-mechanism-notes/agent-07.js"),sec=id=>n.sections.find(s=>s.id===id),obj=t=>JSON.parse(t.slice(t.indexOf("{"),t.lastIndexOf("}")+1)),arr=t=>JSON.parse(t.slice(t.indexOf("["),t.lastIndexOf("]")+1)),get=(x,p)=>p.split(".").reduce((o,k)=>o?.[k],x),set=(x,p,v)=>{const a=p.split("."),k=a.pop();a.reduce((o,q)=>o[q],x)[k]=v},del=(x,p)=>{const a=p.split("."),k=a.pop();delete a.reduce((o,q)=>o[q],x)[k]},state=obj(sec("event-replay-and-derived-state").paragraphs[1]),ctx=obj(sec("twenty-turn-compression-walkthrough").paragraphs[1]),req=arr(sec("context-assembly-and-invariant-gates").paragraphs[1]),nested=["taskContract.current.version","taskContract.current.hardConstraints","taskContract.current.permissions","taskContract.current.changeProvenance"],z=JSON.parse(sec("semantic-probes-and-module-boundary").callout.body),expand=r=>{let m=/^evt-(\d+)$/.exec(r);if(m)return[+m[1]];m=/^evt-(\d+)\.\.evt-(\d+)$/.exec(r);return!m||+m[1]>+m[2]?null:Array.from({length:+m[2]-+m[1]+1},(_,i)=>+m[1]+i)},known=new Set(ctx.eventIndex.ranges.flatMap(r=>expand(r)||[]).concat(ctx.eventIndex.events.flatMap(r=>expand(r)||[]))),gate=x=>{if([...req,...nested].some(p=>get(x,p)===undefined))return{result:"REJECT_MISSING_INVARIANT",actAllowed:false};const pv=x.taskContract.previous.version,cv=x.taskContract.current.version,av=x.pendingSideEffect.authorizedByContractVersion;if(![pv,cv,av,x.eventCursor,x.summary.version,x.summary.coversThroughEvent].every(Number.isInteger))return{result:"REJECT_INVARIANT_TYPE",actAllowed:false};if(!Array.isArray(x.taskContract.current.permissions)||!Array.isArray(x.summary.derivedFrom)||!x.summary.provenance||typeof x.summary.generator!=="string")return{result:"REJECT_SUMMARY_PROVENANCE",actAllowed:false};if(!(pv<cv)||av>cv)return{result:"REJECT_VERSION_RELATION",actAllowed:false};const allowedMode=x.pendingSideEffect.status==="UNKNOWN_OUTCOME"&&av<cv&&x.taskContract.current.permissions.every(p=>p.endsWith(".read"))?"RECONCILE_ONLY":"ACT_OR_OTHER",actAllowed=allowedMode!=="RECONCILE_ONLY";if(allowedMode==="RECONCILE_ONLY"&&x.pendingSideEffect.reconcileBeforeAct!==true)return{result:"REJECT_PERMISSION_OR_PENDING_SEMANTICS",actAllowed:false};if(x.pendingSideEffect.actAllowed!==actAllowed)return{result:"REJECT_DERIVED_CONTROL_MISMATCH",actAllowed:false};if(x.summary.version!==z.gatePolicy.expectedSummaryVersion)return{result:"REJECT_SUMMARY_VERSION",actAllowed:false};if(x.summary.coversThroughEvent>x.eventCursor)return{result:"REJECT_SUMMARY_RANGE",actAllowed:false};const refs=x.summary.derivedFrom.map(expand);if(!refs.length||refs.some(r=>!r)||refs.flat().some(e=>!known.has(e)))return{result:"REJECT_SUMMARY_EVENT_REFERENCE",actAllowed:false};const covered=new Set(refs.flat());for(let e=z.gatePolicy.coverageStart;e<=x.summary.coversThroughEvent;e++)if(!covered.has(e))return{result:"REJECT_STALE_OR_UNTRACEABLE_SUMMARY",actAllowed:false};if(x.summary.generator!==z.gatePolicy.allowedGenerator||typeof x.summary.provenance.assemblerVersion!=="string"||x.summary.provenance.sourceRunId!==x.runId)return{result:"REJECT_SUMMARY_PROVENANCE",actAllowed:false};return{result:"ACCEPT",allowedMode,actAllowed}},actual=z.gateCases.map(c=>{const x=structuredClone(ctx);if(c.mutation)set(x,c.mutation.path,c.mutation.value);if(c.removePath)del(x,c.removePath);return{case:c.case,...gate(x)}});for(const x of[state,ctx]){assert.deepEqual([...req,...nested].filter(p=>get(x,p)===undefined),[]);assert.ok(x.knowledge.qualifiedObservations.length>0&&x.knowledge.qualifiedObservations.every(o=>typeof o.sourceRef==="string"&&o.sourceRef.length>0&&typeof o.sourceVersion==="string"&&o.sourceVersion.length>0&&typeof o.id==="string"&&typeof o.integrityStatus==="string"))}const sp=state.artifactPointers[0],sb=sp.accessBoundary,caller=z.pointerPolicy.caller;assert.ok(Number.isInteger(sp.version)&&sp.version>0);assert.equal(sp.runId,state.runId);assert.equal(sp.tenantId,caller.tenantId);assert.deepEqual({scope:sb.scope,tenantId:sb.tenantId,runId:sb.runId,permission:sb.permission},{scope:"run",tenantId:sp.tenantId,runId:sp.runId,permission:z.pointerPolicy.typePermissions[sp.type]});assert.ok(caller.acl.includes(sb.permission));assert.deepEqual(actual,z.gateExpected);const normal=z.packing.cases.find(x=>x.case==="normal-pack"),pack=c=>{const blocks=c.blocksFrom?normal.blocks:c.blocks,availableTokens=c.windowTokens-c.fixedReserve-c.outputReserve-c.safetyMargin,invariants=blocks.filter(x=>["P0","P1"].includes(x.priority)).reduce((a,x)=>a+x.tokenEstimate,0);if(invariants>availableTokens)return{case:c.case,result:"BLOCKED_CONTEXT_INVARIANTS_OVER_BUDGET",availableTokens,actAllowed:false};let usedTokens=0,stop=false;const why={P0:"required-invariant",P1:"decision-evidence"},manifest=blocks.map(b=>{const common={...b,assemblerVersion:z.packing.assemblerVersion};if(stop)return{...common,omittedReason:"higher-priority-capacity-stop"};if(usedTokens+b.tokenEstimate>availableTokens){stop=true;return{...common,omittedReason:"capacity"}}usedTokens+=b.tokenEstimate;return{...common,includedReason:why[b.priority]||"remaining-capacity"}});return{case:c.case,result:"PACKED",availableTokens,usedTokens,manifest}},packed=z.packing.cases.map(pack);assert.deepEqual(packed,z.packing.expected);const repack=m=>{const availableTokens=m.windowTokens-m.fixedReserve-m.outputReserve-m.safetyMargin;let usedTokens=0,stop=false;const why={P0:"required-invariant",P1:"decision-evidence"},blocks=m.blocks.map(b=>{const common={id:b.id,priority:b.priority,sourceRef:b.sourceRef,version:b.version,tokenEstimate:b.tokenEstimate};if(stop)return{...common,includedReason:null,omittedReason:"higher-priority-capacity-stop"};if(usedTokens+b.tokenEstimate>availableTokens){stop=true;return{...common,includedReason:null,omittedReason:"capacity"}}usedTokens+=b.tokenEstimate;return{...common,includedReason:why[b.priority]||"remaining-capacity",omittedReason:null}});return{assemblerVersion:m.assemblerVersion,windowTokens:m.windowTokens,fixedReserve:m.fixedReserve,outputReserve:m.outputReserve,safetyMargin:m.safetyMargin,availableTokens,usedTokens,status:"PACKED",blocks}},actualManifest=repack(ctx.manifest);assert.deepEqual(actualManifest,ctx.manifest);console.log(JSON.stringify({requiredPaths:req.length,stateMissing:req.filter(p=>get(state,p)===undefined),contextMissing:req.filter(p=>get(ctx,p)===undefined),gate:actual,packing:packed,observationProvenance:true,statePointerContract:true,actualManifest:{status:actualManifest.status,availableTokens:actualManifest.availableTokens,usedTokens:actualManifest.usedTokens,blocks:actualManifest.blocks.length,repackedDeepEqual:true}}))'
```

本轮真实输出：`{"requiredPaths":16,"stateMissing":[],"contextMissing":[],"gate":[{"case":"normal","result":"ACCEPT","allowedMode":"RECONCILE_ONLY","actAllowed":false},{"case":"reconcile-before-act-false","result":"REJECT_PERMISSION_OR_PENDING_SEMANTICS","actAllowed":false},{"case":"summary-self-reports-act","result":"REJECT_DERIVED_CONTROL_MISMATCH","actAllowed":false},{"case":"summary-version-999","result":"REJECT_SUMMARY_VERSION","actAllowed":false},{"case":"summary-nonexistent-event-ref","result":"REJECT_SUMMARY_EVENT_REFERENCE","actAllowed":false},{"case":"summary-partial-range","result":"REJECT_STALE_OR_UNTRACEABLE_SUMMARY","actAllowed":false},{"case":"summary-covers-beyond-cursor","result":"REJECT_SUMMARY_RANGE","actAllowed":false},{"case":"summary-without-provenance","result":"REJECT_SUMMARY_PROVENANCE","actAllowed":false},{"case":"invalid-version-type","result":"REJECT_INVARIANT_TYPE","actAllowed":false},{"case":"non-monotonic-contract","result":"REJECT_VERSION_RELATION","actAllowed":false},{"case":"pending-authorized-by-future","result":"REJECT_VERSION_RELATION","actAllowed":false},{"case":"missing-hard-constraint","result":"REJECT_MISSING_INVARIANT","actAllowed":false},{"case":"missing-pending","result":"REJECT_MISSING_INVARIANT","actAllowed":false}],"packing":[{"case":"normal-pack","result":"PACKED","availableTokens":4300,"usedTokens":3800,"manifest":[{"id":"invariants","priority":"P0","sourceRef":"state://run-supplier-17","version":4,"tokenEstimate":1700,"assemblerVersion":"context-policy-2","includedReason":"required-invariant"},{"id":"observations","priority":"P1","sourceRef":"events://run-supplier-17?from=17&to=20","version":20,"tokenEstimate":2100,"assemblerVersion":"context-policy-2","includedReason":"decision-evidence"},{"id":"history","priority":"P2","sourceRef":"summary://run-supplier-17?v=4","version":4,"tokenEstimate":900,"assemblerVersion":"context-policy-2","omittedReason":"capacity"},{"id":"chitchat","priority":"P3","sourceRef":"transcript://run-supplier-17?to=20","version":20,"tokenEstimate":400,"assemblerVersion":"context-policy-2","omittedReason":"higher-priority-capacity-stop"}]},{"case":"invariants-over-budget","result":"BLOCKED_CONTEXT_INVARIANTS_OVER_BUDGET","availableTokens":3500,"actAllowed":false}],"observationProvenance":true,"statePointerContract":true,"actualManifest":{"status":"PACKED","availableTokens":4300,"usedTokens":3800,"blocks":4,"repackedDeepEqual":true}}`。

### `agent-07` fixture 与 actual pointer resolver probe

以下命令既验证 9 组反例，也直接解析 20 轮 compressed output 的 event-range/artifact pointers；两者都经 type/scheme、URI authority、metadata、accessBoundary、caller ACL 和可信 store ACL/content hash 的同一 resolver：

```bash
node --input-type=module -e 'import assert from"node:assert/strict";import{createHash}from"node:crypto";const{agent07Note:n}=await import("./src/data/agent-mechanism-notes/agent-07.js"),sec=id=>n.sections.find(s=>s.id===id),obj=t=>JSON.parse(t.slice(t.indexOf("{"),t.lastIndexOf("}")+1)),ctx=obj(sec("twenty-turn-compression-walkthrough").paragraphs[1]),z=JSON.parse(sec("semantic-probes-and-module-boundary").callout.body),canon=x=>Array.isArray(x)?"["+x.map(canon).join(",")+"]":x&&typeof x==="object"?"{"+Object.keys(x).sort().map(k=>JSON.stringify(k)+":"+canon(x[k])).join(",")+"}":JSON.stringify(x),sha=x=>createHash("sha256").update(canon(x)).digest("hex"),set=(x,p,v)=>{const a=p.split("."),k=a.pop();a.reduce((o,q)=>o[q],x)[k]=v},caller=z.pointerPolicy.caller,resolve=(p,store,acl)=>{const u=new URL(p.uri);if(z.pointerPolicy.typeSchemes[p.type]!==u.protocol)return"REJECT_POINTER_TYPE_SCHEME";if(u.hostname!==caller.runId)return"REJECT_POINTER_URI_RUN";if(p.runId!==caller.runId)return"REJECT_POINTER_RUN";if(p.tenantId!==caller.tenantId)return"REJECT_POINTER_TENANT";if(p.accessBoundary?.scope!=="run"||p.accessBoundary.runId!==caller.runId||p.accessBoundary.tenantId!==caller.tenantId||p.accessBoundary.permission!==z.pointerPolicy.typePermissions[p.type])return"REJECT_POINTER_ACCESS_BOUNDARY";if(!acl.includes(p.accessBoundary.permission)||!store.acl.includes(p.accessBoundary.permission))return"REJECT_POINTER_ACL";if(p.integrity!=="sha256:"+sha(store.content))return"REJECT_POINTER_INTEGRITY";return"OBSERVATION_ONLY"},base=z.pointerCases.find(x=>x.case==="valid"),actual=z.pointerCases.map(c=>{const f=structuredClone(c.from==="valid"?base:c);if(c.mutation)set(f,c.mutation.path,c.mutation.value);const result=resolve(f.pointer,{content:f.storeContent,acl:f.storeAcl},c.callerAcl??caller.acl);return result==="OBSERVATION_ONLY"?{case:c.case,result,factsBefore:1,factsAfter:1}:{case:c.case,result}});assert.deepEqual(actual,z.pointerExpected);const stores=new Map([["events://run-supplier-17?from=1&to=20",{content:{runId:"run-supplier-17",from:1,to:20},acl:["events:read"]}],["artifact://run-supplier-17/board-9?v=5",{content:{artifactId:"board-9",version:5},acl:["artifact:read"]}]]),actualContextPointers=ctx.artifactPointers.map(p=>({type:p.type,result:resolve(p,stores.get(p.uri),caller.acl),factsBefore:1,factsAfter:1}));assert.equal(actualContextPointers.length,2);assert.ok(actualContextPointers.every(x=>x.result==="OBSERVATION_ONLY"&&x.factsBefore===x.factsAfter));console.log(JSON.stringify({pointers:actual,actualContextPointers}))'
```

本轮真实输出：`{"pointers":[{"case":"valid","result":"OBSERVATION_ONLY","factsBefore":1,"factsAfter":1},{"case":"invalid-scheme","result":"REJECT_POINTER_TYPE_SCHEME"},{"case":"type-scheme-mismatch","result":"REJECT_POINTER_TYPE_SCHEME"},{"case":"uri-only-cross-run","result":"REJECT_POINTER_URI_RUN"},{"case":"cross-run","result":"REJECT_POINTER_RUN"},{"case":"cross-tenant","result":"REJECT_POINTER_TENANT"},{"case":"access-boundary-mismatch","result":"REJECT_POINTER_ACCESS_BOUNDARY"},{"case":"no-permission","result":"REJECT_POINTER_ACL"},{"case":"hash-mismatch","result":"REJECT_POINTER_INTEGRITY"}],"actualContextPointers":[{"type":"event-range","result":"OBSERVATION_ONLY","factsBefore":1,"factsAfter":1},{"type":"artifact","result":"OBSERVATION_ONLY","factsBefore":1,"factsAfter":1}]}`。

### `agent-08` 单课质量与可追溯性

- `courseFieldBasis`：objectives[0..1]、concepts[0..4]、quiz[0..1]、`iq-agent-08-1..3` 的 shortAnswer/deepDive/misconceptions/followUps、exercise.steps[0..2]/deliverable 与 completionCriteria[0..1] 已全部映射到 coverage matrix；不是 resource evidence。
- `知识结构`：七节按可信架构→任务契约/完成证据→四个只读工具/安全 gate→状态/计划/循环/恢复→三条主轨及 timeout/handoff→手写/框架/Harness→模块边界/语义验收递进。Architecture、task、tools、state、three traces 与 semantic fixture 均为可直接 `JSON.parse` 的独立数据。
- `轨迹与谓词`：payloadHash 覆盖 success/data/error/contentTrust/resourceVersion；secret/PII 使用全量 host scanner，content 使用 host policy，claims/terminal 使用带 reportHash 的 host validator，model:// 均拒绝。预算绑定 phase/delta/deadline/action chain，并以 Set 拒绝复用 reservationId/eventId；35 组 completion mutation 全部按预期。
- `安全与框架边界`：reconcile 仅吸收 validated OBSERVED、可信 obs:// resultRef；PENDING、伪造/未验证/冲突 tuple 均 handoff。safe retry 要求 resultRef=null、精确 reservationIds 与宿主 NOT_EXECUTED/SAFE_TO_RETRY attestation，否则 unknown-handoff。handoff reason/packet 与策略和 terminal event 绑定；29 组 recovery 与 11 组 reconcile mutation 全部按预期。
- `来源`：五个实际 source ID 均三重解析；OpenAI/MS/HF 只支持 Agent 组成、工具、循环、风险和框架路线，AgentBench/τ-bench 只支持各自评测边界。`res-agent-douyin-claude-code` 仅保留为 extension，未用作章节机制 evidence。
- `质量自评`：覆盖 25/25；结构 20/20；来源 24/25（五个 ID 三重解析，全部生产协议标课程综合）；教学 20/20（35 completion、29 recovery、11 reconcile、15 security、5 envelope 与 4 预算重放）；数据契约 9/10（正文 6421 字符、39 分钟、七节、纯数据、稳定 ID、递归冻结）。总分 `98/100`，`brokenReferenceCount: 0`；状态 `ready-for-integration`，模块仍为 `publicationReady: false`。

### `agent-08` 可复制 contract/source/freeze probe

```bash
node --input-type=module -e 'import assert from"node:assert/strict";import{agent08Note as n}from"./src/data/agent-mechanism-notes/agent-08.js";import{agentMechanism as c}from"./src/data/agent-mechanism.js";const lesson=c.lessons.find(x=>x.id==="agent-08"),reg=new Map(c.resources.map(x=>[x.id,x])),body=[n.introduction,...n.sections.flatMap(s=>s.paragraphs),n.nextStep].reduce((a,s)=>a+s.trim().length,0),ids=n.sections.map(s=>s.id),deep=v=>{if(v&&typeof v==="object"){assert.equal(Object.isFrozen(v),true);Object.values(v).forEach(deep)}};assert.ok(n.readingMinutes>=35&&n.readingMinutes<=40);assert.equal(n.sections.length,7);assert.equal(new Set(ids).size,ids.length);assert.ok(body>=5000&&body<=9000);assert.ok(n.sections.every(s=>/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(s.id)&&s.paragraphs.length>=2&&s.paragraphs.length<=4&&s.paragraphs.every(p=>p.trim().length>=60)&&s.keyPoints.length>=2&&s.sourceIds.length>=1&&new Set(s.sourceIds).size===s.sourceIds.length&&s.sourceIds.every(id=>lesson.resourceIds.includes(id)&&reg.has(id)&&reg.get(id).evidence)));for(const s of n.sections)if(s.callout)JSON.parse(s.callout.body);assert.ok(n.misconceptions.length>=4&&n.misconceptions.length<=6);assert.ok(n.recap.length>=5);deep(n);const sources=[...new Set(n.sections.flatMap(s=>s.sourceIds))];assert.equal(sources.includes("res-agent-douyin-claude-code"),false);console.log(JSON.stringify({readingMinutes:n.readingMinutes,body,sections:n.sections.length,minParagraph:Math.min(...n.sections.flatMap(s=>s.paragraphs.map(p=>p.trim().length))),sources,misconceptions:n.misconceptions.length,recap:n.recap.length,jsonCallouts:n.sections.filter(s=>s.callout).length,sourceTripleResolution:true,extensionUsedAsMechanismEvidence:false,deepFrozen:true,brokenReferenceCount:0}))'
```

本轮真实输出：`{"readingMinutes":39,"body":6421,"sections":7,"minParagraph":175,"sources":["res-agent-openai-guide","res-agent-ms-course","res-agent-hf-course","res-agent-tau-bench","res-agent-agentbench"],"misconceptions":6,"recap":11,"jsonCallouts":6,"sourceTripleResolution":true,"extensionUsedAsMechanismEvidence":false,"deepFrozen":true,"brokenReferenceCount":0}`。

### `agent-08` 完成证据、预算、恢复与可信路径语义 probe

以下命令重算 payloadHash/resourceVersion、全量 scanner/content/validator 可信类型与 reportHash、全局唯一预算 ID，以及 TIMEOUT route 与 handoff reason/packet。它执行 35 组 completion、29 组 recovery、11 组 reconcile、15 组 security 与 5 组 envelope；reconcile 覆盖 verified ledger-hit、host-attested safe-retry、PENDING/冲突/未知 handoff：

```bash
node --input-type=module -e 'import assert from "node:assert/strict"; import { createHash } from "node:crypto"; import path from "node:path"; import { agent08Note as n } from "./src/data/agent-mechanism-notes/agent-08.js"; import { agentMechanism as c } from "./src/data/agent-mechanism.js"; const sec = (id) => n.sections.find((s) => s.id === id); const task = JSON.parse(sec("task-contract-and-evidence-predicate").callout.body); const spec = JSON.parse(sec("trusted-readonly-tools-and-security-gates").callout.body); const state = JSON.parse(sec("state-plan-loop-and-recovery").callout.body); const traces = JSON.parse(sec("three-auditable-terminal-traces").callout.body); const z = JSON.parse(sec("module-boundaries-and-semantic-probes").callout.body); const ctx = z.trustedContext; const revision = task.facts.find((x) => x.id === "fact-revision").value; const tools = new Map(spec.tools.map((x) => [x.name, x])); const plain = (x) => x !== null && typeof x === "object" && !Array.isArray(x); const canonical = (x) => Array.isArray(x) ? `[${x.map(canonical).join(",")}]` : plain(x) ? `{${Object.keys(x).sort().map((k) => `${JSON.stringify(k)}:${canonical(x[k])}`).join(",")}}` : JSON.stringify(x); const digest = (x) => `sha256:${createHash("sha256").update(canonical(x)).digest("hex")}`; const actionKey = (a) => `${a.tool}|${digest(a.args)}|${revision}|contract-1|plan-1`; const descendantOf = (candidate, oracle) => candidate === oracle || candidate.startsWith(`${oracle}/`); const schemaValid = (value, schema) => { if ("const" in schema && value !== schema.const) return false; if (schema.enum && !schema.enum.includes(value)) return false; if (schema.type === "object") { if (!plain(value) || (schema.required || []).some((k) => !(k in value))) return false; if (schema.additionalProperties === false && Object.keys(value).some((k) => !(k in (schema.properties || {})))) return false; return Object.entries(value).every(([k, v]) => !schema.properties?.[k] || schemaValid(v, schema.properties[k])); } if (schema.type === "array") return Array.isArray(value) && (schema.minItems === undefined || value.length >= schema.minItems) && (schema.maxItems === undefined || value.length <= schema.maxItems) && value.every((x) => schemaValid(x, schema.items)); if (schema.type === "string") return typeof value === "string" && (schema.minLength === undefined || value.length >= schema.minLength) && (schema.maxLength === undefined || value.length <= schema.maxLength) && (!schema.pattern || new RegExp(schema.pattern).test(value)); if (schema.type === "integer") return Number.isInteger(value) && (schema.minimum === undefined || value >= schema.minimum) && (schema.maximum === undefined || value <= schema.maximum); if (schema.type === "boolean") return typeof value === "boolean"; return schema.type === undefined || typeof value === schema.type; }; const argsValid = (a) => { const tool = tools.get(a.tool); if (!tool || !schemaValid(a.args, tool.schema)) return false; return a.tool !== "read_file" || (a.args.endLine >= a.args.startLine && a.args.endLine - a.args.startLine + 1 <= tool.schema.maxLines); }; const envelopeValid = (candidate) => { const matches = spec.candidateEnvelopeSchema.oneOf.filter((s) => schemaValid(candidate, s)); return matches.length === 1 && (candidate.kind !== "tool" || argsValid(candidate)); }; const actionValid = (a) => a.kind === "tool" && Number.isInteger(a.attempt) && a.attempt > 0 && argsValid(a) && a.argsHash === digest(a.args) && a.actionKey === actionKey(a) && envelopeValid({ kind: a.kind, tool: a.tool, args: a.args }); const tupleMatches = (a, x) => x.callId === a.callId && x.actionKey === a.actionKey && x.argsHash === a.argsHash && x.attempt === a.attempt; const attestationTupleMatches = (a, x) => x.callId === a.callId && x.actionKey === a.actionKey && x.argsHash === a.argsHash; const expandedSafe = (expanded) => { const normalized = expanded.normalize("NFC"); const resolved = path.posix.resolve(ctx.approvedRoot, normalized); if (!(resolved === ctx.approvedRoot || resolved.startsWith(`${ctx.approvedRoot}/`))) return false; if (ctx.oracles.symlinks.some((o) => o.policyVersion === ctx.pathPolicy.version && descendantOf(normalized, o.path.normalize("NFC")))) return false; return !ctx.oracles.sensitive.some((o) => o.policyVersion === ctx.contentPolicy.version && descendantOf(normalized, o.path.normalize("NFC"))); }; const resolutionFor = (a) => ctx.resolutionEvidence.find((r) => tupleMatches(a, r) && r.target === (a.tool === "search_text" ? a.args.pathGlob : a.args.path)); const resolutionValid = (a, r) => r && tupleMatches(a, r) && canonical(r.args) === canonical(a.args) && r.issuer === "host-filesystem" && r.method === "openat-beneath+ancestor-lstat" && r.openatBeneath === true && r.ancestorLstatComplete === true && r.policyVersion === ctx.pathPolicy.version && r.evidenceRef === `resolve://${a.callId}` && Array.isArray(r.expandedPaths) && r.expandedPaths.length > 0; const securityGate = ({ candidate: a, contentObservation }) => { const tool = tools.get(a.tool); if (a.repoHandle && a.repoHandle !== ctx.repoHandle) return { result: "DENY_REPO_HANDLE_OVERRIDE", evidenceRef: "auth://repo-selection/17" }; if (!tool || !ctx.permissions.includes(tool.permission)) return { result: "DENY_PERMISSION", evidenceRef: ctx.pathPolicy.evidenceRef }; if (contentObservation && contentObservation.contentTrust !== ctx.contentPolicy.allowedObservationTrust[a.tool]) return { result: "DENY_CONTENT_TRUST", evidenceRef: ctx.contentPolicy.evidenceRef }; if (contentObservation?.contentTrust === "untrusted-repo-content" && a.requestedPermission && !ctx.permissions.includes(a.requestedPermission)) return { result: "DENY_AUTHORITY_ESCALATION", evidenceRef: ctx.contentPolicy.evidenceRef }; if (a.tool === "run_check") return ctx.checkPolicy.allowlist.includes(a.args.checkId) ? { result: "ALLOW_OBSERVATION_ONLY", evidenceRef: ctx.checkPolicy.evidenceRef } : { result: "DENY_CHECK_ALLOWLIST", evidenceRef: ctx.checkPolicy.evidenceRef }; const raw = a.tool === "search_text" ? a.args.pathGlob : a.args.path; if (a.tool === "search_text" && !ctx.searchGlobAllowlist.includes(raw)) return { result: "DENY_GLOB", evidenceRef: ctx.pathPolicy.evidenceRef }; if (typeof raw !== "string" || raw.includes("%")) return { result: "DENY_PATH_ENCODING", evidenceRef: ctx.pathPolicy.evidenceRef }; const normalized = raw.normalize("NFC"); if (raw.includes("\0") || raw.includes("\\") || path.posix.isAbsolute(raw) || normalized.split("/").some((x) => x === "." || x === "..")) return { result: "DENY_PATH", evidenceRef: ctx.pathPolicy.evidenceRef }; const r = resolutionFor(a); if (!resolutionValid(a, r)) return { result: "DENY_RESOLUTION_EVIDENCE_MISSING", evidenceRef: ctx.pathPolicy.evidenceRef }; for (const expanded of r.expandedPaths) { const symlink = ctx.oracles.symlinks.find((o) => o.policyVersion === ctx.pathPolicy.version && descendantOf(expanded.normalize("NFC"), o.path.normalize("NFC"))); if (symlink) return { result: "DENY_SYMLINK", evidenceRef: symlink.evidenceRef }; const secret = ctx.oracles.sensitive.find((o) => o.policyVersion === ctx.contentPolicy.version && descendantOf(expanded.normalize("NFC"), o.path.normalize("NFC"))); if (secret) return { result: "DENY_SENSITIVE", evidenceRef: secret.evidenceRef }; if (!expandedSafe(expanded)) return { result: "DENY_PATH", evidenceRef: r.evidenceRef }; } return { result: "ALLOW_OBSERVATION_ONLY", evidenceRef: r.evidenceRef }; }; const outputAttestationValid = (a, o) => { const p = o.outputPolicy; return plain(p) && attestationTupleMatches(a, p) && p.issuer === ctx.outputPolicy.issuer && p.policyVersion === ctx.outputPolicy.policyVersion && p.dataHash === o.dataHash && p.decision === (o.success ? "ALLOW" : "NO_DATA") && p.secretPii === "PASS" && Array.isArray(p.redactions) && p.evidenceRef === `scanner://${a.callId}/${o.dataHash.slice(7)}`; }; const sandboxValid = (a, o) => { const s = o.sandboxAttestation; const policy = ctx.checkPolicy.sandbox; return plain(s) && attestationTupleMatches(a, s) && s.issuer === policy.issuer && s.policyVersion === policy.policyVersion && s.dataHash === o.dataHash && s.repositoryMount === policy.repositoryMount && s.network === policy.network && s.evidenceRef === `sandbox://${a.callId}/${o.dataHash.slice(7)}`; }; const echoAndExpansionValid = (a, o) => { if (a.tool === "run_check") return o.data.checkId === a.args.checkId; const r = resolutionFor(a); if (!resolutionValid(a, r)) return false; if (a.tool === "read_file") return o.data.path === a.args.path && o.data.startLine === a.args.startLine && o.data.endLine === a.args.endLine && r.expandedPaths.includes(o.data.path) && expandedSafe(o.data.path); if (a.tool === "search_text") return o.data.query === a.args.query && o.data.pathGlob === a.args.pathGlob && o.data.matches.every((m) => r.expandedPaths.includes(m.path) && expandedSafe(m.path)); const base = a.args.path.split("/").filter(Boolean).length; return o.data.path === a.args.path && o.data.maxDepth === a.args.maxDepth && o.data.paths.length > 0 && o.data.paths.every((p) => r.expandedPaths.includes(p) && expandedSafe(p) && p.split("/").filter(Boolean).length - base <= a.args.maxDepth); }; const resultPayload = (o) => ({ success: o.success, data: o.data, error: o.error, contentTrust: o.contentTrust, resourceVersion: o.resourceVersion }); const resultValid = (a, o) => { if (!plain(o) || typeof o.success !== "boolean" || !spec.resultProtocol.fields.every((k) => k in o) || !tupleMatches(a, o) || o.runId !== task.runId || o.tool !== a.tool || o.repoRevision !== revision || o.dataHash !== digest(resultPayload(o))) return false; if (!outputAttestationValid(a, o) || (a.tool === "run_check" && !sandboxValid(a, o))) return false; const expectedTrust = o.success ? spec.resultProtocol.contentTrust.success[a.tool] : spec.resultProtocol.contentTrust.failure; if (o.contentTrust !== expectedTrust || spec.resultProtocol.contentTrust.forbidden.includes(o.contentTrust)) return false; if (!o.success && !(spec.resultProtocol.failureResourceVersions[a.tool] || []).includes(o.resourceVersion)) return false; const allowedEvidence = spec.resultProtocol.evidenceTypes[a.tool][o.success ? "success" : "failure"]; if (!allowedEvidence.includes(o.evidenceType)) return false; if (o.success) { if (o.error !== null || o.data === null || !schemaValid(o.data, tools.get(a.tool).outputSchema) || !echoAndExpansionValid(a, o)) return false; if (a.tool !== "run_check" && o.resourceVersion !== revision) return false; return true; } return o.data === null && schemaValid(o.error, spec.resultProtocol.error.schema); }; const ledgerValid = (trace) => trace.resultLedger.length === trace.actions.length && trace.resultLedger.every((entry, i) => tupleMatches(trace.actions[i], entry) && entry.resultRef === trace.observations[i].evidenceRef && entry.status === "OBSERVED"); const executionEventsValid = (trace, terminalStatus = null) => { if (!trace.executionEvents.every((e, i) => e.sequence === i + 1)) return false; let cursor = 0; for (const [i, a] of trace.actions.entries()) { const group = trace.executionEvents.slice(cursor, cursor + 3); const reservationIds = Object.values(a.reservations).filter(Boolean).map((r) => r.reservationId); if (group.length !== 3 || group[0].type !== "RESERVATIONS_COMMITTED" || group[1].type !== "PENDING_ACTION_PERSISTED" || group[2].type !== "TOOL_RESULT_RECORDED") return false; if (!group.every((e) => tupleMatches(a, e) && canonical(e.reservationIds) === canonical(reservationIds))) return false; if (group[1].status !== "PERSISTED_BEFORE_ACT" || group[2].resultRef !== trace.observations[i].evidenceRef) return false; cursor += 3; } const tail = trace.executionEvents.slice(cursor); if (terminalStatus) return tail.length === 1 && tail[0].type === "TERMINAL" && tail[0].status === terminalStatus && !trace.executionEvents.slice(trace.executionEvents.indexOf(tail[0]) + 1).some((e) => e.type === "TOOL_RESULT_RECORDED"); return tail.length === 0; }; const traceResultsValid = (trace, terminalStatus = null) => { if (trace.actions.length !== trace.observations.length || !trace.actions.every(actionValid) || !budgetReplay(trace)) return false; const callIds = new Set(trace.actions.map((a) => a.callId)); const attempts = new Set(trace.actions.map((a) => `${a.actionKey}|${a.attempt}`)); return callIds.size === trace.actions.length && attempts.size === trace.actions.length && trace.actions.every((a, i) => resultValid(a, trace.observations[i])) && ledgerValid(trace) && executionEventsValid(trace, terminalStatus); }; const completion = (trace) => { if (!traceResultsValid(trace, "done") || new Set(trace.actions.map((a) => a.actionKey)).size !== trace.actions.length) return false; const observations = new Map(trace.observations.map((o) => [o.callId, o])); const decisions = trace.actions.map((a) => ({ callId: a.callId, ...securityGate({ candidate: a, contentObservation: observations.get(a.callId) }) })); if (decisions.some((x) => x.result !== "ALLOW_OBSERVATION_ONLY")) return false; const expectedManifest = decisions.map((x) => ({ callId: x.callId, constraintId: "trusted-registry-only", result: "PASS", securityResult: x.result, evidenceRef: x.evidenceRef })); if (canonical(trace.finalReport.actionSecurityManifest) !== canonical(expectedManifest)) return false; const hard = new Map(trace.finalReport.hardConstraintEvidence.map((x) => [x.id, x])); if (hard.size !== task.hardConstraints.length || task.hardConstraints.some((id) => !hard.has(id)) || trace.finalReport.hardConstraintEvidence.length !== hard.size) return false; if (![...hard.values()].every((x) => x.authority === "host-policy" && x.result === "PASS" && (typeof x.evidenceRef === "string" || Array.isArray(x.evidenceRefs)))) return false; if (!expectedManifest.every((m) => { const h = hard.get(m.constraintId); return h?.authority === "host-policy" && h.result === "PASS" && [h.evidenceRef, ...(h.evidenceRefs || [])].includes(m.evidenceRef); })) return false; const checkAction = trace.actions.find((a) => a.tool === "run_check"); const checkObservation = observations.get(checkAction.callId); if (!trace.finalReport.sandboxAttestations.some((s) => canonical(s) === canonical(checkObservation.sandboxAttestation))) return false; if (!["no-repository-writes", "no-network"].every((id) => hard.get(id)?.evidenceRef === checkObservation.sandboxAttestation.evidenceRef && hard.get(id)?.attestationType === "trusted-sandbox")) return false; const scannerRefs = trace.observations.map((o) => o.outputPolicy.evidenceRef); const secretEvidence = hard.get("do-not-expose-secrets-or-PII"); if (secretEvidence?.attestationType !== "host-output-scanner" || canonical(secretEvidence.evidenceRefs) !== canonical(scannerRefs) || secretEvidence.evidenceRef !== scannerRefs[0]) return false; const contentEvidence = hard.get("repo-content-is-untrusted-data"); if (contentEvidence?.attestationType !== "host-content-policy" || contentEvidence.evidenceRef !== z.trustedContext.contentPolicy.evidenceRef) return false; const validator = trace.finalReport.validatorClaim, validatorPolicy = z.trustedContext.validatorPolicy; const reportPayload = (r) => ({ claims: r.claims, hardConstraintEvidence: r.hardConstraintEvidence, unresolvedHighRiskConflicts: r.unresolvedHighRiskConflicts, actionSecurityManifest: r.actionSecurityManifest, sandboxAttestations: r.sandboxAttestations }); if (validator.issuer !== validatorPolicy.issuer || validator.policyVersion !== validatorPolicy.policyVersion || validator.evidenceRef !== validatorPolicy.evidenceRef || validator.reportHash !== digest(reportPayload(trace.finalReport)) || hard.get("report-claims-require-qualified-evidence")?.attestationType !== "host-validator" || hard.get("report-claims-require-qualified-evidence")?.evidenceRef !== validator.evidenceRef) return false; const byEvidence = new Map(trace.observations.map((o) => [o.evidenceRef, o])); const claims = new Map(trace.finalReport.claims.map((x) => [x.id, x])); const common = (x) => x && x.text && x.repoRevision === revision && x.evidenceRefs.length && x.evidenceRefs.every((r) => byEvidence.has(r)) && x.alternativesConsidered.length && x.verificationSteps.length; if (claims.size !== trace.finalReport.claims.length || trace.finalReport.claims.length === 0 || !trace.finalReport.claims.every(common)) return false; const symptom = claims.get("symptom"); const root = claims.get("root-cause"); if (!common(symptom) || !symptom.location || !symptom.evidenceRefs.some((r) => ["check", "user"].includes(byEvidence.get(r).evidenceType)) || !common(root) || !root.path || !root.line) return false; const rootObs = root.evidenceRefs.map((r) => byEvidence.get(r)); const code = rootObs.find((o) => o.evidenceType === "code"); const search = rootObs.find((o) => o.evidenceType === "search"); return !!code && !!search && code.data.path === root.path && root.line.start >= code.data.startLine && root.line.end <= code.data.endLine && search.data.matches.some((m) => m.path === root.path && m.line >= root.line.start && m.line <= root.line.end) && trace.finalReport.unresolvedHighRiskConflicts.length === 0 && validator.reportedResult === "PASS" && trace.terminal.status === "done" && trace.terminal.evidenceRef === validator.evidenceRef && trace.terminal.evidenceRef === validatorPolicy.evidenceRef; }; const set = (x, p, v) => { const keys = p.split("."); const last = keys.pop(); keys.reduce((o, k) => o[k], x)[last] = v; }; const mutate = (trace, mutation) => { if (!mutation) return; if (mutation.target === "action-copy") { const target = trace.actions.find((x) => x.callId === mutation.id); const source = trace.actions.find((x) => x.callId === mutation.sourceId); return set(target, mutation.path, source[mutation.path]); } const collections = { action: trace.actions, observation: trace.observations, ledger: trace.resultLedger, event: trace.executionEvents, budgetEvent: trace.budgetEvents }; const collection = collections[mutation.target]; const target = mutation.target === "trace" ? trace : mutation.target === "terminal" ? trace.terminal : mutation.index !== undefined ? collection[mutation.index] : mutation.target === "action" ? collection.find((x) => x.callId === mutation.id) : collection.find((x) => x.evidenceRef === mutation.id); if (mutation.operation === "push") return mutation.path.split(".").reduce((o, k) => o[k], target).push(mutation.value); if (mutation.operation === "splice") return mutation.path.split(".").reduce((o, k) => o[k], target).splice(mutation.index, 1); set(target, mutation.path, mutation.value); }; const rebindAttestation = (trace, mutation) => { const o = trace.observations.find((x) => x.evidenceRef === mutation.id); o.dataHash = digest(o.data); o.outputPolicy.dataHash = o.dataHash; o.outputPolicy.evidenceRef = `scanner://${o.callId}/${o.dataHash.slice(7)}`; if (o.sandboxAttestation) { o.sandboxAttestation.dataHash = o.dataHash; o.sandboxAttestation.evidenceRef = `sandbox://${o.callId}/${o.dataHash.slice(7)}`; } }; const completionCases = z.completionCases.map((fixture) => { const trace = structuredClone(traces.success); mutate(trace, fixture.mutation); for (const mutation of fixture.mutations || []) mutate(trace, mutation); if (fixture.rebindAttestation) rebindAttestation(trace, fixture.mutation); return { case: fixture.case, result: completion(trace) }; }); assert.deepEqual(completionCases, z.completionCases.map((x) => ({ case: x.case, result: x.expected }))); const recoverySemanticsValid = (trace, kind) => { const observations = trace.observations; if (trace.actions.length < 2 || trace.actions.some((a, i) => a.tool !== "run_check" || a.attempt !== i + 1 || a.actionKey !== trace.actions[0].actionKey || a.argsHash !== trace.actions[0].argsHash)) return false; if (kind === "timeoutRecovery") { const failures = observations.slice(0, -1), recovered = observations.at(-1); return failures.length > 0 && failures.every((o) => o.success === false && o.error.code === "TIMEOUT" && o.error.retryable === true && o.evidenceType === "check-timeout") && recovered.success === true && recovered.evidenceType === "check" && trace.terminal.status === "continue" && trace.terminal.evidenceRef === recovered.evidenceRef && !trace.executionEvents.some((e) => e.type === "TERMINAL"); } const terminal = trace.terminal, event = trace.executionEvents.at(-1), lastAttempt = trace.actions.at(-1).attempt; const evidence = observations.map((o) => o.evidenceRef); const timeoutRoute = observations.every((o, i) => o.success === false && o.error.code === "TIMEOUT" && o.evidenceType === "check-timeout" && o.error.retryable === (i < observations.length - 1)); const packetKeys = ["evidenceRef", "reason", "owner", "stateRef", "resumeToken", "nextAction", "attemptEvidence"]; return timeoutRoute && terminal.status === "handoff" && event.type === "TERMINAL" && event.status === "handoff" && packetKeys.every((k) => canonical(event[k]) === canonical(terminal[k])) && z.trustedContext.handoffPolicy.reasonAllowlist.includes(terminal.reason) && z.trustedContext.handoffPolicy.ownerAllowlist.includes(terminal.owner) && z.trustedContext.handoffPolicy.nextActionAllowlist.includes(terminal.nextAction) && terminal.evidenceRef === z.trustedContext.handoffPolicy.evidenceRef && terminal.stateRef === `state://${task.runId}/v-timeout-${lastAttempt}` && terminal.resumeToken === `resume://${task.runId}/timeout-${lastAttempt}` && canonical(terminal.attemptEvidence) === canonical(evidence); }; const recoveryCases = z.recoveryCases.map((fixture) => { const trace = structuredClone(traces[fixture.trace]); mutate(trace, fixture.mutation); for (const mutation of fixture.mutations || []) mutate(trace, mutation); return { case: fixture.case, result: traceResultsValid(trace, fixture.trace === "handoff" ? "handoff" : null) && recoverySemanticsValid(trace, fixture.trace) }; }); assert.deepEqual(recoveryCases, z.recoveryCases.map((x) => ({ case: x.case, result: x.expected }))); const security = z.securityCases.map((fixture) => ({ case: fixture.case, ...securityGate(fixture) })); assert.deepEqual(security, z.securityCases.map((x) => ({ case: x.case, ...x.expected }))); const nulRaw = z.securityCases.find((x) => x.case === "nul-path").candidate.args.path; assert.ok(nulRaw.includes("\0") && !nulRaw.includes("\\")); const envelopeCases = z.candidateEnvelopeCases.map((x) => ({ case: x.case, result: envelopeValid(x.candidate) })); assert.deepEqual(envelopeCases, z.candidateEnvelopeCases.map((x) => ({ case: x.case, result: x.expected }))); function applyDelta(budget, delta) { const next = { ...budget }; for (const [k, v] of Object.entries(delta)) next[k] += v; return next; } function budgetReplay(trace) { let version = 0, cursor = 0; let budget = structuredClone(trace.actions[0]?.budgetBefore || trace.budgetBefore); const reservationIds = new Set(), eventIds = new Set(); const projection = (x, phase) => phase === "decision" ? { turns: x.turns, wallClockSeconds: x.wallClockSeconds } : { toolCalls: x.toolCalls, checks: x.checks }; const validate = (reservation, event, phase, action) => { if (!reservation || !event || reservationIds.has(reservation.reservationId) || eventIds.has(event.eventId) || reservation.phase !== phase || event.phase !== phase || event.type !== "BUDGET_RESERVED" || event.eventId !== `budget://${reservation.reservationId}` || event.reservationId !== reservation.reservationId || reservation.status !== "COMMITTED" || reservation.expectedStateVersion !== version || event.expectedStateVersion !== version || event.stateVersion !== version + 1 || event.monotonicDeadline !== reservation.monotonicDeadline || canonical(event.delta) !== canonical(reservation.delta) || ![canonical(projection(budget, phase)), canonical(budget)].includes(canonical(reservation.before))) return false; reservationIds.add(reservation.reservationId); eventIds.add(event.eventId); const expectedDelta = phase === "decision" ? { turns: -1, wallClockSeconds: -10 } : { toolCalls: -1, checks: action?.tool === "run_check" ? -1 : 0 }; if (canonical(reservation.delta) !== canonical(expectedDelta)) return false; budget = applyDelta(budget, event.delta); if (Object.values(budget).some((x) => !Number.isInteger(x) || x < 0) || canonical(event.budgetAfter) !== canonical(budget) || ![canonical(projection(budget, phase)), canonical(budget)].includes(canonical(reservation.after)) || reservation.monotonicDeadline !== `mono:${budget.wallClockSeconds}`) return false; version += 1; return true; }; if (!trace.actions?.length) { return trace.budgetEvents.length === 1 && validate(trace.reservations?.decision, trace.budgetEvents[0], "decision", null) && canonical(budget) === canonical(trace.budgetAfter); } if (trace.budgetEvents.length !== trace.actions.length * 2) return false; for (const [i, action] of trace.actions.entries()) { if (canonical(action.budgetBefore) !== canonical(budget) || (i > 0 && canonical(trace.actions[i - 1].budgetAfter) !== canonical(action.budgetBefore))) return false; if (!validate(action.reservations?.decision, trace.budgetEvents[cursor++], "decision", action) || !validate(action.reservations?.act, trace.budgetEvents[cursor++], "act", action) || canonical(action.budgetAfter) !== canonical(budget)) return false; } return true; } const budgetReplayResults = ["success", "blocked", "timeoutRecovery", "handoff"].map((key) => ({ trace: key, result: budgetReplay(traces[key]) })); assert.ok(budgetReplayResults.every((x) => x.result)); const pending = state.control.pendingAction; const pendingAction = traces.success.actions.find((a) => a.callId === pending.callId); assert.ok(tupleMatches(pendingAction, pending) && pending.status === "PERSISTED_BEFORE_ACT" && pending.reconcileBeforeRetry === true && pending.resultRef === null); assert.deepEqual(state.control.resultLedger.entries, traces.success.resultLedger.slice(0, 3)); assert.match(state.control.restartPolicy.onRestart, /lookup-result-ledger.*before-retry/); const reconcilePending = (candidate, ledger) => { if (!plain(candidate) || !tupleMatches(pendingAction, candidate) || candidate.status !== "PERSISTED_BEFORE_ACT") return { transition: "BLOCKED_PENDING_TUPLE", retryAllowed: false, resultRef: null }; const expectedReservations = Object.values(pendingAction.reservations).filter(Boolean).map((r) => r.reservationId); if (candidate.resultRef !== null || canonical(candidate.reservationIds) !== canonical(expectedReservations)) return { transition: "HANDOFF_UNKNOWN_OUTCOME", retryAllowed: false, resultRef: null }; const conflict = ledger.some((e) => (e.callId === candidate.callId || (e.actionKey === candidate.actionKey && e.attempt === candidate.attempt)) && !tupleMatches(candidate, e)); if (conflict) return { transition: "HANDOFF_UNKNOWN_OUTCOME", retryAllowed: false, resultRef: null }; const result = ledger.find((e) => tupleMatches(candidate, e)); if (result) return result.status === "OBSERVED" && /^obs:\/\//.test(result.resultRef) && result.verified === true && /^validator:\/\//.test(result.verificationRef) ? { transition: "ABSORB_RESULT", retryAllowed: false, resultRef: result.resultRef } : { transition: "HANDOFF_UNKNOWN_OUTCOME", retryAllowed: false, resultRef: null }; const attestation = candidate.reconciliationAttestation, policy = z.trustedContext.reconciliationPolicy; const attested = plain(attestation) && tupleMatches(candidate, attestation) && attestation.issuer === policy.issuer && attestation.policyVersion === policy.policyVersion && attestation.outcome === policy.outcome && attestation.safeToRetry === policy.safeToRetry && attestation.evidenceRef === policy.evidenceRef; if (candidate.reconcileBeforeRetry !== true || !attested) return { transition: "HANDOFF_UNKNOWN_OUTCOME", retryAllowed: false, resultRef: null }; return { transition: "RETRY_ALLOWED", retryAllowed: true, resultRef: null }; }; const pendingReconcileCases = z.pendingReconcileCases.map((fixture) => { const candidate = structuredClone(pending); if (fixture.mutation) set(candidate, fixture.mutation.path, fixture.mutation.value); const ledger = structuredClone(state.control.resultLedger.entries); const matching = { callId: candidate.callId, actionKey: candidate.actionKey, argsHash: candidate.argsHash, attempt: candidate.attempt, resultRef: "obs://check/reconciled", status: "OBSERVED", verified: true, verificationRef: "validator://result/reconciled" }; if (fixture.ledgerMode === "matching-verified") ledger.push(matching); if (fixture.ledgerMode === "matching-pending") ledger.push({ ...matching, status: "PENDING" }); if (fixture.ledgerMode === "matching-forged-ref") ledger.push({ ...matching, resultRef: "model://forged" }); if (fixture.ledgerMode === "matching-unverified") ledger.push({ ...matching, verified: false }); if (fixture.ledgerMode === "matching-conflict") ledger.push({ ...matching, argsHash: "sha256:conflict" }); return { case: fixture.case, ...reconcilePending(candidate, ledger) }; }); assert.deepEqual(pendingReconcileCases, z.pendingReconcileCases.map((x) => ({ case: x.case, ...x.expected }))); const handoffResultEvents = traces.handoff.executionEvents.filter((e) => e.type === "TOOL_RESULT_RECORDED"); const handoffTerminalIndex = traces.handoff.executionEvents.findIndex((e) => e.type === "TERMINAL"); const handoffAudit = { retryCount: handoffResultEvents.length - new Set(handoffResultEvents.map((e) => e.actionKey)).size, postTerminalResults: traces.handoff.executionEvents.slice(handoffTerminalIndex + 1).filter((e) => e.type === "TOOL_RESULT_RECORDED").length }; assert.deepEqual(handoffAudit, { retryCount: 1, postTerminalResults: 0 }); const lesson = c.lessons.find((x) => x.id === "agent-08"); const registry = new Map(c.resources.map((x) => [x.id, x])); const sourceIds = [...new Set(n.sections.flatMap((s) => s.sourceIds))]; assert.ok(sourceIds.every((id) => lesson.resourceIds.includes(id) && registry.get(id)?.evidence)); console.log(JSON.stringify({ completionCases, recoveryCases, security, envelopeCases, budgetReplayResults, pendingReconcileCases, pendingLedgerBound: true, handoffAudit, nulCase: { actualNul: true, backslash: false, result: security.find((x) => x.case === "nul-path").result }, sourceTripleResolution: true }));'
```

本轮真实输出：`{"completionCases":[{"case":"valid-success","result":true},{"case":"wrong-run","result":false},{"case":"wrong-revision","result":false},{"case":"wrong-evidence-type","result":false},{"case":"invalid-args","result":false},{"case":"unsafe-read-path","result":false},{"case":"null-success-data","result":false},{"case":"wrong-result-path","result":false},{"case":"wrong-check-id","result":false},{"case":"stale-resource-version","result":false},{"case":"duplicate-action-key","result":false},{"case":"model-sandbox-attestation","result":false},{"case":"list-output-escape","result":false},{"case":"stale-scanner-policy","result":false},{"case":"data-hash-mismatch","result":false},{"case":"forged-budget-event-delta","result":false},{"case":"forged-reservation-delta","result":false},{"case":"broken-action-budget-chain","result":false},{"case":"decision-deducts-tool","result":false},{"case":"act-zero-delta","result":false},{"case":"non-boolean-success","result":false},{"case":"unhashed-error-payload","result":false},{"case":"model-generated-content","result":false},{"case":"scanner-secret-failure","result":false},{"case":"extra-claim-without-evidence","result":false},{"case":"removed-hard-constraint","result":false},{"case":"forged-terminal-validator-ref","result":false},{"case":"forged-budget-phase","result":false},{"case":"forged-budget-deadline","result":false},{"case":"incomplete-secret-scanner-set","result":false},{"case":"model-secret-attestation","result":false},{"case":"model-content-policy","result":false},{"case":"model-validator-attestation","result":false},{"case":"forged-report-hash","result":false},{"case":"reused-budget-identifiers","result":false}],"recoveryCases":[{"case":"valid-timeout-recovery","result":true},{"case":"valid-handoff","result":true},{"case":"wrong-call-id","result":false},{"case":"wrong-action-key","result":false},{"case":"wrong-args-hash","result":false},{"case":"wrong-run","result":false},{"case":"wrong-revision","result":false},{"case":"invalid-error-code","result":false},{"case":"forged-ledger-result-ref","result":false},{"case":"forged-event-action-key","result":false},{"case":"recovery-budget-event-delta","result":false},{"case":"recovery-reservation-delta","result":false},{"case":"recovery-action-budget-chain","result":false},{"case":"non-retryable-first-timeout","result":false},{"case":"timeout-wrong-evidence-type","result":false},{"case":"permission-denied-timeout-route","result":false},{"case":"recovery-error-payload-unhashed","result":false},{"case":"recovery-model-generated-content","result":false},{"case":"recovery-secret-scanner-failure","result":false},{"case":"forged-handoff-owner","result":false},{"case":"empty-attempt-evidence","result":false},{"case":"forged-attempt-evidence","result":false},{"case":"forged-handoff-state-ref","result":false},{"case":"forged-resume-token","result":false},{"case":"forged-next-action","result":false},{"case":"forged-terminal-event-owner","result":false},{"case":"forged-failure-resource-version","result":false},{"case":"forged-handoff-reason","result":false},{"case":"forged-terminal-event-reason","result":false}],"security":[{"case":"valid-read","result":"ALLOW_OBSERVATION_ONLY","evidenceRef":"resolve://security-valid-read"},{"case":"path-traversal","result":"DENY_PATH","evidenceRef":"policy://path/v3"},{"case":"absolute-path","result":"DENY_PATH","evidenceRef":"policy://path/v3"},{"case":"nul-path","result":"DENY_PATH","evidenceRef":"policy://path/v3"},{"case":"backslash-path","result":"DENY_PATH","evidenceRef":"policy://path/v3"},{"case":"encoded-separator","result":"DENY_PATH_ENCODING","evidenceRef":"policy://path/v3"},{"case":"double-encoded-separator","result":"DENY_PATH_ENCODING","evidenceRef":"policy://path/v3"},{"case":"broad-search-glob","result":"DENY_GLOB","evidenceRef":"policy://path/v3"},{"case":"config-search-glob","result":"DENY_GLOB","evidenceRef":"policy://path/v3"},{"case":"symlink-escape","result":"DENY_SYMLINK","evidenceRef":"oracle://symlink/docs-latest"},{"case":"symlink-descendant","result":"DENY_SYMLINK","evidenceRef":"oracle://symlink/docs-latest"},{"case":"unregistered-symlink-descendant","result":"DENY_RESOLUTION_EVIDENCE_MISSING","evidenceRef":"policy://path/v3"},{"case":"secret-path","result":"DENY_SENSITIVE","evidenceRef":"oracle://sensitive/credentials"},{"case":"candidate-fake-repo-handle","result":"DENY_REPO_HANDLE_OVERRIDE","evidenceRef":"auth://repo-selection/17"},{"case":"repo-prompt-injection","result":"DENY_AUTHORITY_ESCALATION","evidenceRef":"policy://content/v2"}],"envelopeCases":[{"case":"tool","result":true},{"case":"ask-user","result":true},{"case":"finish","result":true},{"case":"blocked","result":true},{"case":"mixed-envelope","result":false}],"budgetReplayResults":[{"trace":"success","result":true},{"trace":"blocked","result":true},{"trace":"timeoutRecovery","result":true},{"trace":"handoff","result":true}],"pendingReconcileCases":[{"case":"safe-retry-host-attested","transition":"RETRY_ALLOWED","retryAllowed":true,"resultRef":null},{"case":"absorb-verified-observed-result","transition":"ABSORB_RESULT","retryAllowed":false,"resultRef":"obs://check/reconciled"},{"case":"pending-ledger-handoff","transition":"HANDOFF_UNKNOWN_OUTCOME","retryAllowed":false,"resultRef":null},{"case":"forged-ledger-ref-handoff","transition":"HANDOFF_UNKNOWN_OUTCOME","retryAllowed":false,"resultRef":null},{"case":"unverified-ledger-handoff","transition":"HANDOFF_UNKNOWN_OUTCOME","retryAllowed":false,"resultRef":null},{"case":"missing-reconcile-attestation-handoff","transition":"HANDOFF_UNKNOWN_OUTCOME","retryAllowed":false,"resultRef":null},{"case":"model-reconcile-attestation-handoff","transition":"HANDOFF_UNKNOWN_OUTCOME","retryAllowed":false,"resultRef":null},{"case":"reject-forged-pending-hash","transition":"BLOCKED_PENDING_TUPLE","retryAllowed":false,"resultRef":null},{"case":"pending-result-ref-handoff","transition":"HANDOFF_UNKNOWN_OUTCOME","retryAllowed":false,"resultRef":null},{"case":"forged-reservation-ids-handoff","transition":"HANDOFF_UNKNOWN_OUTCOME","retryAllowed":false,"resultRef":null},{"case":"conflicting-ledger-tuple-handoff","transition":"HANDOFF_UNKNOWN_OUTCOME","retryAllowed":false,"resultRef":null}],"pendingLedgerBound":true,"handoffAudit":{"retryCount":1,"postTerminalResults":0},"nulCase":{"actualNul":true,"backslash":false,"result":"DENY_PATH"},"sourceTripleResolution":true}`。
