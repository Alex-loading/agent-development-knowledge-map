# LLM 基础视觉主张、来源与许可冻结清单

- 冻结日期：2026-07-26
- 范围：`llm-foundation` 八课知识笔记
- 清单规模：40 项，每课 5 项
- 冻结原则：所有图均采用 `original-synthesis`，不复制、裁剪、翻译或近似临摹第三方图片
- 证据边界：`sourceIds` 只表示对应 section 已核验的事实来源；课程字段只用于 assessed coverage，不冒充外部证据

## 字段与判定口径

- `lesson / section` 中的 section 均为 `src/data/llm-foundation-notes/llm-01.js` 至 `llm-08.js` 的真实 ID。
- `assessed coverage` 精确指向 `src/data/llm-foundation.js` 中的学习目标、quiz ID、练习步骤、完成标准或面试题 ID。
- `candidate image URL` 为“无”表示没有选择第三方图片候选，并非缺失信息。
- `permission evidence` 为“不适用”表示站内从零制作原创教学图；`sourceIds` 的页面、仓库许可证或论文发布状态均不被推断为图片再分发许可。
- `verified` 表示 section、考核映射、来源 ID、原创决策和 storyboard 均已冻结；`blocked` 只用于证据或许可不足且无法安全进入制作的条目。
- `visual form` 固定使用 `图形形式；primary=<role>；tags=<tag,...>`。`primary` 是未来 registry 单值 `role` 的唯一来源，允许 `overview`、`mechanism`、`process`、`comparison`、`boundary`、`decision`；`tags` 只表达次级覆盖，允许 `mechanism`、`process`、`comparison`、`boundary`、`decision`、`relationship`、`failure-mode`、`tradeoff`，不得与 primary 重复。
- 每个 storyboard 必须包含固定 `Fixture` 行。`fixture=quantitative` 必须以 `fixtureId` 引用 `tests/fixtures/llm-foundation-visual-fixtures.js`：结构化 `data` 与 `result` 是唯一数值真源，`Expected` 只能由模块 formatter 从 `result` 确定性生成，不能手写数值副本；测试必须从原始输入执行 `Method`，不能依赖预切分、预排序或预过滤结果。`fixture=qualitative` 必须写明不编码数值主张的具体 scope。测试从本清单自身派生分类，fixture 是可执行教学输入，不得冒充真实模型、生产 tokenizer、供应商延迟或通用发布阈值。

## 冻结清单

### llm-01 AI、机器学习与 LLM

| visualId | lesson / section | cognitive question | visual form | assessed coverage | sourceIds | candidate image URL | permission evidence | decision | storyboard | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `visual-llm-01-field-map` | `llm-01 / map-the-field` | AI、机器学习、深度学习、生成式 AI 与 LLM 是一条包含链，还是两条相交分类轴？ | 双轴集合关系图；primary=overview；tags=relationship,boundary | `llm-01.objectives[0]`；`quiz-llm-01-1`；`iq-llm-01-1`；`llm-01.completionCriteria[0]` | `res-ms-ai`, `res-ms-genai`, `res-hf-llm` | 无（不选择第三方图） | 不适用：站内从零绘制；来源只支撑分类事实，不复制第三方图像、构图或配色。 | original-synthesis | **Reading order:** 左上总目标 AI → 下方方法轴 → 右侧生成能力轴 → 交叉处 LLM → 两个反例。<br>**Nodes/regions:** AI 外框、机器学习、深度学习、生成式 AI、LLM、搜索/规划、视觉/语音。<br>**Edges or comparison axes:** 实线嵌套表示方法包含，横向能力轴表示产出类型，交叉标记表示 LLM 同时落在两类语境中。<br>**Color-independent encoding:** 嵌套框、交叉阴影、不同线型及文字图例共同编码。<br>**Fixture:** fixture=qualitative；scope=双轴集合关系图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** LLM 通常是深度学习实现的语言生成模型，但 AI、深度学习和生成式 AI 不能压成一条同义链。<br>**Alt summary:** 双轴图显示方法包含关系与生成能力分类在 LLM 处交叉，并列出非学习 AI 和非语言深度学习反例。<br>**Long-description outline:** 先定义两条轴，再逐层说明集合关系，最后用两个反例解释为何简单箭头链会误导。 | verified |
| `visual-llm-01-learning-loop` | `llm-01 / minimal-learning-loop` | 一批数据怎样经过预测、损失、梯度和优化真正改变参数，标准推理又为何不会执行这条更新回路？ | 闭环流程图；primary=mechanism；tags=process | `llm-01.objectives[1]`；`iq-llm-01-2` | `res-ms-ai`, `res-hf-llm` | 无（不选择第三方图） | 不适用：原创流程编码；来源仅约束训练闭环与泛化边界。 | original-synthesis | **Reading order:** 数据批次 → 当前参数 → 预测 → 损失 → 梯度 → 优化器 → 新参数 → 下一批；最后对照只执行前向的标准推理旁路。<br>**Nodes/regions:** 训练数据区、模型状态区、误差信号区、更新动作区、验证旁路、只读推理旁路。<br>**Edges or comparison axes:** 主路径单向箭头，参数更新回边形成循环；验证与推理均为不回写参数的虚线旁路。<br>**Color-independent encoding:** 圆角矩形表示状态，菱形表示度量，粗实线表示更新，虚线表示只读检查或使用。<br>**Fixture:** fixture=qualitative；scope=闭环流程图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 训练只有在优化器执行后才改变参数，标准推理只使用已部署参数完成前向计算。<br>**Alt summary:** 训练闭环从批次预测到损失、梯度和优化器更新，旁边对比不含反向与更新的只读推理路径。<br>**Long-description outline:** 依次解释一轮训练的七个节点、损失与更新的职责差异，再以只读推理旁路完成训练/推理判断。 | verified |
| `visual-llm-01-autoregressive-generation` | `llm-01 / from-generation-to-llm` | 在不更新参数的推理阶段，文本如何变成 token，并在每一步只生成一个新的 token？ | 分步序列图；primary=process；tags=mechanism | `llm-01.objectives[1]`；`iq-llm-01-2` | `res-ms-genai`, `res-hf-llm`, `res-ms-ai` | 无（不选择第三方图） | 不适用：原创序列与状态示意；不复刻来源中的 tokenizer 或生成动画。 | original-synthesis | **Reading order:** 已部署只读参数 → 原始文本 → tokenizer → token ID 序列 → 当前上下文 → 概率候选 → 追加 token → 停止判断。<br>**Nodes/regions:** 只读模型状态区、编码区、模型可见上下文区、候选分布区、循环追加区。<br>**Edges or comparison axes:** 实线展示数据流，回箭头展示自回归依赖，停止条件为终止分支；参数区没有来自输出的写回边。<br>**Color-independent encoding:** token 用编号胶囊，概率用排序条，新增 token 以双边框和“新”标签标识，只读参数带锁图标。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-01-autoregressive-generation；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 标准推理使用已部署参数按可见 token 逐步追加下一个 token，不会把本次文本写回模型参数。<br>**Alt summary:** 已部署参数保持只读，文本被编码为 token ID，模型反复产生下一 token 分布、选取并追加，直到停止。<br>**Long-description outline:** 先说明推理参数不更新，再区分 token 与自然词，走完一次生成步，最后描述循环依赖与停止边界。 | verified |
| `visual-llm-01-training-inference-boundary` | `llm-01 / training-versus-inference` | prompt、RAG、预训练与微调分别会不会改变模型参数？ | 泳道对比图；primary=comparison；tags=boundary | `llm-01.objectives[1]`；`iq-llm-01-2` | `res-ms-genai`, `res-hf-llm`, `res-ms-ai` | 无（不选择第三方图） | 不适用：原创职责边界图；不把来源页面当成媒体许可。 | original-synthesis | **Reading order:** 上方训练泳道 → 下方推理泳道 → 中央参数边界 → RAG 旁路 → 结论框。<br>**Nodes/regions:** 预训练、微调、优化器、部署参数、prompt、检索文档、prefill、decode。<br>**Edges or comparison axes:** 纵轴比较“更新参数/使用参数”，跨边界只有部署工件；RAG 只连到上下文。<br>**Color-independent encoding:** 训练用齿轮节点，推理用播放节点，参数变更用粗箭头，数据可见性用虚线。<br>**Fixture:** fixture=qualitative；scope=泳道对比图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 预训练和微调通过优化更新参数，prompt 与 RAG 只改变当前推理可见信息。<br>**Alt summary:** 两条泳道对比训练和推理，突出只有训练优化器写入参数，RAG 文档只进入本次上下文。<br>**Long-description outline:** 分别走完训练与推理路径，再定位 RAG，最后说明把文档放进 prompt 为什么不等于训练。 | verified |
| `visual-llm-01-application-decision-stack` | `llm-01 / model-and-application-boundary` | Agent 回答失败时，应该先修应用链的哪一层，何时才考虑模型改动？ | 分层诊断与决策图；primary=decision；tags=failure-mode | `llm-01.objectives[1]`；`quiz-llm-01-2`；`iq-llm-01-3`；`llm-01.exercise.steps[1]` | `res-ms-genai`, `res-hf-llm`, `res-openai-agents` | 无（不选择第三方图） | 不适用：原创诊断栈；来源用于约束模型、服务与应用职责。 | original-synthesis | **Reading order:** 用户症状 → 应用层 → 模型服务层 → 模型层 → 评测证据门 → 修复选择。<br>**Nodes/regions:** 输入/检索、prompt、工具、Schema 校验、服务运行、基础模型、可复现样例。<br>**Edges or comparison axes:** 故障沿调用链向下定位，修复沿最小成本路径返回；“换模型/微调”位于最后条件分支。<br>**Color-independent encoding:** 层级用编号卡片，常见失败用警告三角，证据门用带锁菱形。<br>**Fixture:** fixture=qualitative；scope=分层诊断与决策图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 先用可复现评测定位检索、提示、工具或校验失败，只有瓶颈确属模型能力时才改模型。<br>**Alt summary:** 分层诊断图把 Agent 失败拆为应用、服务和模型层，并把换模型或微调放在证据门之后。<br>**Long-description outline:** 从症状列出每层可观察信号和低成本修复，最后说明进入模型层改动所需的评测证据。 | verified |

### llm-02 神经网络与反向传播

| visualId | lesson / section | cognitive question | visual form | assessed coverage | sourceIds | candidate image URL | permission evidence | decision | storyboard | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `visual-llm-02-training-cycle` | `llm-02 / training-loop-and-tensor-shapes` | 一次训练迭代有哪些阶段，张量形状在哪些边界必须兼容？ | 带形状标注的循环图；primary=overview；tags=process | `llm-02.objectives[0]`；`llm-02.completionCriteria[0]`；`iq-llm-02-1` | `res-d2l-zh`, `res-karpathy`, `res-fastai` | 无（不选择第三方图） | 不适用：原创训练循环；示例形状由课程语义构造，不复制教材图。 | original-synthesis | **Reading order:** 批次 `[B,F]` → 线性层 → 预测 `[B,C]` → 标签对齐 → 标量损失 → backward → optimizer → 下一批。<br>**Nodes/regions:** 数据形状区、前向 Z 与 MSE 区、backward 的 dZ/dW/db 区、SGD 更新后的 W/b 与复算 loss 区。<br>**Edges or comparison axes:** 主环表示训练顺序，形状不兼容处用阻断边，广播风险为旁支。<br>**Color-independent encoding:** 张量用方格和维度标签，运算用圆形节点，更新用粗回箭头。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-02-training-cycle；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 形状语义正确是可解释前向和可信梯度的前提。<br>**Alt summary:** 训练循环图标出批次、特征、类别形状，并串联前向、损失、反向和更新。<br>**Long-description outline:** 按循环逐步说明张量形状、标签对齐、损失归约、梯度产生与参数更新。 | verified |
| `visual-llm-02-neuron-forward` | `llm-02 / linear-layers-and-activation` | 单个神经元如何把输入变成输出，为什么堆叠线性层仍需要非线性？ | 单元计算与折叠对比图；primary=mechanism；tags=comparison | `llm-02.objectives[1]`；`quiz-llm-02-2`；`iq-llm-02-2` | `res-google-ml`, `res-d2l-zh`, `res-fastai` | 无（不选择第三方图） | 不适用：原创计算节点与边界对比。 | original-synthesis | **Reading order:** 输入特征 → 权重乘法 → 求和加偏置 → 激活 → 输出；随后比较“无激活两层”和“有激活两层”。<br>**Nodes/regions:** 输入、参数、仿射结果、激活、输出、两种网络路径。<br>**Edges or comparison axes:** 左侧是加权汇聚边，右侧横轴比较可折叠与非线性决策边界。<br>**Color-independent encoding:** 权重在线上直接标数，仿射/激活用不同形状，决策边界用直线/曲线。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-02-neuron-forward；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 多层仿射变换仍可折叠，非线性激活才扩大可表达关系。<br>**Alt summary:** 单神经元前向计算旁边对比无激活网络的直线边界和有激活网络的曲线边界。<br>**Long-description outline:** 先逐项解释前向公式，再演示两层仿射合并，最后说明激活改变表达能力但不保证易训练。 | verified |
| `visual-llm-02-backprop-graph` | `llm-02 / computation-graph-chain-rule-backprop` | 最终损失如何沿多条计算路径把梯度分配给早期参数？ | 计算图与反向梯度叠层图；primary=mechanism；tags=process | `llm-02.objectives[0]`；`quiz-llm-02-1`；`llm-02.exercise.steps[1]` | `res-d2l-zh`, `res-karpathy`, `res-3b1b-nn` | 无（不选择第三方图） | 不适用：原创教学计算图；不复制 micrograd 或课程画面。 | original-synthesis | **Reading order:** 正向从输入和参数到中间值再到损失；反向从损失按逆拓扑顺序返回。<br>**Nodes/regions:** 共享 x 分叉到 a=xw 与 b=x²、在 c=a+b 汇合、L=c²/2、每条局部导数标签与 dL/dx 累加点。<br>**Edges or comparison axes:** 实线向右表示值依赖，反向虚线表示梯度传播，多路径在汇合点相加。<br>**Color-independent encoding:** 正向箭头为实线三角头，反向箭头为双线空心头，梯度标签统一写 `∂L/∂x`。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-02-backprop-graph；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 反向传播组合局部导数并累加多路径贡献，直接产出梯度而非新参数。<br>**Alt summary:** 计算图先正向得到损失，再逆序传播梯度，多条依赖路径的贡献在共享变量处相加。<br>**Long-description outline:** 说明节点与依赖，沿一条路径应用链式法则，再说明共享变量的梯度累加和优化器的后续职责。 | verified |
| `visual-llm-02-learning-rate-trajectories` | `llm-02 / optimizer-learning-rate-and-zero-grad` | 学习率过小、合适、过大时，参数轨迹和损失会怎样不同？ | 三轨迹地形图；primary=comparison；tags=failure-mode | `llm-02.objectives[1]`；`llm-02.exercise.steps[2]`；`iq-llm-02-1` | `res-d2l-zh`, `res-google-ml`, `res-fastai`, `res-karpathy` | 无（不选择第三方图） | 不适用：原创抽象损失地形，不复用来源具体曲线。 | original-synthesis | **Reading order:** 先读同一损失剖面，再依次跟踪小步、适中步和过大步三条轨迹，最后看观测信号。<br>**Nodes/regions:** 起点、局部低谷、三种步长、损失时间序列小窗、梯度清理提示。<br>**Edges or comparison axes:** 横轴参数位置、纵轴损失；轨迹按步编号，过大路径跨谷震荡。<br>**Color-independent encoding:** 三条轨迹分别用点线/实线/双线并配“小/中/大”标签。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-02-learning-rate-trajectories；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 学习率控制局部更新步长，过小拖慢收敛，过大可能越过低谷并震荡。<br>**Alt summary:** 同一损失地形上三条编号轨迹展示过小、适中和过大学习率的缓慢、收敛与震荡结果。<br>**Long-description outline:** 定义负梯度更新，比较三种步长的路径和损失信号，最后区分数学梯度与框架梯度累积。 | verified |
| `visual-llm-02-generalization-curves` | `llm-02 / generalization-and-training-diagnosis` | 训练损失下降时，怎样区分优化不足、正常泛化与过拟合？ | 训练/验证曲线小倍图；primary=boundary；tags=decision | `llm-02.objectives[1]`；`iq-llm-02-3` | `res-google-ml`, `res-d2l-zh`, `res-karpathy` | 无（不选择第三方图） | 不适用：原创示意曲线；不冒充任何来源报告的实验结果。 | original-synthesis | **Reading order:** 三个小图从“都高”到“共同下降”再到“训练降、验证升”，随后进入发布证据链。<br>**Nodes/regions:** 欠拟合、训练/验证同步改善、过拟合三组独立 epoch/train/validation 数组，另含转折点、数据泄漏警示、业务切片与部署监控。<br>**Edges or comparison axes:** 每图横轴训练步、纵轴损失；下方箭头连接诊断到下一项检查。<br>**Color-independent encoding:** 训练为实线圆点，验证为虚线方点，转折点用垂直标线。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-02-generalization-curves；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 训练损失只是优化信号，独立验证、关键切片和上线监控才构成发布证据。<br>**Alt summary:** 三组训练与验证曲线分别表示欠拟合、同步改善和过拟合，并连接到发布检查项。<br>**Long-description outline:** 逐图解释曲线组合，指出数据泄漏替代解释，再列出从验证到业务切片和部署监控的判断链。 | verified |

### llm-03 Token、Embedding 与上下文

| visualId | lesson / section | cognitive question | visual form | assessed coverage | sourceIds | candidate image URL | permission evidence | decision | storyboard | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `visual-llm-03-text-to-context` | `llm-03 / token-and-tokenizer` | 文本怎样经 token、ID、embedding 和位置表示成为模型上下文？ | 表示转换管线；primary=overview；tags=process | `llm-03.objectives[0]`；`iq-llm-03-1` | `res-tiktoken`, `res-hf-llm`, `res-rasbt`, `res-openai-cookbook` | 无（不选择第三方图） | 不适用：原创表示管线；不复制 tokenizer 工具截图。 | original-synthesis | **Reading order:** 文本 → token 边界 → ID → embedding 查表 → 位置组合 → 上下文化表示。<br>**Nodes/regions:** 人类文本区、离散编码区、连续向量区、顺序信息区、模型层区。<br>**Edges or comparison axes:** 数据流实线前进；“ID 数值无距离语义”和“上下文化后会变化”以两条边界注记指出。<br>**Color-independent encoding:** 文本、ID、向量分别用引号卡、整数胶囊、坐标网格；阶段编号 1–5。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-03-text-to-context；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** token ID 只是离散索引，查表并加入位置信息后才成为可供模型更新的表示。<br>**Alt summary:** 管线从文本切分到 ID、embedding、位置表示和上下文化表示，并区分离散索引与连续向量。<br>**Long-description outline:** 依次定义各阶段输入输出，再解释 ID 与向量语义边界，最后说明上下文层会继续更新表示。 | verified |
| `visual-llm-03-tokenization-comparison` | `llm-03 / token-and-tokenizer` | 中英文、JSON 与代码为何不能用字符数或单词数固定换算 token？ | 对齐分词带对比图；primary=comparison；tags=boundary | `quiz-llm-03-1`；`llm-03.exercise.steps[0]` | `res-tiktoken`, `res-hf-llm`, `res-rasbt`, `res-openai-cookbook` | 无（不选择第三方图） | 不适用：示例必须在制作时用目标 tokenizer 实测；不使用外部截图。 | original-synthesis | **Reading order:** 同义中英文 → JSON → 代码 → 两种 tokenizer 的切分带 → 实测结论。<br>**Nodes/regions:** 原文行、token 边界行、token 计数、模型/tokenizer 版本标签。<br>**Edges or comparison axes:** 横向比较文本类型，纵向比较 tokenizer A/B；边界线对齐原字符。<br>**Color-independent encoding:** token 用交替边框样式和编号，不依赖颜色；计数以数字和刻度同时显示。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-03-tokenization-comparison；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** token 边界由具体 tokenizer 与原文共同决定，预算必须对目标模型实测。<br>**Alt summary:** 多种文本在两个 tokenizer 下得到不同边界和计数，说明字符或词无法固定换算 token。<br>**Long-description outline:** 逐组读出原文、边界和数量差异，解释空格/符号等因素，最后给出版本化实测要求。 | verified |
| `visual-llm-03-embedding-position-space` | `llm-03 / ids-to-embeddings` | embedding 的几何关系从哪里来，位置表示又补充了什么？ | 双面板向量关系图；primary=mechanism；tags=relationship | `llm-03.objectives[0]`；`iq-llm-03-2` | `res-hf-llm`, `res-rasbt`, `res-3b1b-transformer`, `res-ms-genai` | 无（不选择第三方图） | 不适用：原创抽象坐标；示意点不声称是实测 embedding。 | original-synthesis | **Reading order:** 左侧 ID 查表 → 中间向量邻域示意 → 右侧同 token 不同位置 → 层后上下文化表示。<br>**Nodes/regions:** 离散 ID、embedding 表、向量点、位置向量、上下文化输出。<br>**Edges or comparison axes:** 查表箭头连接 ID 到行；相似关系用邻近和标注边；位置差异用同形点的位移箭头。<br>**Color-independent encoding:** 类别用圆/方/三角与文字，位置用序号和方向纹理；坐标轴标“教学投影”。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-03-embedding-position-space；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** embedding 的可用关系由训练目标形成，位置与上下文会让同一 token 的表示继续分化。<br>**Alt summary:** ID 查表得到初始向量，位置编码区分顺序，后续层把相同 token 更新成不同上下文表示。<br>**Long-description outline:** 区分 ID 与向量，解释教学投影不是字典释义，再描述位置组合和上下文化变化。 | verified |
| `visual-llm-03-context-budget` | `llm-03 / context-and-shared-budget` | 系统指令、历史、检索、用户输入和输出如何共享同一个 token 预算？ | 堆叠预算条与溢出边界图；primary=mechanism；tags=boundary | `llm-03.objectives[1]`；`llm-03.exercise.steps[1]`；`llm-03.completionCriteria[1]` | `res-openai-cookbook`, `res-hf-llm`, `res-ms-genai` | 无（不选择第三方图） | 不适用：原创预算编码；所有数字为明确标注的教学示例。 | original-synthesis | **Reading order:** 总窗口上限 → 已占用五类输入 → 预留输出 → 安全余量 → 超限与截断分支。<br>**Nodes/regions:** 系统、历史、检索、用户、工具结果、输出预留、余量。<br>**Edges or comparison axes:** 横轴为 token 数；预算块相加，超限箭头指向缩减/拒绝策略。<br>**Color-independent encoding:** 各块使用纹理、缩写和边界刻度；预留区以空心边框显示。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-03-context-budget；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 上下文是输入与待生成输出共享的预算，先预留输出再决定材料取舍。<br>**Alt summary:** 一个总 token 条分配给多类输入和输出预留，超出安全线后进入缩减或拒绝分支。<br>**Long-description outline:** 说明各预算组成、求和关系、输出预留理由，以及超限和虽未超限但噪声过多的两种风险。 | verified |
| `visual-llm-03-context-strategy-matrix` | `llm-03 / context-strategy-tradeoffs` | 超长材料应截断、分块、摘要还是检索，各自会丢失什么？ | 决策矩阵与信息损失路径；primary=decision；tags=failure-mode,tradeoff | `llm-03.objectives[1]`；`quiz-llm-03-2`；`iq-llm-03-3`；`llm-03.exercise.steps[2]` | `res-openai-cookbook`, `res-ms-genai`, `res-llm-universe`, `res-hf-llm` | 无（不选择第三方图） | 不适用：原创决策矩阵；来源事实重新组织且不临摹原视觉表达。 | original-synthesis | **Reading order:** 先读任务轴（局部查询/全局连续）与材料轴（短/长且更新） → 四种策略格 → 每格失败模式 → 评测回路。<br>**Nodes/regions:** 截断、分块、摘要、检索、信息丢失、冲突、召回失败、对照评测。<br>**Edges or comparison axes:** 横轴信息需全局连贯程度，纵轴材料规模与更新频率；每格箭头连接相应风险。<br>**Color-independent encoding:** 四策略用不同图标与线框，风险用编号警告而非单色。<br>**Fixture:** fixture=qualitative；scope=决策矩阵与信息损失路径只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 上下文策略是在信息保真、成本、延迟和更新性之间取舍，必须用任务评测选择。<br>**Alt summary:** 二维矩阵把截断、分块、摘要和检索放入不同任务条件，并列出各自的信息损失。<br>**Long-description outline:** 定义两条决策轴，逐格说明适用条件和失败模式，最后描述如何用基线对照验证。 | verified |

### llm-04 Attention 与 Transformer

| visualId | lesson / section | cognitive question | visual form | assessed coverage | sourceIds | candidate image URL | permission evidence | decision | storyboard | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `visual-llm-04-decoder-block` | `llm-04 / decoder-block-information-flow` | 一个 GPT-like decoder-only / Pre-Norm 示例怎样更新 token 表示，它与 Post-Norm 及原始 encoder-decoder Transformer 有何边界？ | Transformer 块信息流图；primary=overview；tags=process,boundary | `llm-04.objectives[1]`；`iq-llm-04-3`；`llm-04.completionCriteria[1]` | `res-attention-paper`, `res-rasbt`, `res-limu-transformer`, `res-happy-llm` | 无（不选择第三方图） | 不适用：从零绘制，不复制论文 Figure 1 或教材架构图；来源只支撑模块关系。 | original-synthesis | **Reading order:** 主面板先读 GPT-like decoder-only / Pre-Norm 示例：输入 → LN → masked multi-head self-attention → 残差相加 → LN → FFN → 残差相加；再读 Post-Norm 对照：子层 → 残差相加 → LN；最后读原始论文语境卡。<br>**Nodes/regions:** Pre-Norm decoder-only 主路径、两条残差旁路、Post-Norm 顺序对照、原始 encoder-decoder 区、decoder cross-attention、层堆叠出口。<br>**Edges or comparison axes:** 主数据流纵向并以虚线标梯度回路；对照轴明确“LN 在子层前/后”；原始论文区分 encoder 全可见 self-attention、decoder 因果 self-attention 与读取 encoder 输出的 cross-attention。<br>**Color-independent encoding:** Pre-Norm 用编号实线卡，Post-Norm 用字母虚线卡，cross-attention 使用双输入节点；每条路径都直接写架构名称与归一化位置。<br>**Fixture:** fixture=qualitative；scope=Transformer 块信息流图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 本图主路径只是 GPT-like decoder-only / Pre-Norm 示例；原始论文是 encoder-decoder / Post-Norm 且 decoder 含 cross-attention，两者都不是所有 Transformer 的无条件标准。<br>**Alt summary:** 主图展示 GPT-like decoder-only 的 Pre-Norm 因果块，旁边对比 Post-Norm 顺序，并说明原始 encoder-decoder 论文的 decoder 还有 cross-attention。<br>**Long-description outline:** 先沿 Pre-Norm 主路径说明信息和梯度，再逐节点对照 Post-Norm，最后恢复原始论文的 encoder、decoder、cross-attention 与 Post-Norm 语境，并声明具体模型必须查其架构资料。 | verified |
| `visual-llm-04-qkv-flow` | `llm-04 / qkv-query-match-aggregate` | Q、K、V 分别承担什么职责，最终被汇总的到底是什么？ | 查询—匹配—聚合三步图；primary=mechanism；tags=process | `llm-04.objectives[0]`；`quiz-llm-04-1`；`iq-llm-04-1`；`llm-04.completionCriteria[0]` | `res-3b1b-attention`, `res-attention-paper`, `res-rasbt` | 无（不选择第三方图） | 不适用：原创三步机制图；不复制论文公式排版或视频画面。 | original-synthesis | **Reading order:** 当前位置表示 → Q；所有可见位置 → K/V → Q·K 匹配 → 权重 → 对 V 加权求和 → 更新表示。<br>**Nodes/regions:** Query 区、Key 候选区、匹配分数区、Value 信息区、聚合输出区。<br>**Edges or comparison axes:** Q 只连 K 产生权重；权重再跨到对应 V；V 的粗细箭头汇聚到输出。<br>**Color-independent encoding:** Q/K/V 用问号、钥匙、信息卡图标及字母标签；权重同时以线宽和数值表示。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-04-qkv-flow；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** Q 与 K 决定读取权重，真正按权重汇总并写回当前位置的是 Value。<br>**Alt summary:** Q 与各位置 K 匹配形成权重，权重对应到 V 并加权求和生成当前位置的新信息。<br>**Long-description outline:** 解释三种投影的来源与非固定语义，走完匹配和 softmax 前后的职责，最后强调 V 才是汇总对象。 | verified |
| `visual-llm-04-score-mask-softmax` | `llm-04 / scaled-dot-softmax` | 匹配分数怎样经缩放、因果掩码和 softmax 变成合法读取权重？ | 逐步矩阵变换图；primary=mechanism；tags=process | `quiz-llm-04-2`；`llm-04.exercise.steps[2]` | `res-attention-paper`, `res-rasbt`, `res-3b1b-attention` | 无（不选择第三方图） | 不适用：原创数值小例子与矩阵布局；不复制论文图表。 | original-synthesis | **Reading order:** 原始 QK 分数矩阵 → 按维度缩放 → 上三角未来位置屏蔽 → 逐行 softmax → 行和为 1 的权重矩阵。<br>**Nodes/regions:** 原始 QK 分数、d_k 与 sqrt(d_k)、缩放分数、mask 后分数、softmax 权重、V 加权输出与归一化检查。<br>**Edges or comparison axes:** 阶段箭头从左到右；矩阵行表示查询位置，列表示被读取位置。<br>**Color-independent encoding:** 被屏蔽格用交叉阴影和 `−∞`，可见格写数值，当前行用粗边框。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-04-score-mask-softmax；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 因果掩码在 softmax 前排除未来位置，softmax 才把剩余分数归一成读取权重。<br>**Alt summary:** 四个矩阵依次展示缩放、未来位置屏蔽和逐行 softmax，最终每行可见权重之和为一。<br>**Long-description outline:** 定义矩阵轴，跟踪一行数值变化，说明掩码时机与未来信息不可见的训练意义。 | verified |
| `visual-llm-04-multi-head-merge` | `llm-04 / multi-head-attention` | 多头注意力比单头多了哪些投影、并行关系和合并步骤？ | 并行分支—合并图；primary=mechanism；tags=relationship | `llm-04.objectives[1]`；`iq-llm-04-2` | `res-attention-paper`, `res-rasbt`, `res-happy-llm` | 无（不选择第三方图） | 不适用：原创并行关系图；不声称各头具有预设语法职责。 | original-synthesis | **Reading order:** 同一输入 → 两组独立 Q/K/V 投影 → 每头 Q/K 匹配得 weights → weights 对 Values 加权求和得 Hn → 拼接 → 输出投影 → 联合更新。<br>**Nodes/regions:** 输入、Head 1/2、每头 Query、Keys、scores、weights、Values、ΣV、H1/H2、Concat、W_O 输出投影、联合更新端点。<br>**Edges or comparison axes:** 每头 Q 与 K 共同连向 weights，weights 与 Values 共同连向 ΣV，H1/H2 汇合到 concat，输出投影再连向联合更新；分叉边表示独立参数。<br>**Color-independent encoding:** 两个头用不同边框纹理和编号，每个权重同时写出数值，汇合节点用双槽容器显示拼接。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-04-multi-head-merge；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 多头各自完成匹配与 Value 聚合，再经拼接和输出投影形成联合更新；每个头的职责不是人工预先分配。<br>**Alt summary:** 两个独立注意力头分别从 scores 算出权重、聚合 Values 为 H1/H2，再拼接并投影成联合更新。<br>**Long-description outline:** 用 fixture 的每头 scores、weights、Values 与 Hn 走完可复算机制，再说明 concat、W_O 和联合更新端点，以及两个头不等于固定语义标签。 | verified |
| `visual-llm-04-causal-visibility` | `llm-04 / causal-mask` | 训练时每个位置能看见哪些 token，错误地开放未来连接会造成什么失败？ | 三角可见性矩阵与泄漏对比；primary=boundary；tags=failure-mode | `quiz-llm-04-2`；`llm-04.exercise.steps[2]` | `res-attention-paper`, `res-rasbt`, `res-happy-llm` | 无（不选择第三方图） | 不适用：原创可见性矩阵；不使用论文原图。 | original-synthesis | **Reading order:** token 序列 → 正确下三角可见矩阵 → 某查询位置的允许边 → 错误全可见矩阵 → “偷看答案”后果。<br>**Nodes/regions:** 查询行、键列、过去/当前、未来、训练目标，以及 fixture 中 4×4 嵌套 0/1 可见矩阵。<br>**Edges or comparison axes:** 矩阵轴均为位置；右侧对比允许连接与泄漏连接。<br>**Color-independent encoding:** 允许格用勾和实线边，屏蔽格用叉和斜纹，泄漏格用警告三角。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-04-causal-visibility；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** decoder-only 训练必须屏蔽未来 token，才能让每个位置的可见信息与真实生成一致。<br>**Alt summary:** 下三角矩阵只允许位置读取自身和过去，对比全可见矩阵暴露未来答案的泄漏。<br>**Long-description outline:** 逐行解释可见范围，跟踪一个位置的允许连接，再说明开放未来为何让下一 token 训练失真。 | verified |

### llm-05 预训练、微调与对齐

| visualId | lesson / section | cognitive question | visual form | assessed coverage | sourceIds | candidate image URL | permission evidence | decision | storyboard | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `visual-llm-05-method-map` | `llm-05 / objectives-before-methods` | 面对能力、行为、知识更新或证据引用问题，应先选择训练阶段还是应用方法？ | 目标—方法路线图；primary=overview；tags=decision | `llm-05.objectives[0]`；`llm-05.objectives[1]`；`llm-05.completionCriteria[0]` | `res-hf-llm`, `res-ms-genai`, `res-openai-cookbook` | 无（不选择第三方图） | 不适用：原创路线图；来源只支撑方法目标和工程边界。 | original-synthesis | **Reading order:** 业务失败 → 能力/行为/知识/证据四类目标 → 预训练/SFT/偏好/LoRA/RAG 候选 → 评测门。<br>**Nodes/regions:** 问题分类、参数更新方法、推理时方法、数据与成本、发布评测。<br>**Edges or comparison axes:** 主轴按“改变参数/改变上下文”分流；每条候选边都先经过目标匹配再到评测。<br>**Color-independent encoding:** 参数方法用齿轮框，应用方法用文档框，决策点用编号菱形。<br>**Fixture:** fixture=qualitative；scope=目标—方法路线图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 先定义要改变的是基础能力、稳定行为还是运行时知识，再选择训练或 RAG。<br>**Alt summary:** 路线图从四类业务目标分流到预训练、后训练、LoRA 或 RAG，并要求通过评测门。<br>**Long-description outline:** 定义四类问题，说明各方法直接改变的对象，最后列出数据、成本和证据要求。 | verified |
| `visual-llm-05-stage-objectives` | `llm-05 / sft-and-behavior-shaping` | 预训练、继续预训练与 SFT 的训练样本和直接目标有何不同？ | 阶段时间线与目标卡；primary=mechanism；tags=comparison | `llm-05.objectives[0]` | `res-hf-llm`, `res-rasbt`, `res-stanford-cs336` | 无（不选择第三方图） | 不适用：原创阶段对比，不复制课程训练流程图。 | original-synthesis | **Reading order:** 基础模型起点 → 预训练 → 继续预训练 → SFT → 每阶段数据/目标/可验证结果。<br>**Nodes/regions:** 原始文本、领域文本、输入输出示范、下一 token 目标、行为格式、评测样例。<br>**Edges or comparison axes:** 横向为阶段顺序，纵向三行比较数据、直接优化目标和不能保证的结果。<br>**Color-independent encoding:** 阶段用 I/II/III 编号，数据卡/损失卡/边界卡用不同形状。<br>**Fixture:** fixture=qualitative；scope=阶段时间线与目标卡只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 阶段名称相邻不等于目标相同，SFT 用示范塑造回答行为而不是简单继续喂文本。<br>**Alt summary:** 三阶段时间线对比预训练、继续预训练和 SFT 的数据形态、优化目标与能力边界。<br>**Long-description outline:** 按阶段说明输入、目标和参数变化，再列出每阶段不能自动保证的事实性、引用或业务成功。 | verified |
| `visual-llm-05-preference-boundary` | `llm-05 / preference-optimization-boundaries` | 偏好信号怎样塑造相对选择，又为什么不能把“更受偏好”当成全面更好？ | 成对选择与多目标天平；primary=mechanism；tags=boundary,comparison | `llm-05.objectives[0]`；`iq-llm-05-1` | `res-hf-llm`, `res-rasbt`, `res-happy-llm` | 无（不选择第三方图） | 不适用：原创选择机制示意；不呈现未由来源支持的具体算法公式。 | original-synthesis | **Reading order:** 同一 prompt → 回答 A/B → 偏好标签 → 优化信号 → 新行为分布 → 多目标边界。<br>**Nodes/regions:** 成对回答、偏好来源、相对选择、行为变化、有用/安全/信息量/校准。<br>**Edges or comparison axes:** 主链展示偏好信号进入优化；右侧天平比较四个目标，警示单目标过优化。<br>**Color-independent encoding:** 选择用勾/叉与排名数字，多目标用四臂刻度和文字。<br>**Fixture:** fixture=qualitative；scope=成对选择与多目标天平只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 偏好优化改变相对回答倾向，但偏好标签本身不能证明事实正确或所有目标同时改善。<br>**Alt summary:** 两个回答被相对选择并形成训练信号，右侧多目标天平显示过度优化单一偏好的代价。<br>**Long-description outline:** 先说明成对信号与绝对真值的区别，再描述行为分布变化，最后列出数据偏差和多目标权衡。 | verified |
| `visual-llm-05-lora-update` | `llm-05 / lora-as-parameter-efficient-update` | LoRA 冻结了什么、训练了什么，低秩增量如何合到原权重路径？ | 矩阵分解与旁路更新图；primary=mechanism；tags=process | `llm-05.objectives[0]`；`quiz-llm-05-2`；`iq-llm-05-3` | `res-rasbt`, `res-hf-llm`, `res-happy-llm` | 无（不选择第三方图） | 不适用：原创矩阵形状示意；不复制教材公式图。 | original-synthesis | **Reading order:** 冻结原权重 W → 输入主路径 → 低秩 A/B 旁路 → 增量 ΔW → 与主路径相加 → 训练/部署状态。<br>**Nodes/regions:** 原权重、两个低秩矩阵、可训练参数、合并节点、适配器存储。<br>**Edges or comparison axes:** 主路径实线保持冻结，旁路经 A 再 B 后相加；下方比较全量更新与低秩更新参数区域。<br>**Color-independent encoding:** 冻结块加锁纹，可训练块加铅笔图标；矩阵尺寸直接标注。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-05-lora-update；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** LoRA 以低秩旁路学习权重增量，减少可训练参数，但不消除基础模型、数据和评测成本。<br>**Alt summary:** 输入同时经过冻结权重和两个可训练低秩矩阵，增量结果相加形成适配后的输出。<br>**Long-description outline:** 定义冻结主权重、低秩分解和加法路径，再比较参数成本，并说明 rank 与目标层选择边界。 | verified |
| `visual-llm-05-rag-finetune-matrix` | `llm-05 / rag-finetuning-decision-lab` | 频繁更新且需出处的知识与稳定输出行为，分别应优先验证 RAG 还是微调？ | 五轴决策矩阵；primary=decision；tags=failure-mode,comparison | `llm-05.objectives[1]`；`quiz-llm-05-1`；`iq-llm-05-2`；`llm-05.exercise.steps[1]`；`llm-05.exercise.steps[2]` | `res-ms-genai`, `res-openai-cookbook`, `res-hf-llm`, `res-rasbt` | 无（不选择第三方图） | 不适用：原创矩阵与四个业务案例，不采用来源配图。 | original-synthesis | **Reading order:** 四个需求行 → 命名决策轴与 hasExamples → RAG/SFT 门 → 两个证据不足反例 → Prompt/工具基线或继续收集数据 → 上线评测。<br>**Nodes/regions:** 制度问答 RAG 正例、稳定客服语气 SFT 正例、不稳定目标反例、无示例反例、更新频率/引用/稳定行为/示例证据、计算与风险评测。<br>**Edges or comparison axes:** 更新频率或引用需求达到高阈值时进入 RAG；否则只有 stableBehavior 与 hasExamples 同时为真才进入 SFT/LoRA；两项否定路径均进入 `insufficient evidence—do not fine-tune`。<br>**Color-independent encoding:** 评分用 1–3 个实心格及文字“低/中/高”，推荐用带理由的箭头。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-05-rag-finetune-matrix；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 可更新或需引用的事实先验证 RAG；只有稳定行为目标且已有代表性示例时才评估 SFT/LoRA，否则先做 Prompt/工具基线并继续收集证据。<br>**Alt summary:** 四个用例按命名轴进入 RAG、SFT/LoRA 或证据不足边界，两个反例明确禁止在目标不稳或没有示例时直接微调。<br>**Long-description outline:** 先定义高阈值、稳定行为和示例证据，再走完 RAG/SFT 两个正例与不稳定/无示例两个反例，最后列出 Prompt/工具基线、数据收集和发布评测。 | verified |

### llm-06 推理、采样与 KV Cache

| visualId | lesson / section | cognitive question | visual form | assessed coverage | sourceIds | candidate image URL | permission evidence | decision | storyboard | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `visual-llm-06-generation-loop` | `llm-06 / sampling-experiment-and-serving-tradeoffs` | 一次请求怎样从 prefill 进入逐 token decode，并由采样与停止条件结束？ | 端到端解码环；primary=overview；tags=process | `llm-06.objectives[0]`；`llm-06.completionCriteria[0]` | `res-rasbt`, `res-hf-llm`, `res-openai-cookbook` | 无（不选择第三方图） | 不适用：原创端到端解码环；不复制服务商界面或课程动画。 | original-synthesis | **Reading order:** 输入上下文 → prefill → logits → 概率处理 → 候选限制 → token 选择 → 追加/KV 更新 → EOS/stop/上限检查。<br>**Nodes/regions:** 输入阶段、单步选择、缓存、循环、终止分支。<br>**Edges or comparison axes:** 主循环每次返回 logits；四类停止结果分别连到“正常完成/截断/业务判断”。<br>**Color-independent encoding:** 阶段用编号，循环边用粗线，停止原因用四种图标和文字。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-06-generation-loop；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 自回归服务反复执行单步选择与缓存更新，直到某个明确停止条件结束输出。<br>**Alt summary:** 请求先 prefill，再循环计算 logits、采样、追加 token 和更新缓存，最后由多种停止条件终止。<br>**Long-description outline:** 先分 prefill/decode，再走完一次 decode 步，最后解释 EOS、stop、上限和业务完成并非同一信号。 | verified |
| `visual-llm-06-logit-softmax` | `llm-06 / logits-softmax-next-token` | logits 与概率有什么区别，softmax 如何形成下一 token 的局部分布？ | 候选条形变换图；primary=mechanism；tags=process | `llm-06.objectives[0]` | `res-rasbt`, `res-hf-llm` | 无（不选择第三方图） | 不适用：原创教学数字；候选和数值明确标为示例。 | original-synthesis | **Reading order:** 五个 token 的原始 logits → 整体平移不变提示 → softmax → 概率排序 → 选中 token。<br>**Nodes/regions:** 候选词表切片、未归一化分数、概率分布、选择结果。<br>**Edges or comparison axes:** 横向同一候选跨两阶段对齐；纵轴分别标分数和概率，不混用刻度。<br>**Color-independent encoding:** 候选用固定编号，条形写精确数值，选中项用双边框与勾。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-06-logit-softmax；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** logits 是相对分数，softmax 把它们归一成当前一步的候选概率。<br>**Alt summary:** 同一组候选先显示未归一化 logits，再显示 softmax 后总和为一的概率和选中项。<br>**Long-description outline:** 定义 logits，逐候选展示 softmax 后排序，说明概率只属于当前上下文的一步选择。 | verified |
| `visual-llm-06-temperature-top-p` | `llm-06 / top-p-and-combined-sampling` | temperature 改变整个分布与 top-p 截取动态候选集合有何不同？ | 分布小倍图与累计质量阶梯；primary=comparison；tags=mechanism | `llm-06.objectives[0]`；`quiz-llm-06-1`；`iq-llm-06-1`；`llm-06.exercise.steps[1]` | `res-hf-llm`, `res-openai-cookbook`, `res-rasbt` | 无（不选择第三方图） | 不适用：原创概率示意；不声称示例是某模型真实输出。 | original-synthesis | **Reading order:** 基准 logits → 低/高 temperature 两个分布 → 对其中一图按概率排序 → 累计质量 → top-p 截断集合。<br>**Nodes/regions:** 温度缩放、概率条、累计曲线、阈值、保留/排除候选。<br>**Edges or comparison axes:** 左侧横轴候选、纵轴概率；右侧横轴排序候选、纵轴累计概率。<br>**Color-independent encoding:** 温度用 `T↓/T↑` 标签和线型；保留候选用勾，排除用叉和斜纹。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-06-temperature-top-p；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** temperature 重塑全分布，top-p 再按累计概率质量动态限制候选，两者都不会增加知识。<br>**Alt summary:** 低高温分布对比后，用累计概率曲线显示 top-p 如何选择每一步大小可变的候选集合。<br>**Long-description outline:** 先比较温度缩放，再逐项累计概率形成 top-p 集合，最后说明两参数组合与正确性边界。 | verified |
| `visual-llm-06-kv-cache` | `llm-06 / kv-cache-reuse-memory-and-concurrency` | KV Cache 复用了什么计算，为什么序列变长又会挤压显存和并发？ | 无缓存/有缓存时间展开图；primary=mechanism；tags=tradeoff,comparison | `llm-06.objectives[1]`；`quiz-llm-06-2`；`iq-llm-06-2`；`llm-06.completionCriteria[1]` | `res-hf-llm`, `res-rasbt`, `res-openai-cookbook` | 无（不选择第三方图） | 不适用：原创时间展开与内存条；不复制实现框架图。 | original-synthesis | **Reading order:** 上排无缓存三步重复前缀 → 下排 prefill 缓存历史 K/V → 新 token 只追加自身 K/V → 右侧显存/并发天平。<br>**Nodes/regions:** 历史 token、每层 K/V、重复计算、追加位置、缓存容量、并发请求。<br>**Edges or comparison axes:** 横轴生成步；上排重复边逐步增长，下排读取缓存；右侧轴比较延迟收益和内存代价。<br>**Color-independent encoding:** 已缓存块用斜纹和锁存图标，新块用空心框，重复计算用回环标记。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-06-kv-cache；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** KV Cache 避免每步重算历史 K/V，但缓存随序列与批量增长，会限制可服务并发。<br>**Alt summary:** 无缓存路径重复计算整个前缀，有缓存路径只追加新位置 K/V，旁边显示内存增加导致并发下降。<br>**Long-description outline:** 对比两条生成路径，定义缓存内容，说明它不是答案缓存，最后解释序列、层数、批量与显存关系。 | verified |
| `visual-llm-06-latency-breakdown` | `llm-06 / prefill-decode-and-latency-metrics` | 首 token 延迟、生成速度、排队和总时延分别由哪些阶段主导？ | 延迟瀑布与诊断矩阵；primary=boundary；tags=decision,tradeoff | `llm-06.objectives[1]`；`iq-llm-06-3` | `res-hf-llm`, `res-openai-cookbook` | 无（不选择第三方图） | 不适用：原创性能分解，不使用供应商基准图或数字。 | original-synthesis | **Reading order:** 请求到达 → 排队 → prefill → 首 token → 多个 decode 间隔 → 完成；下方按症状进入优化手段。<br>**Nodes/regions:** Queue、prefill、TTFT、TPOT、输出长度、P95、缓存/批处理/路由。<br>**Edges or comparison axes:** 横轴时间，瀑布段相加；矩阵行是症状，列是可观测指标和候选修复。<br>**Color-independent encoding:** 阶段用纹理块和缩写，首 token 用旗标，尾延迟用 P95 刻度。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-06-latency-breakdown；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 先把排队、prefill 与 decode 分开测量，才能选择真正降低延迟与成本的手段。<br>**Alt summary:** 请求时间线分成排队、prefill 和逐 token decode，并把不同慢点映射到相应诊断指标。<br>**Long-description outline:** 定义 TTFT、单 token 间隔和总时延，逐阶段列出主要驱动因素，再说明为何不能只看平均值。 | verified |

### llm-07 Prompt 与结构化输出

| visualId | lesson / section | cognitive question | visual form | assessed coverage | sourceIds | candidate image URL | permission evidence | decision | storyboard | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `visual-llm-07-runtime-contract` | `llm-07 / prompt-as-runtime-contract` | 一个可测试的 Prompt 契约需要声明哪些输入、规则、输出和失败行为？ | 契约剖面图；primary=overview；tags=boundary | `llm-07.objectives[0]`；`iq-llm-07-1`；`llm-07.completionCriteria[0]` | `res-ms-genai`, `res-openai-cookbook`, `res-hf-agents` | 无（不选择第三方图） | 不适用：原创契约结构，不复制文档模板或界面。 | original-synthesis | **Reading order:** 任务目标 → 可信规则 → 输入边界 → few-shot 边界例 → 输出 Schema → 失败行为 → 评测版本。<br>**Nodes/regions:** Prompt 固定区、运行时数据区、输出契约区、控制流区、版本记录。<br>**Edges or comparison axes:** 主路径从输入到校验；规则对数据单向约束，数据不能反向覆盖规则。<br>**Color-independent encoding:** 固定规则用锁框，数据用可替换卡槽，Schema 用字段表，失败用分支节点。<br>**Fixture:** fixture=qualitative；scope=契约剖面图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 健壮 Prompt 是可版本化的运行时契约，而不是依赖神奇措辞的长文本。<br>**Alt summary:** 契约图依次列出目标、高层规则、不可信输入、示例、Schema、失败行为和评测版本。<br>**Long-description outline:** 定义固定与动态部分，解释成功标准和边界示例，最后说明输出校验与版本化评测。 | verified |
| `visual-llm-07-instruction-boundary` | `llm-07 / instruction-and-untrusted-data-boundaries` | 外部文档或用户内容伪装成指令时，哪些控制必须放在模型之外？ | 信任边界与攻击路径图；primary=boundary；tags=failure-mode | `llm-07.objectives[0]`；`iq-llm-07-3`；`llm-07.exercise.steps[1]` | `res-owasp-prompt-injection`, `res-ms-genai`, `res-hf-agents` | 无（不选择第三方图） | 不适用：原创威胁路径；不复制 OWASP 图形资产。 | original-synthesis | **Reading order:** 高层规则 → 用户/检索/工具三类不可信输入 → 模型判断 → 工具提议 → 权限/参数/确认门 → 执行。<br>**Nodes/regions:** 信任区、不可信数据区、模型区、策略执行点、外部系统。<br>**Edges or comparison axes:** 攻击虚线尝试跨界；只有经最小权限和确认的实线可达外部动作。<br>**Color-independent encoding:** 信任区双边框，不可信区斜纹，攻击边虚线带叉，允许边实线带勾。<br>**Fixture:** fixture=qualitative；scope=信任边界与攻击路径图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 指令层级能表达优先级，却不能替代模型外的权限、参数校验和高风险确认。<br>**Alt summary:** 不可信内容可影响模型提议，但工具调用必须经过外部权限、参数和确认门才能执行。<br>**Long-description outline:** 先标出信任域，沿直接和间接注入路径走到模型，再列出阻止影响扩大为动作的控制。 | verified |
| `visual-llm-07-schema-pipeline` | `llm-07 / schema-and-structured-output` | “返回 JSON”与通过 JSON Schema 的结构化输出之间隔着哪些检查？ | 生成—解析—Schema 管线；primary=mechanism；tags=process | `llm-07.objectives[1]`；`iq-llm-07-2`；`llm-07.exercise.steps[0]` | `res-openai-cookbook`, `res-ms-genai`, `res-hf-agents` | 无（不选择第三方图） | 不适用：原创数据管线；字段示例来自课程练习。 | original-synthesis | **Reading order:** 模型文本 → JSON 解析 → 必填/类型/枚举 Schema → 跨字段业务规则 → 权限/数据库检查 → typed object。<br>**Nodes/regions:** 原始输出、不可信对象、结构校验、业务校验、可信消费端。<br>**Edges or comparison axes:** 每个校验门有成功主边与错误分支；只有全部通过才进入副作用区。<br>**Color-independent encoding:** 校验门用编号六边形，错误类型直接写标签，可信对象加签章图标。<br>**Fixture:** fixture=qualitative；scope=生成—解析—Schema 管线只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** JSON 语法只是第一关，Schema、业务规则和权限校验共同决定输出是否可用。<br>**Alt summary:** 模型输出依次经过 JSON 解析、Schema、业务和权限检查，失败均分流，全部通过才成为可信对象。<br>**Long-description outline:** 区分语法、结构、跨字段和外部状态校验，给出每关的失败例，再说明为何不能直接执行副作用。 | verified |
| `visual-llm-07-retry-state-machine` | `llm-07 / validation-retry-and-side-effects` | 校验失败后何时修复重试、何时降级或转人工，怎样避免重复副作用？ | 有限重试状态机；primary=mechanism；tags=failure-mode,process | `llm-07.objectives[1]`；`llm-07.exercise.steps[2]`；`llm-07.completionCriteria[1]`；`iq-llm-07-2` | `res-openai-cookbook`, `res-ms-genai`, `res-owasp-prompt-injection` | 无（不选择第三方图） | 不适用：原创状态机；不复制任何 SDK 控制流图。 | original-synthesis | **Reading order:** 生成 → 校验 → 有限重试 → 通过 → 逐条读取 key/payload 事件 → 新 key 执行并缓存 → 相同 key 复用 → 不同 key 再执行 → 非法 key 拒绝。<br>**Nodes/regions:** 初始、验证失败、有限重试、成功、降级、人工、事件流、结果缓存、副作用计数与拒绝结果。<br>**Edges or comparison axes:** 状态边标事件和 guard；循环仅在 `attempt < limit`；合法新 key 写入缓存，相同 key 只读缓存，不同 key 产生第二次副作用，空或非法 key 无执行边。<br>**Color-independent encoding:** 状态用圆角框，终止态双圈，guard 写在线上；事件同时写 key 与 payload，executed/deduplicated/rejected 使用文字、形状与线型编码。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-07-retry-state-machine；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 校验通过后仍须逐事件验证幂等 key：同 key 复用结果、不同 key 独立执行、非法 key 拒绝且不产生副作用。<br>**Alt summary:** 有限状态机先修复结构错误，随后事件流展示同 key 去重、不同 key 第二次执行，以及空或非法 key 的无副作用拒绝。<br>**Long-description outline:** 列出校验失败与重试 guard，再逐条执行五个 key/payload 事件，核对返回结果、缓存键、去重状态与副作用执行次数，最后说明非法 key 拒绝边界。 | verified |
| `visual-llm-07-version-eval-loop` | `llm-07 / observable-versioned-evaluation` | Prompt、模型、Schema 变化怎样通过日志和回归评测形成可发布版本？ | 版本化反馈回路；primary=process；tags=decision | `iq-llm-07-1` | `res-openai-cookbook`, `res-llm-universe`, `res-ms-genai`, `res-owasp-prompt-injection` | 无（不选择第三方图） | 不适用：原创发布回路；不使用产品仪表板截图。 | original-synthesis | **Reading order:** 版本元组 → 离线样例 → 运行输出 → 结构/业务/安全指标 → 失败聚类 → 修改 → 回归比较 → 发布/回滚。<br>**Nodes/regions:** Prompt/模型/Schema 版本、评测集、可观测日志、失败簇、发布门。<br>**Edges or comparison axes:** 环形反馈边连接失败到修改；发布门横向比较当前基线与新版本；失败样本 3 明确属于安全切片，因此安全 hard gate 会阻断 v1。<br>**Color-independent encoding:** 版本用哈希标签，指标用数值和箭头，发布/阻断用勾/停牌。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-07-version-eval-loop；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 只有把 Prompt 与模型、Schema、评测集和日志绑定，改进才能被复现、比较和回滚。<br>**Alt summary:** 版本元组经过离线和运行评测，失败聚类推动修改，再由回归门决定发布或回滚。<br>**Long-description outline:** 定义版本元组和必要日志，说明失败怎样进入数据集，最后描述比较基线与发布门。 | verified |

### llm-08 能力边界、评测与安全

| visualId | lesson / section | cognitive question | visual form | assessed coverage | sourceIds | candidate image URL | permission evidence | decision | storyboard | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `visual-llm-08-failure-map` | `llm-08 / failure-taxonomy-not-fluency` | 一个流畅但不可用的回答，可能属于哪些不同失败类型？ | 失败分类地图；primary=overview；tags=boundary | `llm-08.objectives[0]`；`llm-08.completionCriteria[0]` | `res-ms-genai`, `res-openai-cookbook`, `res-hf-agents` | 无（不选择第三方图） | 不适用：原创分类地图；不复制安全或评测框架图。 | original-synthesis | **Reading order:** 观察到的失败 → 事实/推理/格式/指令/权限/性能六类 → 各自证据 → 对应缓解。<br>**Nodes/regions:** 症状层、原因层、诊断信号层、控制层。<br>**Edges or comparison axes:** 一个症状可连多个原因；每个原因只连匹配的检查与缓解，避免单一“幻觉”桶。<br>**Color-independent encoding:** 六类用编号和图标，症状到原因用虚线，确认证据用实线。<br>**Fixture:** fixture=qualitative；scope=失败分类地图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 流畅度不能定位失败，必须先按事实、推理、格式、安全和系统性能分类。<br>**Alt summary:** 分类图把同一不可用回答分解为六类失败，并为每类连接不同证据和缓解措施。<br>**Long-description outline:** 从常见症状出发，逐类定义失败与可观测证据，再说明为何错误分类会选择错误修复。 | verified |
| `visual-llm-08-grounding-chain` | `llm-08 / evidence-grounding-and-uncertainty` | 证据被检索进上下文后，还要经过哪些环节才能支持忠实答案？ | 证据链与断点图；primary=mechanism；tags=failure-mode,process | `iq-llm-08-1` | `res-openai-cookbook`, `res-ms-genai`, `res-llm-universe` | 无（不选择第三方图） | 不适用：原创证据链；不复制 RAG 产品图。 | original-synthesis | **Reading order:** 用户问题 → 文档版本 → 检索召回 → 排序 → 上下文组装 → 答案主张 → 引用对齐 → 拒答/复核。<br>**Nodes/regions:** 知识源、检索、上下文、生成、验证、处置。<br>**Edges or comparison axes:** 主证据链逐段前进；每段下方标缺失、错误、冲突、误用等断点。<br>**Color-independent encoding:** 成功链为实线，断点为断裂线和编号警告，引用用同号脚注连接主张。<br>**Fixture:** fixture=qualitative；scope=证据链与断点图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** 证据进入上下文不等于答案忠实，召回、使用和引用对齐都必须分别验证。<br>**Alt summary:** 从知识源到引用的证据链包含多个可失败环节，每个主张必须与支持它的证据对齐。<br>**Long-description outline:** 逐节点说明输入输出，列出对应失败类型，再描述信息不足、冲突和高风险时的拒答或人工复核。 | verified |
| `visual-llm-08-eval-funnel` | `llm-08 / eval-dataset-and-slices` | 真实需求和失败日志怎样变成覆盖正常、边界和对抗场景的发布评测？ | 评测集—切片—发布门漏斗；primary=process；tags=decision | `llm-08.objectives[1]`；`iq-llm-08-2`；`llm-08.exercise.steps[0]` | `res-openai-evals`, `res-openai-cookbook`, `res-ms-genai`, `res-hf-agents` | 无（不选择第三方图） | 不适用：原创漏斗；不使用评测产品界面截图。 | original-synthesis | **Reading order:** 需求/日志/事故 → 去重与版本化样本 → 正常/边界/对抗切片 → grader 组合 → 切片阈值 → 发布门 → 线上回流。<br>**Nodes/regions:** 样本来源、数据治理、切片、规则/代码/模型 grader、阈值、监控。<br>**Edges or comparison axes:** 漏斗向下收敛到发布决策，线上失败以回边进入样本池；切片结果并排比较。<br>**Color-independent encoding:** 样本来源用图标和标签，切片用不同线框，阈值用刻度线和数值。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-08-eval-funnel；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 有效评测从真实需求与失败长出，并以版本化切片阈值而非单一平均分守住发布门。<br>**Alt summary:** 真实需求和失败日志经治理、切片和组合评分进入发布门，线上新失败再回流到评测集。<br>**Long-description outline:** 说明样本来源和防污染，逐切片列出目的，再解释 grader 组合、阈值与持续回归。 | verified |
| `visual-llm-08-injection-defense` | `llm-08 / prompt-injection-threat-model` | Prompt Injection 怎样从不可信内容沿模型与工具链扩大影响，控制应放在哪些点？ | 攻击树与纵深防御叠图；primary=mechanism；tags=boundary,failure-mode | `llm-08.objectives[0]`；`quiz-llm-08-1`；`llm-08.exercise.steps[2]` | `res-owasp-prompt-injection` | 无（不选择第三方图） | 不适用：原创威胁模型；不复制 OWASP 图像或页面布局。 | original-synthesis | **Reading order:** 攻击入口 → 直接/间接注入 → 模型被诱导 → 敏感数据/工具目标 → 输入隔离、最小权限、参数校验、确认、日志响应。<br>**Nodes/regions:** 外部内容、模型、工具代理、数据域、五层控制。<br>**Edges or comparison axes:** 攻击树向右扩散；每条路径由一到多个控制栅栏截断，未截断路径到达影响。<br>**Color-independent encoding:** 攻击边虚线箭头，控制为带编号栏杆，影响节点为爆裂边框。<br>**Fixture:** fixture=qualitative；scope=攻击树与纵深防御叠图只冻结结构、关系、边界与阅读顺序，不编码数值主张。<br>**One-sentence caption conclusion:** Prompt Injection 是跨信任边界的系统风险，必须用多层模型外控制限制其可达影响。<br>**Alt summary:** 直接和间接注入可诱导模型接近敏感工具，但多层权限、校验、确认与监控在路径上截断影响。<br>**Long-description outline:** 从两个入口走攻击路径，逐控制点说明阻断对象，最后解释提示文本为何不是强安全边界。 | verified |
| `visual-llm-08-release-pareto` | `llm-08 / defense-in-depth-and-runtime-operations` | 质量、安全、成本和延迟冲突时，怎样先守硬门，再用独立指标判断候选支配关系？ | 双小倍图与硬门表；primary=decision；tags=comparison,tradeoff | `llm-08.objectives[1]`；`iq-llm-08-3`；`llm-08.exercise.steps[1]`；`llm-08.completionCriteria[1]` | `res-owasp-prompt-injection`, `res-hf-agents`, `res-ms-genai`, `res-anthropic-agents` | 无（不选择第三方图） | 不适用：原创决策图；示例阈值不冒充来源或生产基准。 | original-synthesis | **Reading order:** 独立指标表 → 安全硬门筛除 → 质量–成本小倍图 → 质量–延迟小倍图 → 按共享候选编号读取 dominance → 发布/路由/人工升级。<br>**Nodes/regions:** A–E 候选指标表、安全硬门、质量–成本面板、质量–延迟面板、支配/非支配标记、在线监控。<br>**Edges or comparison axes:** 两图纵轴都为质量；第一图横轴仅为每请求成本，第二图横轴仅为 P95 延迟，不合成单位；相同候选编号在两图一一对应，安全失败先在表中剔除。<br>**Color-independent encoding:** A–E 同时用字母和不同点形，硬门失败加叉，B→D 的 dominance 用带标签箭头，非支配候选加外圈。<br>**Fixture:** fixture=quantitative；fixtureId=fixture-llm-08-release-pareto；source=tests/fixtures/llm-foundation-visual-fixtures.js。<br>**One-sentence caption conclusion:** 安全硬门失败直接阻断；合格候选再用分开的质量–成本与质量–延迟图判断 dominance，而不制造未定义的综合分数。<br>**Alt summary:** 指标表先淘汰安全不达标的 C；两个共享 A–E 编号的小倍图分别比较质量与成本、质量与延迟，显示 B 支配 D，A/B/E 非支配。<br>**Long-description outline:** 先逐列读出质量、安全、成本和 P95 的单位与硬门，再在两个独立坐标系中定位 A–E，解释 B 为何支配 D、A/B/E 为何互有取舍，最后说明这些教学阈值必须由实际 owner 重填。 | verified |

## 第三方图像许可审计结论

本轮冻结没有选择任何第三方图片文件、截图、论文原图、视频画面或官方媒体素材作为站内资产。因此：

- `licensed-reproduction`：0
- `licensed-adaptation`：0
- `official-media`：0
- `link-only-original-replacement`：0
- `original-synthesis`：40

所有 `candidate image URL` 均明确为“无（不选择第三方图）”。这是已完成的原创决策，不是许可信息缺失。论文页面、课程页面、仓库许可证、视频页面或搜索摘要均不自动证明某张图片允许本站再分发或修改；制作阶段不得从 `sourceIds` 对应页面下载图片或近似临摹其独特构图。若未来提出使用某张第三方原图，必须把该项重新置于发布门外，并补齐原始正文/媒体页面、作者、可唯一识别的图号、实际许可证或媒体政策 URL、再分发与修改权限、版本、获取日期及修改记录后，才能改用 `licensed-*` 或 `official-media`。

本轮没有第三方媒体复用需求，因而没有可适用的许可证 URL；没有使用搜索摘要、转载页或来源标题推断许可，也没有为了填满许可类型而强行选择外部图片。

## 覆盖与制作门

| lesson | total | overview | mechanism/process/relationship | boundary/comparison/failure/decision | verified | blocked |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `llm-01` | 5 | 1 | 2 | 2 | 5 | 0 |
| `llm-02` | 5 | 1 | 2 | 3 | 5 | 0 |
| `llm-03` | 5 | 1 | 2 | 3 | 5 | 0 |
| `llm-04` | 5 | 1 | 3 | 1 | 5 | 0 |
| `llm-05` | 5 | 1 | 3 | 4 | 5 | 0 |
| `llm-06` | 5 | 1 | 3 | 2 | 5 | 0 |
| `llm-07` | 5 | 1 | 3 | 3 | 5 | 0 |
| `llm-08` | 5 | 1 | 3 | 4 | 5 | 0 |
| **合计** | **40** | **8** | **21** | **22** | **40** | **0** |

类别列允许同一张图同时承担机制与边界任务，因此横向分类之和可能大于课程总数。制作时仍须逐项满足以下门：

1. 只依据该行 `sourceIds` 与对应 section 的证据范围组织事实，不添加来源未建立的实现细节或定量结论。
2. 从空白画布执行 storyboard；不得下载、描摹或改写第三方图片，即使其页面或仓库看起来开放。
3. 保留 caption、alt 与 long description；颜色必须与形状、文字、线型或纹理冗余编码。
4. 任何新增第三方媒体需求都视为新许可审计，不能沿用本清单的“不适用”结论。
