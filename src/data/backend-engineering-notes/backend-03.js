function deepFreeze(value) {
  if (value && typeof value === 'object' && !Object.isFrozen(value)) {
    Object.freeze(value);
    Object.values(value).forEach(deepFreeze);
  }
  return value;
}

export const backend03Note = deepFreeze({
  readingMinutes: 26,
  introduction: 'AI 请求不是均匀的小工作：prompt 长度、输出长度、工具数量和模型类型都会让服务时间相差几个数量级。只提高并发上限会把等待转移到连接池、内存、供应商配额或 GPU 队列，最终让少量慢请求拖累所有用户。本课用 Little 定律建立排队直觉，再把 deadline、准入控制、公平性和尾延迟组织成一个闭环，目标不是“永不拒绝”，而是在过载时仍能给出可预测、可恢复的结果。',
  sections: [
    {
      id: 'queueing-intuition',
      title: 'Little 定律只连接平均量',
      paragraphs: [
        'Columbia 的 Little 定律讲义从累计到达、累计离开与样本路径面积说明 L = λW：长期平均系统内数量等于长期平均有效到达率乘以长期平均停留时间。使用这个等式前，要确认相应长期极限存在且有限；若观测窗口内系统持续积压、没有稳态或均值发散，就不能把短窗口数字当成定律已经满足。',
        'L = λW 不是 p95 或 p99 公式，也不告诉我们延迟分布形状、服务时间方差和优先级效果。若每秒接受 20 个请求且平均停留 3 秒，平均约有 60 个在途请求；这个例子只用于检查均值量级。一个平均三秒的系统仍可能有严重长尾，分位数、直方图和按工作类型分组的测量不可替代。',
        '当到达率长期超过可完成率，队列必然增长。增加无限队列只是推迟拒绝，同时让等待超过用户 deadline、占用内存并积累注定过期的工作。容量规划应记录到达率、完成率、队列深度、最老任务年龄和服务时间分布；一旦超过安全工作区，就在入口减少接纳，而不是等进程崩溃后随机丢请求。',
      ],
      keyPoints: ['Little 定律用于平均在途量和停留时间的健全性检查', '尾延迟与过载需要分布和队列年龄证据'],
      sourceIds: ['res-backend-little-law', 'res-backend-tail-at-scale', 'res-backend-sre-cascading'],
    },
    {
      id: 'deadline-budget',
      title: 'Deadline 是端到端预算',
      paragraphs: [
        '客户端真正关心的是整个操作何时不再有价值，因此 deadline 应从入口沿调用链传递，而不是每层重新获得完整超时。服务先扣除已用时间，再为排队、数据库、模型、工具和清理分配剩余预算。若剩余时间不足以安全开始昂贵阶段，应尽早拒绝或降级，避免做完结果也来不及交付。',
        '超时值需要来自观测和产品目标。连接超时、首字节、单次读取、总模型调用和整体 job 的失败含义不同；统一设置 30 秒会让某些阶段过宽、另一些过窄。预算还要给重试和清理留空间，例如单次调用最多使用剩余预算的一部分，失败后只有在错误可重试且仍有足够时间时再尝试。',
        'deadline 到达不证明远端停止。服务应记录本地何时放弃等待、取消请求是否发出、远端是否确认，以及迟到结果如何处置。对可复用或已付费的结果，可以在后台持久化后供查询；对不应继续的交互，执行有界取消并标记 unknown outcome。诚实状态比把所有超时统一写成“已取消”更可靠。',
      ],
      keyPoints: ['预算沿调用链递减而不是逐层重置', '超时后仍要追踪远端结果和副作用'],
      sourceIds: ['res-backend-openai-rate-limits', 'res-backend-sre-cascading'],
    },
    {
      id: 'admission-control',
      title: '在昂贵工作之前做准入',
      paragraphs: [
        '准入控制回答“当前是否应该开始这项工作”，限流则常按时间窗口控制请求数量，两者相关但不完全相同。AI 服务还要考虑并发 slot、估算 token、模型池、tenant 配额、队列年龄和依赖健康。入口在持久化必要审计后尽早判定，避免已经占用数据库连接或模型配额才发现系统过载。',
        '429 可以告诉调用者当前限额不足，并结合 Retry-After 或响应头提供节奏信息；503 更适合表达服务或依赖暂时不可用。无论使用哪个状态码，拒绝必须便宜、可观察且不制造额外 fan-out。客户端需要指数退避、随机抖动和总重试预算，否则同一批失败请求会在固定间隔同步返回，形成重试风暴。',
        '公平性要求把租户和工作类型纳入准入。单一全局 semaphore 可能让一个大客户或长报告占满 slot，使短交互完全饥饿。可以为租户设置配额、为交互和批处理建立不同池，并使用加权队列；但规则必须可解释且防止身份维度爆炸。优先级不是让高优请求无限插队，也要保留低优工作获得服务的机制。',
      ],
      keyPoints: ['准入要发生在昂贵资源分配之前', '公平性应覆盖租户、成本和工作类型'],
      sourceIds: ['res-backend-rfc6585', 'res-backend-openai-rate-limits', 'res-backend-dagor'],
    },
    {
      id: 'tail-latency',
      title: '尾延迟会被调用扇出放大',
      paragraphs: [
        '一次请求若并行调用多个工具或分片，整体通常要等待最慢分支，单个依赖很小的慢请求概率会被 fan-out 放大。平均延迟稳定并不能保护 p99；共享队列、垃圾回收、连接抖动和热点数据还会制造相关慢请求，使简单的独立性估算过于乐观。诊断必须保存每个分支的阶段时长和共同资源状态。',
        'hedging、备份请求和更早择优返回可能降低尾部，但会增加负载和费用，并可能产生重复副作用。只有读操作或具备严格幂等与取消协议的动作才适合考虑，而且应在超过某个延迟阈值后有限触发。系统已经过载时盲目复制请求会加剧拥塞，因此要与准入、容量余量和全局预算共同设计。',
        'Tail at Scale 和 DAGOR 提供重要机制与经验，却不能把论文中的数字直接写成当前 AI 服务承诺。其工作负载、规模、硬件、调用图和优先级语义与本系统不同。正确用法是提炼“尾部会放大”“过载应在入口控制”等可检验假设，再通过自己的流量回放和故障注入测量阈值。',
      ],
      keyPoints: ['fan-out 让最慢分支决定整体尾部', '缓解机制会增加负载，必须在本地工作负载验证'],
      sourceIds: ['res-backend-tail-at-scale', 'res-backend-dagor'],
    },
    {
      id: 'overload-loop',
      title: '用闭环控制而非静态魔法数字',
      paragraphs: [
        '并发上限、队列长度和超时不是一次写死的常量。先定义安全工作区，例如 CPU、内存、连接、模型 slot、错误率和队列年龄；再用负载测试找出吞吐开始饱和、尾延迟陡升的位置，并留出恢复余量。线上控制依据变化缓慢的信号调整准入，避免每次瞬时抖动都让阈值来回震荡。',
        '级联故障通常形成反馈环：依赖变慢导致在途请求增加，资源耗尽引发更多超时，客户端重试又提高到达率，健康检查和重启进一步损失容量。打断环路需要有界队列、负载丢弃、重试预算、保守 liveness 和降级路径。单独扩大线程池或超时往往只把崩溃推迟并增加故障恢复时间。',
        '运行手册要把信号映射为动作：队列年龄升高但 worker 空闲时查路由和租约；worker 满载且模型延迟稳定时扩 worker；模型延迟上升时降低准入或切换较小模型；429 增多但整体健康时检查租户公平性。每个动作都应记录预期效果、回滚条件和观察窗口，使容量管理成为可复盘实验。',
      ],
      keyPoints: ['阈值来自压测与持续观测并保留余量', '用负反馈打断重试、排队和资源耗尽的级联环'],
      sourceIds: ['res-backend-sre-cascading', 'res-backend-dagor', 'res-backend-openai-rate-limits'],
    },
    {
      id: 'capacity-experiment',
      title: '容量实验要控制变量与恢复阶段',
      paragraphs: [
        '先固定模型、硬件、prompt 长度分布和缓存状态，以阶梯方式提高开放环到达率。开放环生成器不会因为服务变慢自动减少请求，更接近真实突发；若只用“上一个完成后再发下一个”的闭环压测，系统延迟升高会自动降低施压，可能掩盖过载点。每个台阶持续到队列和资源进入可判断状态。',
        '报告同时绘制 offered load、accepted load 与 completed throughput，并标注拒绝、超时和取消。吞吐平台期只是饱和线索，安全上限通常要放在尾延迟陡升之前。按短交互、长报告、租户和模型拆分分位数，检查全局平均是否掩盖某一类工作饥饿；再用 Little 定律核对平均在途量是否与测量一致。',
        '峰值结束后继续观察恢复时间。一个受控系统会停止积累过期工作，队列年龄下降并恢复正常延迟；一个不受控系统可能持续重试、内存交换或反复重启。记录拒绝比例、清空队列时间和恢复期间错误预算，才能比较两种准入策略，而不是只比较峰值每秒请求数。',
      ],
      keyPoints: ['开放环负载更容易暴露真实过载反馈', '容量评估同时覆盖饱和、拒绝、公平性和恢复时间'],
      sourceIds: ['res-backend-tail-at-scale', 'res-backend-sre-cascading', 'res-backend-dagor', 'res-backend-coordinated-omission'],
    },
  ],
  misconceptions: [
    { claim: 'L = λW 可以直接算出服务的 p99。', correction: '它连接长期平均量，不能替代延迟分布、分位数和服务时间方差测量。' },
    { claim: '队列越长，系统越不容易拒绝请求。', correction: '无限队列会积累超过 deadline 的工作并耗尽资源，只是把明确拒绝变成迟到失败。' },
    { claim: '给每一层相同超时就是端到端 deadline。', correction: '逐层重置会放大总时长，预算应扣除已用时间并为重试与清理留余量。' },
    { claim: '提高并发一定能提高吞吐。', correction: '超过瓶颈后并发只会增加排队、上下文切换和尾延迟，必须用饱和曲线验证。' },
    { claim: '备份请求总能降低尾延迟。', correction: 'hedging 会增加负载和重复副作用风险，只适合有容量余量且可安全重复的受控场景。' },
  ],
  recap: [
    'Little 定律用于平均在途量，不是尾延迟公式。',
    '到达率长期高于完成率时，有界队列和早拒绝不可避免。',
    'deadline 是沿调用链递减的端到端预算。',
    '准入控制要在昂贵资源分配前执行并考虑公平性。',
    'fan-out 会放大慢分支，缓解手段也会消耗容量。',
    '过载防护要打断排队、超时、重试和重启形成的反馈环。',
  ],
  nextStep: '为一个模拟模型端点构造短请求、长请求和突发租户三种流量，逐步提高到达率并记录完成率、平均与 p95/p99、队列深度、最老任务年龄和资源利用率。用这些数据选择初始并发门与队列上限，再加入端到端 deadline、429、Retry-After 和带抖动的有限重试。最后让依赖延迟翻倍，验证系统会受控拒绝并恢复，而不是形成重试风暴。压测结束后继续观察到队列清空，比较启用与关闭准入控制时的恢复时长、失败尝试总数和内存峰值。按租户及长短任务拆分数据，确认高优流量获得保护的同时，普通任务不会永久饥饿。把阈值、硬件和流量分布写入报告，禁止脱离这些条件复用结论。最后用第二组不同长度分布重复实验，检查阈值是否对工作负载漂移过度敏感，并写出重新校准条件、值班观察步骤、安全回退值和变更审核责任人，留存完整实验档案。',
  tests: {
    status: 'passed',
    commands: ['node --test tests/backend-engineering-data.test.js', 'npm test'],
    results: [
      { command: 'node --test tests/backend-engineering-data.test.js', exitCode: 0, summary: '9 项目标数据测试通过。' },
      { command: 'npm test', exitCode: 0, summary: '全量测试通过。' },
    ],
  },
});
