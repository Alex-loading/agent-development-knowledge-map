function deepFreeze(value) {
  if (value && typeof value === 'object' && !Object.isFrozen(value)) {
    Object.freeze(value);
    Object.values(value).forEach(deepFreeze);
  }
  return value;
}

export const backend01Note = deepFreeze({
  readingMinutes: 24,
  introduction: 'AI 后端的第一项工作不是挑框架，而是把“用户想得到一份结果”拆成可以被客户端、网关、服务和运维共同理解的协议。模型调用昂贵、耗时且可能失败，Agent 还会跨越多个工具与状态，因此普通 CRUD 中被忽略的边界会迅速变成重复扣费、错误重试和无法追责的问题。本课从服务边界、HTTP 语义和 OpenAPI 契约出发，建立后续流式、异步和可靠性设计都能复用的词汇。',
  sections: [
    {
      id: 'service-boundary',
      title: '先画服务边界，再写接口',
      paragraphs: [
        '研究报告服务看似只有“提交主题、返回报告”，实际至少包含身份校验、配额判断、请求持久化、Agent 编排、模型调用、工具访问、结果存储和通知。边界设计要先说明哪一层拥有业务事实，哪一层只做转发或缓存，以及一次请求离开本服务以后还有哪些副作用。若这些责任没有写清，团队会把一次 HTTP 成功误当成整个研究任务已经可靠完成。',
        '可以从不变量反推边界：同一租户不能读取他人报告；相同幂等键不能无意创建两个付费任务；已接受的任务必须能查询最终状态；模型供应商超时不能让数据库留下互相矛盾的记录。不变量比类图更有用，因为它直接说明必须在哪个持久化边界内检查条件、提交事实，并为边界外动作留下可对账的证据。',
        '接口层只应该承诺它能控制的事实。例如 202 表示服务接受了处理请求，而不是报告必然成功；201 可以表示资源已经创建，却不代表下游模型已经执行。将“接受”“开始”“完成”“失败”分成不同状态后，客户端才不会以连接是否存活猜测业务事实，后端也能为同步、流式和后台处理共用同一状态模型。',
      ],
      keyPoints: ['用不变量和所有权定义服务边界', 'HTTP 响应只承诺当前边界已经证明的事实'],
      sourceIds: ['res-backend-rfc9110', 'res-backend-openapi'],
    },
    {
      id: 'http-semantics',
      title: 'HTTP 方法和状态码是行为契约',
      paragraphs: [
        'HTTP 方法不是路由命名风格，而是代理、客户端和重试组件理解请求意图的共同语言。GET 应当读取而不制造业务副作用，PUT 通常以目标资源为中心表达替换或创建，POST 常用于提交处理或创建服务器分配标识的资源。这里的“幂等”是重复相同请求的预期效果相同，并不意味着每次响应字节一致，也不证明内部只执行了一次。',
        '状态码要帮助调用方决定下一步，而不是只区分成功和失败。400 表示请求本身不符合约束，401 与 403 区分认证和授权，404 可用于资源不存在，409 适合当前状态冲突，422 可表达语义校验失败，429 表示限额触发，5xx 则说明服务器未能履行请求。错误体还应提供稳定机器码、可读消息、requestId，以及在安全范围内给出字段级细节。',
        '限流响应尤其要避免含糊。RFC 6585 定义了 429，但没有替服务决定令牌桶、并发门或租户配额。Retry-After 能传递建议等待时间，也不代表届时一定有容量。客户端需要有最大尝试次数、退避和抖动；服务端需要按调用者、模型和成本维度实施公平准入，两者共同避免瞬时拥塞被重试放大。',
      ],
      keyPoints: ['HTTP 幂等不等于内部 exactly-once', '状态码和错误体应支持确定的客户端决策'],
      sourceIds: ['res-backend-rfc9110', 'res-backend-rfc6585'],
    },
    {
      id: 'openapi-contract',
      title: 'OpenAPI 描述可检查的接口表面',
      paragraphs: [
        'OpenAPI 将路径、operation、参数、请求体、响应、schema 和安全方案组织成机器可读文档。它的价值在于让前后端、SDK、mock、网关和契约测试共享同一份接口表面，而不是靠聊天记录同步。对 AI 服务而言，尤其要明确枚举状态、可空字段、错误结构、分页游标、幂等键和 requestId，避免自由文本承担机器协议。',
        '一份 schema 仍然可能与运行实现漂移。生成文档并不会验证数据库迁移、鉴权规则、超时、并发上限或副作用，因此 OpenAPI 不会自动保证实现兼容和可靠。团队应在 CI 中验证规范格式、生成客户端做编译检查、用契约样例命中真实服务，并把破坏性变更作为需要迁移窗口的产品决策，而不是悄悄修改字段。',
        '版本兼容要围绕调用方行为设计。增加可选字段通常比删除或改义安全，但客户端也应忽略未知响应字段；枚举增加值可能击穿穷举分支；把可选改成必填或改变默认值都可能破坏旧客户端。兼容窗口还要覆盖队列中等待的旧任务，因为它们可能由新 worker 读取旧 payload，不能只测试同版本即时 HTTP 请求。',
      ],
      keyPoints: ['规范、实现和契约测试三者需要持续对齐', '兼容性要覆盖客户端与队列中的旧载荷'],
      sourceIds: ['res-backend-openapi'],
    },
    {
      id: 'resource-state',
      title: '把长任务建模成资源状态机',
      paragraphs: [
        '长任务接口最好先创建 job 资源，再通过稳定标识查询或订阅进度。初始响应返回 jobId、当前状态和后续链接；状态可以包含 queued、running、succeeded、failed、cancelled 等业务含义。状态转换必须由权威存储约束，例如成功和失败都属于终态，不能因为迟到的 worker 回报而从终态退回运行中。',
        'AWS 的幂等 API 工程文章用 client request ID 区分调用意图，并专门讨论相同 ID 却表达 different intent、迟到请求和 unknown outcome。应用可据此保存键、规范化请求摘要以及已创建资源：摘要相同则回放原资源，摘要不同则报告冲突。键的作用域、保留期与过期响应不是该文章替所有系统规定的标准，而是本系统根据最大重试、迟到到达和审计窗口自行定义并验证的策略。',
        '取消也是状态机事件而非删除记录。服务可以接受取消意图，并在 worker 的安全点停止后续步骤，但已提交的数据库事务、已发出的邮件或远端模型请求未必能撤销。接口应区分 cancellation requested 与 cancelled，保留审计信息，并说明可能留下的不可逆副作用；这为后续流式断线和异步任务课程建立共同语义。',
      ],
      keyPoints: ['长任务用可查询资源表达而非绑死单次连接', '幂等键要持久化请求指纹与既有结果'],
      sourceIds: ['res-backend-rfc9110', 'res-backend-openapi', 'res-backend-aws-idempotent-apis'],
    },
    {
      id: 'evidence-review',
      title: '把证据边界写进工程决策',
      paragraphs: [
        '规范资料适合回答协议“允许什么、要求什么”，却不会提供本服务的容量和错误预算。RFC 能校准 HTTP 术语，OpenAPI 能校准文档结构，但它们不能证明某个框架默认配置足以生产使用。设计评审时应把规范主张、产品文档、研究结论和本地实验分别标注，避免用一个权威链接替代自己的验证。',
        '中文开源课程可以降低入门成本，帮助学习者找到 API、数据库和部署示例，但项目实现不等于通用规范。示例可能省略多租户隔离、迁移、限流和故障恢复，因此适合用来建立直觉或练习导航，不应单独承担本课关于 HTTP 语义、契约兼容和可靠性边界的考核结论。核心判断仍回到开放规范与可重复实验。',
        '一次可评审的接口设计应附上假设清单：预期调用者、最大 payload、超时、并发、状态保留期、敏感字段、幂等作用域和依赖边界；再列出验证方式，包括契约测试、状态转换测试、重复请求注入和错误响应快照。证据不是文档末尾的装饰，而是说明每个承诺如何被证明以及尚未证明什么。',
      ],
      keyPoints: ['不同来源承担不同强度的工程主张', '设计承诺必须配套可执行验证与限制说明'],
      sourceIds: ['res-backend-openapi', 'res-backend-datawhale'],
    },
    {
      id: 'contract-review',
      title: '用契约评审连接产品与实现',
      paragraphs: [
        '评审可以从三类读者依次进行。产品人员检查状态与错误是否能支撑用户旅程，客户端开发检查每种响应能否做出确定动作，后端与运维检查承诺是否能由事务、容量和观测证明。三方若对“已接受”“进行中”或“已取消”理解不同，应先修订术语表与状态机，再讨论框架代码。',
        '为每个 operation 写出前置条件、成功后置条件和失败不变量。例如创建报告成功后一定存在可授权查询的 job；配额不足时不应产生模型调用；不同请求指纹复用幂等键必须冲突。随后把条件转成自动测试和数据库约束，让规范中的句子拥有运行时证据。',
        '最后执行向后兼容演练：用旧客户端调用新服务，用新 worker 消费旧消息，并在滚动发布中保留两种版本。记录未知枚举、可选字段、默认值和错误码的处理结果。兼容不是一次静态审核，而是每次接口或 schema 变化都要重复的验证流程。',
      ],
      keyPoints: ['从不同调用者视角检查同一状态语义', '把前置条件和不变量转为自动测试与约束'],
      sourceIds: ['res-backend-openapi', 'res-backend-rfc9110'],
    },
  ],
  misconceptions: [
    { claim: '有了 OpenAPI 文件，客户端就一定兼容。', correction: 'OpenAPI 只描述接口表面，还需要防漂移检查、真实契约测试和版本迁移策略。' },
    { claim: 'POST 不能设计成幂等操作。', correction: 'HTTP 没有默认保证 POST 幂等，但应用可以用持久幂等键约束重复创建的业务效果。' },
    { claim: '返回 202 就表示后台任务一定完成。', correction: '202 仅表示请求被接受处理；任务仍可能排队、失败、取消或需要人工对账。' },
    { claim: '429 后按 Retry-After 重试就不会过载。', correction: '等待提示不能创造容量，客户端仍需重试预算与抖动，服务端仍需准入和公平性控制。' },
    { claim: '取消 job 等同于撤销全部副作用。', correction: '取消通常是协作式意图，已提交事务和远端动作可能不可逆，必须记录并对账。' },
  ],
  recap: [
    '先用不变量、事实所有权和副作用位置定义服务边界。',
    'HTTP 方法、状态码和错误结构共同构成客户端行为契约。',
    'OpenAPI 让接口表面可检查，但不会自动保证实现兼容或可靠。',
    '长任务应建模为可查询、可审计的资源状态机。',
    '幂等键需要明确作用域和请求指纹；保留期与持久化位置由系统按实际重试、迟到和审计窗口确定。',
    '规范、项目文档和课程资料要按证据强度分别使用。',
  ],
  nextStep: '下一步先为研究报告服务画出上下文图和 job 状态机，再写一份包含成功、校验错误、冲突、限流和服务器故障响应的 OpenAPI 草案。随后用三个实验验证边界：重复提交相同幂等键、用不同 payload 复用同一键、在创建成功后模拟连接断开。记录每次客户端看到的响应、数据库事实和可恢复路径，作为进入流式协议课程的基线。邀请一名前端开发只阅读规范完成调用伪代码，收集其无法确定的分支并修订契约，同时保存评审决定及其证据。',
  tests: {
    status: 'passed',
    commands: ['node --test tests/backend-engineering-data.test.js', 'npm test'],
    results: [
      { command: 'node --test tests/backend-engineering-data.test.js', exitCode: 0, summary: '9 项目标数据测试通过。' },
      { command: 'npm test', exitCode: 0, summary: '全量测试通过。' },
    ],
  },
});
