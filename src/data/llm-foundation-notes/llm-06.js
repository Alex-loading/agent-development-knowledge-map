export const llm06Note = {
  readingMinutes: 36,
  introduction: '上一课讨论了预训练、SFT、偏好优化与 LoRA 如何改变模型参数；本课从“训练已经完成、参数保持不变”这一刻继续追踪一次生成。Transformer 输出头先为整个词表产生 logits，解码规则把这些分数变成下一个 token，停止条件决定何时结束；推理服务同时把输入处理拆成 prefill、把逐 token 生成拆成 decode，并以 KV Cache 换取更少的重复计算。理解这条链不是为了背一组万能参数，而是为了把质量、复现性、输出长度、首 token 延迟、生成速度、显存与并发放进同一份可测量决策。本章还会严格区分请求内 KV Cache、跨请求前缀缓存与最终结果缓存，并对照站内 sampling 实验的真实代码边界。学完后，你应能从 logits 讲到采样与终止，诊断 P95 延迟，解释 KV Cache 为什么提速又为何挤压并发，并提交至少六次真实输出的受控对比表。',
  sections: [
    {
      id: 'logits-softmax-next-token',
      title: '从 logits 到下一个 token：分数、概率与局部选择',
      paragraphs: [
        '问题：模型完成一轮前向计算后，究竟交给解码器什么？语言模型输出头为词表中的每个候选 token 产生一个 logit，也就是 softmax 之前、尚未归一化的实数分数。logit 可以为正、为负或彼此接近，它本身不是概率，数值也不要求落在零到一之间。Softmax 对整组分数取指数并除以指数和，把它们转成非负且总和为一的相对概率；实现常先减去本组最大 logit 再取指数，以改善数值稳定性，但不会改变理论上的概率比例。',
        '自回归生成把这一步放进循环：根据当前可见前缀得到下一位置的 logits，经过允许的解码处理选择一个 token，把它追加到前缀，再计算下一步。贪心解码（greedy decoding）在每一步选择当前概率最大的 token，采样则按处理后的概率随机抽取。两者都只在当前前缀下作下一步决策；逐步 argmax 不等于在所有可能完整序列中找到全局概率最高的序列，更不等于输出事实正确。模型知识、上下文证据和解码规则分别影响结果，不能靠“选择最高分”把错误分布修成正确知识。',
        '可以用一个明确构造、并非真实模型测量的例子检查概念：三个候选的 logits 为 2、1、0 时，softmax 会保持第一项概率最高，但不会把分数直接解释成百分之二、百分之一和零。若贪心选第一项，下一轮还会基于新增 token 产生一组全新的 logits；若按分布采样，低概率项仍可能被抽到。工程记录应至少保存模型与版本、完整输入、主要解码配置、终止原因和输出，才能判断变化来自输入、模型还是选择过程。',
      ],
      keyPoints: [
        'Logits 是 softmax 前的未归一化词表分数，概率来自对整组分数的共同归一化。',
        '贪心逐步选择当前 argmax，不保证整条序列全局最优，也不保证事实正确。',
      ],
      callout: {
        kind: 'boundary',
        title: '局部最可能不是全局正确',
        body: '解码器只能在模型当前给出的分布上选择；如果证据、能力或分布本身有问题，贪心和采样都不会自动完成事实核验。',
      },
      sourceIds: ['res-rasbt', 'res-hf-llm'],
    },
    {
      id: 'temperature-and-greedy-boundary',
      title: 'Temperature：改变分布形状，但不许把零塞进公式',
      paragraphs: [
        '问题：temperature 怎样改变同一组 logits？对严格大于零的温度 T，可写成 p_i=softmax(z_i/T)。T=1 是不额外缩放的基准；T 大于 1 时各 logit 差距被缩小，概率通常更平；0 小于 T 且 T 小于 1 时差距被放大，概率通常更尖。这里的“通常”针对同一组有限 logits 的形状变化，不表示高温必然更有创意、低温必然更正确，也不表示温度会增加模型没有学到的知识。',
        'T=0 不能代入 z_i/T，因为除以零没有定义。Raschka 的教学实现会把 nonpositive temperature 分支直接处理为 argmax，这是一项清楚可读的实现选择，不是所有推理接口必须遵守的数学定律；其他库或服务可能拒绝、钳制或专门处理零值。即使都采用贪心，并列最大值怎样打破平局、数值精度怎样影响排序，也取决于实现。因此面试回答应把“零温常被接口用于确定性路径”与“公式只对 T>0 成立”分开。',
        '低温与 seed 只能提高特定条件下的复现性，不能承诺跨模型、模型版本、服务后端、硬件和并发调度绝对一致。某些接口会把 seed 描述为尽力复现，同时保留后端变化的边界；另一些接口可能根本没有该参数。实验要记录 seed，但仍要重复运行并保存模型版本。对于代码生成，应把编译与测试当质量门；对于结构抽取，应把解析和 Schema 校验当质量门；对于创意标题，应由多样性与人工偏好指标决定。任何任务都不能只凭某个固定温度数值上线。',
      ],
      keyPoints: [
        '温度公式只对 T>0 定义：高于一通常更平，低于一通常更尖，T=1 是基准。',
        '零温、并列最大和 seed 的行为都有实现边界；低随机性只能改善复现性，不能替代任务验证。',
      ],
      sourceIds: ['res-rasbt', 'res-hf-llm', 'res-openai-cookbook'],
    },
    {
      id: 'top-p-and-combined-sampling',
      title: 'Top-p：用动态概率质量限制候选集合',
      paragraphs: [
        '问题：top-p 与 temperature 的控制对象有何不同？Nucleus sampling（核采样）先按概率从高到低排列候选，取累计概率达到阈值 p 的最小前缀，丢弃集合外候选，再对保留概率重新归一化并从中采样。候选数会随每一步的分布动态变化：分布很尖时少量 token 就可能覆盖阈值，分布较平时则可能需要更多 token。因此 top-p 不是固定保留前多少个词，也不是把每个候选概率单独与 p 比较。',
        '一条便于教学的组合链是“temperature 缩放 logits→softmax→按概率排序形成 top-p 最小前缀→对保留集合重新归一化→采样”。它让我们分别看到分布形状和候选范围，但生产服务还可能应用禁用词、重复惩罚、token bias、最小长度等其他 logits processors；这些处理器是否存在及顺序如何，必须查所用库或接口契约。某些供应商还建议主要调整 temperature 或 top-p 中一个，这只是一项具体接口建议，不能推广成所有模型的普遍最佳实践。',
        '比较 top-p 时要固定原始 logits 所对应的模型、输入和其他设置，逐步改变一个变量并重复运行。若服务没有返回 logits 或 logprobs，就只能观察最终输出和任务指标，不能声称“看见了真实候选核里有哪些 token”；更不能从一次文本倒推出完整分布。可记录重复率、类别覆盖、编译通过、Schema 通过、事实错误等可观察指标，但不要把“不同措辞更多”直接当成质量更高。参数只是实验变量，结论必须绑定任务集、模型版本和运行条件。',
      ],
      keyPoints: [
        'Top-p 保留累计概率达到阈值的最小动态前缀，丢弃外部候选后要重新归一化再采样。',
        'Temperature 与 top-p 的组合顺序及其他 logits processors 具有接口边界，不能假设所有服务完全相同。',
      ],
      callout: {
        kind: 'intuition',
        title: '一道会随分布伸缩的门',
        body: 'Top-p 像按总概率质量开门：尖分布只需少数候选，平分布可能放入更多；类比不表示阈值直接等于候选数量。',
      },
      sourceIds: ['res-hf-llm', 'res-openai-cookbook', 'res-rasbt'],
    },
    {
      id: 'stop-max-output-and-termination',
      title: '停止不是一种信号：EOS、stop、上限与业务完成',
      paragraphs: [
        '问题：生成循环何时真的结束？模型可能生成 EOS（end-of-sequence，序列结束）token；调用方也可能配置 stop 字符串或 token 序列、最大输出 token 数，或在应用层检测结构与业务状态。EOS 来自模型词表与生成分布，stop 是服务或解码器施加的匹配规则，最大输出是硬预算，业务终止则由可信代码判断“所需字段是否齐全、工具目标是否完成、是否应交给人工”。这些机制可以共同出现，却不能互相冒名。',
        'Stop 的支持范围、按字符串还是 token 匹配、命中片段是否包含在返回文本中，以及终止原因字段怎样命名，都属于供应商和接口语义。最大输出上限只保证达到边界时停止，不保证句子、代码块或 JSON 恰好完整；截断可能留下缺括号、缺字段或语义未完的结果。反过来，stop 只识别预先约定的边界，也不能证明输出符合 Schema、字段具有业务意义或模型没有在边界前生成错误内容。',
        '可靠链路会同时记录请求的停止配置、实际生成长度、终止原因、解析结果与业务校验结果。对于结构化抽取，可以使用结构约束或 Schema 能力降低格式错误，再由服务端执行解析、Schema 和业务三层校验；达到最大长度、未出现预期终止或校验失败都进入有限重试或降级，而不是盲目继续生成。这个边界直接通向下一课：停止条件管理“何时不再产生 token”，Prompt 与 Schema 管理“应产生什么结构”，可信代码负责判断是否可以使用。',
      ],
      keyPoints: [
        'EOS、stop、最大输出和应用层业务完成是四类不同终止机制，应分别记录和处理。',
        '最大输出可能截断 JSON，stop 也不能替代 Schema 与业务校验。',
      ],
      callout: {
        kind: 'warning',
        title: '停止了不等于完成了',
        body: '一次响应因命中 stop 或长度上限结束，只说明生成循环停下；是否完整、合法且可执行仍由解析、Schema 与业务规则判断。',
      },
      sourceIds: ['res-hf-llm', 'res-openai-cookbook'],
    },
    {
      id: 'prefill-decode-and-latency-metrics',
      title: 'Prefill 与 decode：先拆阶段，再诊断延迟',
      paragraphs: [
        '问题：一次请求的时间都花在哪里？Prefill 处理整段输入上下文，让各层为已有位置计算表示及初始 K/V；它能利用输入位置上的并行计算。Decode 随后自回归地逐 token 追加输出，每一步依赖刚生成的 token，计算当前 token 的新状态并读取历史缓存。输入更长会增加 prefill 工作，输出更长会增加 decode 步数，但真实耗时还受模型、硬件、批处理、内存访问和服务调度影响，不能把字符数直接当延迟。',
        '首 token 延迟（Time to First Token，TTFT）从客户端发起请求到收到首个输出 token，它不等于纯 prefill：网络传输、鉴权与路由、排队、冷启动和服务端调度也可能包含其中。首 token 之后可用 token 间延迟（Inter-Token Latency，ITL）或每输出 token 时间（Time Per Output Token，TPOT）观察生成节奏；端到端延迟还包含完整输出长度与客户端处理。流式返回能改善用户感知，却不必然缩短模型完成整段输出的时间或总成本。',
        'P95 是一组延迟观测按从小到大排列后，约九十五百分位请求不超过的分位值；具体统计窗口、插值方法和样本过滤必须写进监控定义。诊断 P95 突升时，不应只看全局平均，而要按输入 token、输出 token、并发、模型与版本、地区、队列、冷启动和 cache hit 分桶，再对比 TTFT 与 ITL/TPOT。只有六次教学输出既不能稳定估计尾延迟，也不能代表生产流量；实验表可以记录六次耗时作案例，但 P95 需要足够样本和代表性分布。',
        '优化也应与阶段对应：减少无关输入、改进前缀复用或路由可能主要改善 prefill 与排队；限制无效输出、选用合适模型、调度批处理或优化内存访问可能影响 decode、吞吐和成本。量化、批处理、流式、模型路由和并发限制都有质量或资源取舍，不能只报“平均快了”。上线门槛应同时包含任务质量、TTFT/ITL/端到端的分位数、错误率、吞吐与单请求成本，并在同一流量切片下比较。',
      ],
      keyPoints: [
        'Prefill 处理整段输入并建立初始各层 K/V，decode 逐 token 追加并读取历史状态。',
        'TTFT 含网络、排队、路由等服务时间；P95 应按长度、并发和缓存命中等维度分桶诊断。',
      ],
      sourceIds: ['res-hf-llm', 'res-openai-cookbook'],
    },
    {
      id: 'kv-cache-reuse-memory-and-concurrency',
      title: 'KV Cache：用显存保存历史计算，而不是答案',
      paragraphs: [
        '问题：为什么 decode 不必在每一步重算完整前缀？在因果 self-attention 中，过去位置在某一层产生的 Key 和 Value 一旦算出，后续 token 仍可复用。KV Cache 为每层保存这些历史 K/V；新 token 依然要通过每一层产生当前 Query、Key、Value，再用当前 Query 读取缓存中的历史与当前 K/V。缓存没有保存 Query，也没有存“正确答案”，更没有取消注意力计算；它减少的是历史位置 K/V 投影等重复工作。',
        '缓存容量可用一个缩放关系建立直觉：“2 × layers × active sequences × cached length × KV heads × head dimension × bytes”。二代表 K 与 V，layers 是层数，active sequences 是并发活动序列，cached length 是已缓存长度，其余项描述每层 K/V 头及数值宽度。这不是精确显存公式：padding、预分配、分页、碎片、对齐、临时张量、量化、分片和运行时元数据都会改变实际占用。Grouped-Query Attention（GQA）、Multi-Query Attention（MQA）或滑动窗口等变体也会改变可缓存头数或历史范围，必须按具体架构核算。',
        '因此在固定显存下，单请求上下文更长通常会让缓存占用增大，并可能挤压可同时服务的请求数；这是需要以实际服务器、批处理与调度策略测量的工程推论，不是对所有实现的硬保证。分页 KV Cache 可减少预留浪费和碎片，量化可降低每元素字节数，调度可在吞吐与尾延迟间取舍，但它们都不会让内存成本归零。面试解释“长上下文为何降低并发”时，应沿“每层每活动序列保存更长 K/V→显存余量下降→可驻留序列可能减少→排队或吞吐变化”回答，并补充实测边界。',
        '还要区分三类经常被统称为缓存的东西。KV Cache 服务单个活动请求的逐 token decode；prompt 或 prefix cache 在服务规则允许时跨请求复用完全相同的前缀计算，命中条件、TTL、模型版本和租户隐私必须明确；result cache 直接保存最终响应，需要定义 key、租户隔离、权限、时效与失效规则。三者都不是按语义相似度搜索文档的 RAG。前缀稍有不同就可能不命中，结果缓存也不能在用户权限或实时数据变化时盲目复用。',
      ],
      keyPoints: [
        'KV Cache 保存各层历史 Key/Value；新 token 仍产生当前 Q/K/V 并执行注意力。',
        '缓存随层数、活动序列、长度、KV 头、头维度与字节数缩放，实际占用还受运行时实现影响。',
        '请求内 KV Cache、跨请求前缀缓存和最终结果缓存的命中、隐私与失效语义完全不同。',
      ],
      callout: {
        kind: 'boundary',
        title: '公式是容量尺，不是显存账单',
        body: '缩放关系适合比较长度、并发和 KV 头变化；精确容量规划仍要读取架构与服务实现，并在真实并发、分页和分片配置下测量。',
      },
      sourceIds: ['res-hf-llm', 'res-rasbt', 'res-openai-cookbook'],
    },
    {
      id: 'sampling-experiment-and-serving-tradeoffs',
      title: '采样实验：先读懂教学面板，再验证真实服务',
      paragraphs: [
        '问题：站内“采样分布台”实际证明了什么？源码固定了“巴黎、法国首都、伦敦、不确定、一首诗”五个候选及其构造 logits，本地 sampleDistribution 把 temperature 限制在 0.05 到 2、top-p 限制在 0.05 到 1，先减最大 logit 做稳定 softmax，再按概率降序标记达到阈值的最小候选前缀。界面展示所有候选的温度后概率和是否进入 nucleus；它不会远程调用模型，不会随机生成文本，也没有执行 top-p 后的重新归一化与抽样。因此面板适合观察“温度改变分布、top-p 改变候选成员”，不代表某个真实模型词表，更不是六次真实输出。',
        '“稳定答案预设”和“创意写作预设”只是操作入口，其中数值属于构造教学选择，不是生产推荐或 benchmark。先在面板固定 top-p、移动 temperature，记录排序、概率和集合变化；再固定 temperature、逐步收窄 top-p，观察动态前缀。注意界面即使把候选标为排除，仍展示 temperature softmax 后、尚未按保留集合重归一化的原概率；真实核采样还需在保留集合内重归一化并实际抽样。用这一步建立机制直觉后，才进入真实模型实验。',
        '真实服务实验先固定模型与版本、messages、stop、最大输出、提示版本及除研究变量外的其他设置，对同一提示在较低与较高温度条件下分别运行至少三次，合计不少于六次；再另建一组固定 temperature、逐步改变 top-p 的对照。每次记录输出、seed、输入输出 token、终止原因、错误类型、TTFT 和端到端耗时；seed 只作条件记录，不写成确定性保证。若接口不返回 logits/logprobs，只能报告输出与任务指标，不能声称真实候选集合按预想变化。',
        '最终为代码生成、创意标题和结构化抽取各写一份参数决策，但不预填通用数值：代码记录编译、测试与功能正确率，标题记录约束满足、重复与人工偏好，抽取记录解析、Schema 与业务校验。可用模板提交：“在固定___下，我只改变___；重复___次后，输出差异与错误类型为___。该结果仅适用于模型/版本___和样本集___。下一步保留___，调整___，并以___作为上线门槛。”若你还能从 logits 讲到 temperature、top-p、停止条件，分别解释 TTFT 与 decode，并用缓存缩放关系说明并发代价，就满足本课两道测验、三道面试题及完成标准。',
      ],
      keyPoints: [
        '站内面板是固定候选与本地分布计算，只标记 nucleus，不调用模型、不生成文本、不展示重归一化后的采样结果。',
        '真实实验一次只改一个变量、重复运行并记录版本与终止；三类任务分别用编译测试、偏好指标和 Schema 校验判断质量。',
      ],
      callout: {
        kind: 'example',
        title: '六次输出只是交付起点',
        body: '至少六次运行足以练习受控比较和错误分类，却不足以估计生产 P95 或宣布通用最佳参数；正式决策仍需代表性样本、重复试验和质量门槛。',
      },
      sourceIds: ['res-rasbt', 'res-hf-llm', 'res-openai-cookbook'],
    },
  ],
  misconceptions: [
    {
      claim: 'Logit 就是概率，最大的 logit 也一定对应事实正确的完整答案。',
      correction: 'Logit 是 softmax 前的未归一化分数；逐步选择当前最大项只是在模型分布上作局部决定，不提供事实验证或整序列全局最优保证。',
    },
    {
      claim: 'Temperature 可以取零直接代入 softmax(z/T)，低温加 seed 后就能跨环境绝对确定。',
      correction: 'T=0 不能代入除法，接口会另行拒绝或特判；低温和 seed 只能在特定模型、版本与服务条件下提高复现性。',
    },
    {
      claim: 'Top-p 等于固定保留前 p 个 token，或者把每个候选概率与 p 单独比较。',
      correction: 'Top-p 按概率降序取累计质量达到阈值的最小动态前缀，再对保留集合重新归一化后采样。',
    },
    {
      claim: '命中 stop 或最大输出上限就说明答案结构完整、业务已经完成。',
      correction: '这些条件只终止生成；上限可能截断 JSON，stop 也不验证 Schema 和业务含义，仍需可信代码检查。',
    },
    {
      claim: 'TTFT 就是纯 prefill 时间，用六次实验耗时即可计算可靠的生产 P95。',
      correction: 'TTFT 还可能含网络、排队、路由与冷启动；P95 需要足够且有代表性的样本，并按长度、并发和缓存命中等分桶。',
    },
    {
      claim: 'KV Cache 保存 Query 或最终答案，并且会取消注意力计算；所有缓存也都等同于 RAG。',
      correction: 'KV Cache 保存每层历史 K/V，新 token 仍计算当前 Q/K/V 并读取它们；前缀缓存、结果缓存与语义检索的命中和安全边界各不相同。',
    },
  ],
  recap: [
    '输出头为词表生成未归一化 logits，softmax 才把整组分数转成概率；解码始终在当前模型分布上选择。',
    '对 T>0，temperature 通过 z/T 改变分布形状；T=0 需要实现特判，低温和 seed 都不是绝对确定保证。',
    'Top-p 取累计概率达到阈值的最小动态前缀，丢弃外部候选后重新归一化并采样。',
    'EOS、stop、最大输出与应用业务终止职责不同；停止生成不能替代结构和业务校验。',
    'Prefill 处理整段输入并建立初始 K/V，decode 逐 token 追加；TTFT、ITL/TPOT 与端到端延迟要分开观测。',
    'P95 必须定义统计口径并按输入输出长度、并发、队列与 cache hit 分桶，少量教学运行不能代表尾延迟。',
    'KV Cache 保存每层历史 K/V 以减少重复计算，但占用随长度和活动序列等因素增长，固定显存下可能挤压并发。',
    '请求内 KV Cache、跨请求精确前缀缓存和最终结果缓存不是同一种机制，也都不等于语义检索。',
    '站内 sampling 面板只展示固定候选的本地温度概率与 nucleus 成员；真实服务实验需固定配置、一次改一个变量并重复验证。',
  ],
  nextStep: '本章把一次生成拆成了“logits→概率与候选控制→token 选择→终止”，又把服务过程拆成 prefill、逐 token decode、KV Cache 与延迟并发指标。下一课将把这些可调推理能力包装成可靠接口：Prompt 如何写清任务、指令与不可信数据的边界，少量示例如何减少歧义，Schema 如何约束结构，以及解析、业务校验、有限重试和降级怎样保证即使生成因 stop 或长度上限结束，系统也不会直接使用不完整或不合法的结果。',
};
