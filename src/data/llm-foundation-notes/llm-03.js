export const llm03Note = {
  readingMinutes: 28,
  introduction: '上一课沿一次训练迭代说明了参数怎样被损失、梯度和优化器改变；本课把视线移到模型实际接收的语言输入。文本不会原样进入网络，而要先被 tokenizer 切分并变成 token ID，再由 embedding 映射为向量；模型还需要位置相关信息来区分先后顺序。到了应用层，这些 token 又构成一项有限预算：系统指令、用户输入、历史消息、检索证据与待生成输出都要在目标模型和接口的限制内协同安排。本章会把 token、ID、初始向量、上下文化表示和检索向量逐层分开，并用一个客服 Agent 实验比较截断、摘要与检索的取舍。学完后，你应能用匹配目标模型的 tokenizer 实测用量，为真实请求预留输出空间，并用对照评测而不是“上下文越多越好”的直觉决定材料策略。',
  sections: [
    {
      id: 'token-and-tokenizer',
      title: 'Token 与 tokenizer：模型看到的不是字符或词',
      paragraphs: [
        '问题：一句自然语言怎样成为模型可处理的离散输入？Tokenizer 是把文本编码成 token 序列并映射为整数 ID 的组件，token 则是其词表和编码规则决定的处理单位。它可能对应一个常见词、词的一部分、标点、汉字或更小的字节片段，所以 token 既不是“字符”的固定别名，也不是“单词”的固定别名。只有把 tokenizer 和模型版本一起说明，token 数才有明确口径。',
        '编码结果受词表、合并规则和原始文本共同影响。同一句话换用另一个 tokenizer，边界、ID 和总数都可能改变；空格、大小写、标点、JSON 结构、代码符号与罕见字符也可能造成不同切分。对于使用子词或字节片段的编码器，不熟悉的片段往往需要更多单位表示，但不能据此发明一个跨语言恒定换算率。可靠做法是保存原文并用目标模型对应的编码器实测，而不是按字符数或英文词数猜测。',
        'tiktoken 提供了观察 BPE 编码、token ID 与解码结果的实用工具，适合做本课实验；但对一段纯文本的本地计数，不必然等于一次完整 API 请求的计费或窗口计数。消息角色、边界标记、工具定义、结构化字段等接口包装也可能占用 token，且具体口径与接口版本有关。工程上应优先使用目标服务的官方计数说明或请求用量记录，并把本地估算和服务端实际值做校准。',
      ],
      keyPoints: [
        'Token 边界由具体 tokenizer 的词表与编码规则决定，不能用字符或自然语言单词固定换算。',
        '预算与计费要匹配目标模型和接口；纯文本本地计数只是可校准的组成部分。',
      ],
      callout: {
        kind: 'boundary',
        title: '不要记跨语言固定比例',
        body: '“一个汉字等于多少 token”或“一个英文单词等于多少 token”都不是稳定定律；文本、编码器与接口口径变化后，比例也会变化。',
      },
      sourceIds: ['res-tiktoken', 'res-hf-llm', 'res-rasbt', 'res-openai-cookbook'],
    },
    {
      id: 'ids-to-embeddings',
      title: '从 ID 到 embedding：数字标签怎样变成可学习表示',
      paragraphs: [
        '问题：整数 ID 为什么不能直接代表语义？ID 只是词表项的离散索引，例如编号相邻的两个 token 并不因此更相似。输入 embedding 通常通过可学习的向量表，把每个 token ID 查成一组连续数值；这些数值才可进入后续矩阵运算。训练目标推动向量形成对预测有用的几何关系，但 embedding 不是人工编写的字典释义，也不能把某一维直接命名为一个稳定的人类概念。',
        '还要区分“初始 embedding”与“上下文化表示”。同一个 token ID 在查表时可以获得相同的初始向量，但它随后会与位置相关信息及周围 token 经过多层网络交互，形成依赖当前句子的表示。因此，一个词在不同语境中的作用不必由一枚永久不变的坐标完整表达。面试回答“embedding 为什么能表达语义”时，关键不是说模型存了一本释义表，而是说明训练会保留有利于预测的关系，后续层再结合上下文更新表示。',
        '检索增强生成也常使用 embedding，但检索向量与生成模型内部的 token embedding 不能默认是同一个模型、同一维度或同一向量空间。检索模型通常把查询和文档片段映射到适合相似度搜索的表示，再返回候选证据；相似度只是排序信号，不保证候选一定与问题相关、事实正确或足以作答。工程上必须分别记录所用 embedding 模型、切分方法和检索评测，不能把“向量相近”直接改写成“答案可信”。',
      ],
      keyPoints: [
        'Token ID 是离散索引，输入 embedding 才是可学习的连续初始表示。',
        '初始 token embedding、网络中的上下文化表示与 RAG 检索 embedding 是不同层次的对象。',
      ],
      callout: {
        kind: 'intuition',
        title: '索引、起点与沿途状态',
        body: '可以把 token ID 看作目录编号，把初始 embedding 看作进入网络的数值起点，把上下文化表示看作结合当前位置和周围内容后的沿途状态；这个比喻不表示向量维度能逐项翻译成人类概念。',
      },
      sourceIds: ['res-hf-llm', 'res-rasbt', 'res-3b1b-transformer', 'res-ms-genai'],
    },
    {
      id: 'positional-representation',
      title: '位置表示：相同 token 也要知道先后关系',
      paragraphs: [
        '问题：如果输入只有 token 的初始向量，模型怎样区分“用户退款”和“退款用户”这样的次序差异？Token embedding 说明当前词表项的数值起点，位置表示则向计算提供顺序或相对位置线索。位置相关信息与内容表示共同进入后续层后，模型才有机会学习“谁在前、谁在后、相隔多远”对当前预测的影响；它并不是在文本外另存一份自然语言语法规则。',
        '位置表示不是只有一种实现。不同 Transformer 可以使用固定或可学习的绝对位置信息，也可以通过相对位置关系或其他变换把距离与顺序带入注意力计算；具体模型还可能有不同的组合与长度处理方式。因此，本课只保留稳定机制：无排列感知的内容向量不足以表达序列顺序，模型必须获得位置相关信号；不能从某个教程的实现推断所有模型都采用同一种绝对位置编码。',
        '位置机制还提示了一个工程边界：模型在某个长度范围内训练或配置出来的行为，不能仅凭“接口允许放入”就推断它会同等可靠地利用每个位置。应用应在真实任务上按证据位置、上下文长度和文档顺序切片评测，而不是把窗口容量当作理解质量保证。下一节会把这种位置与容量问题转成可计算的请求预算。',
      ],
      keyPoints: [
        '内容 embedding 与位置相关信号共同支持模型区分序列顺序和距离关系。',
        '位置表示有多种实现，不能把一种教程代码推广成所有 Transformer 的固定做法。',
      ],
      sourceIds: ['res-hf-llm', 'res-rasbt', 'res-3b1b-transformer'],
    },
    {
      id: 'context-and-shared-budget',
      title: '上下文窗口：为输入和输出共同留出预算',
      paragraphs: [
        '问题：模型在一次请求里究竟“看见”什么？上下文是推理时提供并保持可见的 token 序列，可能包括系统或开发者指令、用户输入、历史消息、检索片段和工具结果。把一份文档放进上下文，只改变这次计算可用的信息，不等于通过训练把文档永久写入参数，也不自动形成跨会话记忆；下一次请求若未再次提供或由系统恢复，模型未必还能看到它。',
        '可把请求预算写成符号约束：“指令 I + 用户输入 U + 历史 H + 检索证据 R + 工具结果 T + 输出预留 O，不超过当前模型与接口按其口径计算的限制 L”。若接口另有独立最大输出限制，还要同时满足该限制。这里故意不用某个当前型号的具体数字，因为模型、版本和 API 规则会变化；真正实施时应读取所用服务的正式限制，并用匹配的 tokenizer 或服务端用量数据计算。',
        '输出预留不能等输入拼完后再考虑。输入占满可用容量时，系统可能报错、截断某部分内容，或者没有足够空间生成完整答案；具体行为应以接口文档和应用自己的截断代码为准。客服 Agent 可以先为必须完整保留的系统规则、当前问题和输出设置硬预算，再把历史与检索放进可调整区域，并为每一部分记录实际 token 数，这样超限时才知道应该压缩哪一项。',
        '容量也不是质量承诺。即使总量小于 L，低相关、错误或误导性材料仍可能分散模型对关键证据的利用；关键事实被放在不同位置时，结果也可能变化。这里应使用“可能”而非普遍因果定律：影响大小取决于模型、任务和材料，必须通过保持问题集不变的对照评测验证。预算设计因此同时服务于质量、输入成本、首 token 前处理时间和可观测性。',
      ],
      keyPoints: [
        '上下文是请求时可见输入，不等于模型参数中的永久学习或天然跨会话记忆。',
        '预算必须同时计算指令、输入、历史、证据、工具结果和输出预留，并服从目标接口口径。',
      ],
      callout: {
        kind: 'example',
        title: '先预留，再装材料',
        body: '像装行李前先留出返程必需空间：先保护系统规则、当前请求和输出额度，再让历史与检索证据竞争剩余预算；类比只说明预算顺序，不表示各部分价值能仅按长度判断。',
      },
      sourceIds: ['res-openai-cookbook', 'res-hf-llm', 'res-ms-genai'],
    },
    {
      id: 'context-strategy-tradeoffs',
      title: '超长材料的四类取舍：截断、分块、摘要与检索',
      paragraphs: [
        '问题：材料超过预算时，应该删、拆、压缩还是搜索？截断按明确规则只保留一部分，成本低、行为容易复现，适合尾部确实较旧或任务只依赖局部内容的情况；代价是被删掉的信息完全不可见。不能默认“只留最近消息”总是正确，因为早期约束、用户承诺或工单编号可能仍是完成任务所必需的状态。截断策略应声明保留优先级，并用缺失关键事实的样例验证。',
        '分块把长材料拆成多个可处理单元，可逐块提取、汇总或分别回答；它避免一次塞入全部文本，却可能切断跨块关系，并引入合并结果的额外步骤。摘要用较短表述保留已知重点，适合压缩对话历史或文档阶段结果，但摘要本身是有损转换：限定条件、否定、数值、出处和少见例外都可能被遗漏。应保留摘要版本与原始材料引用，并设计检查问题检测关键事实是否仍在。',
        '检索按当前问题从外部集合选出候选片段，适合知识量大、更新频繁且每次只需少量证据的场景；它把“选择什么进入上下文”变成可评测组件，但相关性和正确性都不保证。切分、查询改写、embedding、召回与排序中的任一步都可能漏掉关键证据或引入噪声。评测需要同时看候选是否包含必要来源、答案是否忠于候选以及无证据时是否适当拒答，不能只看最终措辞流畅。',
        '四类策略可以组合：用结构化状态保存稳定字段，用检索选择原始证据，用摘要压缩低风险历史，再在明确边界上截断。选择依据不是技术流行度，而是信息损失、可追溯性、时效性、成本与延迟。对同一固定问题集分别运行“基线、截断、摘要、检索”版本，记录关键事实保留、证据引用和错误类型，才能判断额外材料是在帮助还是在制造干扰。',
      ],
      keyPoints: [
        '截断可复现但会永久移除本次输入信息；分块保留局部内容却可能切断跨块关系。',
        '摘要是有损压缩，检索是有失败概率的选择步骤；二者都需要来源追踪和任务级评测。',
      ],
      sourceIds: ['res-openai-cookbook', 'res-ms-genai', 'res-llm-universe', 'res-hf-llm'],
    },
    {
      id: 'customer-service-token-budget-experiment',
      title: '客服 Agent 实验：把 token 预算变成可复现决策',
      paragraphs: [
        '问题：怎样完成一份不仅“测了 token”，还能指导客服系统设计的交付？先固定目标模型与 tokenizer，并建立一组版本化客服问题，至少覆盖普通问答、需要引用制度、依赖早期对话条件、证据冲突和信息不足。另建三组计数材料：意思相近的中英文句子、一段 JSON、一段代码或含罕见符号文本。逐条保存原文、编码器名称、token 数和解码抽查结果，只报告实际测量值，不把本次比例推广到其他语言、文本或模型。',
        '第二步建立预算表，列出 L、I、U、H、R、T 与 O 的目标额度和实测用量，并注明接口是否另设输出限制。系统规则、当前用户问题与安全必要字段设为不可静默截断项；历史和检索材料写清选择、压缩和溢出顺序。若接口提供 usage，就记录服务端返回的输入与输出 token；若不提供，则保留可校准的服务端日志或本地估算，并持续对齐本地估算与服务端计量口径。还要记录总耗时或首 token 延迟代理，以及是否触发截断，才能定位偏差来自估算口径、材料选择还是实际生成长度。',
        '第三步做受控对照，而不是随意多聊几次。对同一问题集依次运行基线、加入无关长文、摘要历史、按问题检索四种条件；除被研究的上下文策略外，尽量固定模型、提示版本与采样设置。逐例记录关键事实是否保留、是否给出正确证据引用、答案是否满足任务标准、输入输出 token、成本估算与延迟代理，并分类“漏证据、误用证据、条件丢失、无依据回答、输出截断”等错误。模型输出有随机性时，应进行重复运行并报告波动，而不是挑一个顺眼结果。',
        '最终提交一张预算表、三组 tokenizer 计数和实验结论。结论可以直接使用这份不预设结果的模板：“在固定问题集与配置下，策略___相对基线把输入 token 从实测___变为___；关键事实保留率、证据引用或人工评分由___变为___，延迟与成本代理由___变为___。观察到的主要错误是___，但样本量、模型版本和计数口径限制了外推。下一版将保留___，修改___，并用___对照复测。”若数据没有显示无关材料造成退化，也要如实记录，不能为了迎合测验而虚构结论。',
      ],
      keyPoints: [
        '实验要固定问题集与主要配置，只改变上下文策略，并同时测 token、证据、质量、成本和延迟代理。',
        '交付报告填写实测结果与限制，不预设“更长必然更差”或捏造任何比较数字。',
      ],
      callout: {
        kind: 'warning',
        title: '对照结论不等于普遍规律',
        body: '一次客服问题集上的结果只支持该模型、提示、材料与评测条件；改变版本或任务后要重新校准，尤其不能把局部 token 比例和噪声影响写成跨系统定律。',
      },
      sourceIds: ['res-tiktoken', 'res-openai-cookbook', 'res-ms-genai', 'res-llm-universe'],
    },
  ],
  misconceptions: [
    {
      claim: '一个 token 永远等于一个汉字或一个英文单词，可以直接用固定比例换算。',
      correction: 'Token 由具体 tokenizer 的词表、规则和输入共同决定；应使用目标模型对应编码器实测，并与接口实际用量校准。',
    },
    {
      claim: 'Token ID 的数字距离越近，两个 token 的语义就越相似。',
      correction: 'ID 只是离散词表索引；可利用的关系来自学习到的 embedding 与后续上下文计算，不来自编号大小。',
    },
    {
      claim: '每个词只有一个永不变化的 embedding，RAG 和生成模型也必然共享同一向量空间。',
      correction: '输入查表向量会被网络上下文化；RAG 检索 embedding 还可能来自另一模型、维度和训练目标，必须分别说明与评测。',
    },
    {
      claim: '所有 Transformer 都使用同一种绝对位置编码。',
      correction: '位置相关信息有固定、可学习、相对关系等不同实现；只能根据具体模型资料说明，不能从单一教程推广。',
    },
    {
      claim: '文档放进上下文后，模型就永久学会了内容，而且窗口没满时材料越多越好。',
      correction: '上下文只在请求时提供可见信息，不自动更新参数；低相关、错误或误导材料可能影响证据利用，大小需通过对照评测确认。',
    },
    {
      claim: '检索或摘要可以无损解决所有超长上下文问题，输出也不需要单独预留空间。',
      correction: '摘要可能遗漏限定与出处，检索可能漏召回或引入错误候选；预算还必须给生成输出留出额度并满足接口限制。',
    },
  ],
  recap: [
    'Tokenizer 依词表与编码规则把文本变成 token ID；token 不是字符或自然语言单词的固定同义词。',
    'Token ID 只是离散索引，初始 embedding 是可学习向量，经过网络后形成依赖当前上下文的表示。',
    'RAG 检索 embedding 与模型内部 token embedding 不必属于同一模型或空间，相似度也不保证事实相关与正确。',
    '位置相关信息让模型区分顺序与距离，但不同 Transformer 的位置表示实现并不统一。',
    '上下文是请求时可见输入而非永久记忆；指令、用户输入、历史、证据、工具结果和输出预留共同消耗预算。',
    '截断、分块、摘要和检索分别承担删除、拆分、压缩与选择，都会引入可评测的信息损失或失败路径。',
    '客服预算实验要固定问题集、匹配 tokenizer、记录输入输出 token、证据和质量指标，并用对照结果决定策略。',
  ],
  nextStep: '本章已经说明离散 token 怎样获得初始向量、位置怎样进入表示，以及应用如何决定哪些 token 能进入一次请求。下一课将继续追踪这些表示：当前位置如何用 Query、Key 与 Value 从可见 token 中选择和聚合信息，缩放、softmax、多头与因果掩码如何组成 Attention，并最终进入完整的 Transformer 数据流。',
};
