export const llm05Note = {
  readingMinutes: 36,
  introduction: '上一课解释了 Transformer 怎样把上下文表示逐层变成下一 token 的 logits，但模型中的投影、注意力和前馈层参数从哪里来，以及为什么基础模型还要经历后训练，仍需要另一张地图。本课不把所有“继续训练”混成一种方法，而是先以目标为坐标：预训练和继续预训练优化语言建模目标，监督微调（SFT）用输入—期望输出对塑造任务行为，偏好优化用相对选择信号进一步调整回答倾向，低秩适配（Low-Rank Adaptation，LoRA）则是承载这些目标的一种参数高效更新方式。随后再把检索增强生成（Retrieval-Augmented Generation，RAG）放到应用层比较：它在推理时提供可更新证据，而不是把知识写进参数。这张坐标系的目的不是给出一条固定流水线，而是让你在任何新需求中先问“要改变什么、证据放在哪里、怎样验证”，避免用训练掩盖资料、检索或接口问题。学完后，你应能按行为、事实更新、引用、权限和风险选择 prompt、RAG、SFT、偏好优化或 LoRA，并用一张可验证的决策表说明为什么选择、如何评测以及何时回滚。',
  sections: [
    {
      id: 'objectives-before-methods',
      title: '先定目标，再选训练或应用方法',
      paragraphs: [
        '问题：面对一个业务需求，为什么不能先问“要不要微调”？因为方法名称没有说明真正的失败。基础模型可能缺少完成任务所需的能力，也可能已经具备能力但不稳定遵循格式；答案可能缺少每天变化的制度证据，也可能只是语气不一致。先把需求写成可观察目标、失败样例和风险切片，才能判断应改变参数中的稳定行为，还是改变每次推理可见的证据与工具。',
        '可以用两条轴建立坐标系。第一条是优化目标：语言建模目标让模型学习预测文本，SFT 目标让给定输入对应期望输出，偏好目标让被选择回答相对不被选择回答更受偏好。第二条是系统位置：prompt 与 RAG 改变推理输入，SFT 或偏好优化改变参数，LoRA 决定以怎样的参数更新预算承载训练目标。LoRA 因而不是与 SFT 并列的独立教学阶段，RAG 也不是一种训练损失。',
        '工程决策从最便宜、最可逆的基线开始：先固定一组代表性样例，测 prompt、上下文和现有模型；若充分证据已进入上下文但稳定行为仍达不到门槛，再评估 SFT 或偏好优化；若核心是新鲜事实、出处、版本或权限，则优先评估 RAG。每条路线都要有相同任务切片、质量指标、训练计算与部署成本、延迟和回滚记录，否则“换方法后更好”只是印象而不是证据。还要预先写清不做训练的对照方案，避免只比较两个昂贵候选而遗漏简单有效的应用层修复。',
      ],
      keyPoints: [
        '先把失败分成基础能力、稳定行为、可更新证据和系统控制问题，再选择方法。',
        '预训练、SFT、偏好优化描述训练目标，LoRA 描述参数更新方式，RAG 描述推理时证据路径。',
      ],
      callout: {
        kind: 'intuition',
        title: '目标是地图，方法是路线',
        body: '同一个“回答不好”可能由不同瓶颈造成；没有基线、失败样例和上线门槛，方法名无法替你完成定位。',
      },
      sourceIds: ['res-hf-llm', 'res-ms-genai', 'res-openai-cookbook'],
    },
    {
      id: 'pretraining-and-continued-pretraining',
      title: '预训练与继续预训练：建立并延续语言建模能力',
      paragraphs: [
        '问题：基础能力怎样进入参数？自回归语言模型的预训练通常让模型依据前文预测下一个 token，在大量序列上反复计算语言建模损失并更新参数。这个目标促使模型学习可用于预测的语言形式、模式和关系，但训练目标本身不是事实数据库接口，也不保证每条输出都有出处、符合某个权限版本或能随外部制度更新。能力来自目标、数据、模型与优化的共同作用，不能只由“数据很多”推出。',
        '继续预训练（continued pretraining）是从已有 checkpoint 继续优化语言建模目标，常用于让数据分布更贴近某个领域或语言。它与 SFT 的分界要看目标函数和样本结构，而不是看是否加载了已有权重：两者都可能从 checkpoint 开始，前者仍围绕连续文本的语言建模信号，后者则把输入与期望回答组织成监督任务。把所有 checkpoint 后的训练都叫微调，会掩盖数据与目标差异。',
        '继续预训练也不能被描述成“可靠灌入可更新知识”。领域文本可能改变模型对术语和模式的处理，但某项事实是否被记住、能否准确召回、是否与新旧版本冲突，必须通过专门评测判断；它不会天然生成可审计引用，也不会自动实施用户权限。若业务要求每日制度更新并给出处，把版本化资料留在外部检索系统通常比依赖参数记忆更可检查、更易撤回。即使选择继续预训练，也应保留通用任务回归与领域留出集，以发现领域适应收益是否伴随原能力退化。',
      ],
      keyPoints: [
        '预训练以语言建模目标建立通用预测能力，不等于构建可审计事实库。',
        '继续预训练与 SFT 应按优化目标和数据结构区分，而不是按是否从 checkpoint 开始区分。',
      ],
      callout: {
        kind: 'boundary',
        title: '参数记忆没有版本与引用接口',
        body: '领域继续预训练可以成为实验路线，但新鲜度、召回、冲突、出处和权限都要另行设计与评测，不能由训练动作自动获得。',
      },
      sourceIds: ['res-stanford-cs336', 'res-rasbt', 'res-hf-llm'],
    },
    {
      id: 'sft-and-behavior-shaping',
      title: 'SFT：用示范塑造“怎样回答”',
      paragraphs: [
        '问题：模型会续写文本，为什么还需要监督微调？监督微调（Supervised Fine-Tuning，SFT）把输入与期望输出组成训练样本，让模型学习指令到回答的任务映射，包括输出格式、语气、拒答范式和领域任务步骤。它主要回答“面对这类输入应怎样表现”，并不因为示例里出现了某个事实，就变成能按版本、出处和权限查询的知识库。',
        '样本结构本身就是训练信号。系统指令、用户消息、助手回答的边界，答案长度、措辞、格式和错误示范都会影响最终行为；数量增加无法补偿目标错位、标签冲突或低质量模板。数据审查因此要同时看内容是否正确和格式分布是否符合真实请求。后训练也不能凭空恢复基础模型未具备的能力：如果模型在充分上下文下仍无法完成必要推理，复制更多同类格式示例未必解决根因，应先以基线确认能力、数据或任务定义究竟缺在哪里。',
        '合格的 SFT 实验要保留未训练的基线，并把训练、验证和最终测试按实体、时间或近重复内容隔离。评测不仅看平均任务分，还要看语言、长度、用户群体、高风险请求与拒答等切片，并运行原有能力的回归集。若上线目标是稳定客服语气，可先用 prompt 建基线；只有语气和格式在代表性样例上持续成为瓶颈，且收益覆盖数据、训练、部署和回滚成本时，才升级到 SFT。',
      ],
      keyPoints: [
        'SFT 用输入—期望输出对塑造指令遵循、格式、语气和任务映射，核心是稳定行为。',
        'SFT 需要基线、隔离数据、关键切片与回归评测，不能凭空补回基础能力。',
      ],
      sourceIds: ['res-hf-llm', 'res-rasbt', 'res-stanford-cs336'],
    },
    {
      id: 'preference-optimization-boundaries',
      title: '偏好优化：相对选择信号与方法边界',
      paragraphs: [
        '问题：已有标准答案示范后，为什么还会收集 chosen 与 rejected？有用性、安全性、风格或信息密度往往难以由唯一标准答案完整表达，偏好数据让标注者或评审器在候选回答之间作相对选择。稳定的共同点是用相对偏好进一步塑造回答倾向；但“偏好优化”不是一种单一算法，也不应把所有方法都统称为完全相同的基于人类反馈的强化学习（Reinforcement Learning from Human Feedback，RLHF）管线。',
        '经典 RLHF 流程常从偏好对训练一个显式奖励模型，再用近端策略优化（Proximal Policy Optimization，PPO）一类方法根据奖励更新策略。直接偏好优化（Direct Preference Optimization，DPO）则直接使用 chosen、rejected 与 reference policy 构造优化目标；reference policy 是用于比较并约束当前策略不要任意偏离的参考策略。DPO 不需要在算法内单独训练显式奖励模型，也不依赖与 PPO 相同的 on-policy rollout 更新环。二者都利用偏好信号，却在中间模型、训练稳定性、计算和实现复杂度上不同；回答面试题时应说明共同目标和结构差异，而不是把名词互换。',
        '偏好标签并非无偏真值。标注群体、说明书、候选生成方式、长度和表达风格都可能系统性影响选择，例如把更长或更自信误当成更好。过度追逐单一偏好目标可能损害回答多样性、置信校准或原任务表现，所以必须记录标注分布，检查不同群体与内容类型的一致性，并用任务集、安全集和回归集共同决定停止点。还应保留标注分歧，而不是把争议样本强压成虚假的唯一答案；偏好优化改变倾向，不提供事实来源或正确性保证。',
      ],
      keyPoints: [
        '偏好优化的稳定共性是用相对选择信号塑造行为，但具体算法和训练管线并不相同。',
        '经典奖励模型加 PPO 路线与 DPO 的直接偏好目标应分开说明，偏好偏差也必须切片评测。',
      ],
      callout: {
        kind: 'warning',
        title: '“更受偏好”不是“在所有维度更好”',
        body: '标注群体、候选长度和风格都会影响偏好；优化前应定义要保留的任务能力、校准和多样性，并用回归集守住边界。',
      },
      sourceIds: ['res-hf-llm', 'res-rasbt', 'res-happy-llm'],
    },
    {
      id: 'lora-as-parameter-efficient-update',
      title: 'LoRA：参数高效的权重更新方式',
      paragraphs: [
        '问题：LoRA 为什么能降低微调负担，又为什么不能说“几乎不用模型资源”？全量微调直接更新基础模型参数；LoRA 属于参数高效微调（Parameter-Efficient Fine-Tuning，PEFT），它冻结基础模型原权重，只在选定模块旁训练低秩矩阵乘积表示的权重增量。训练时可训练参数少于全量更新，通常也减少与这些参数相关的梯度、优化器状态和适配器存储；同一个基础模型还可以配多套小型 adapter。它改变的是更新参数的方式，而不是另造一个训练目标。',
        '因此 LoRA 可以承载 SFT，也可以用于某些偏好优化训练；“使用 LoRA”仍要回答样本、损失和评测分别是什么。基础模型仍需加载并参与前向计算，中间激活、数据处理、训练运行、验证与部署管理也不会消失。推理时可按具体实现合并增量或动态挂载 adapter，但动态切换、并发和版本兼容都属于需要实测的服务工程问题，不能只用 adapter 文件较小推断总成本。',
        'rank 决定低秩更新空间的容量，target modules 决定把增量放到哪些层或投影；选择过窄可能限制可学变化，选择更广或更高 rank 又会增加参数与资源。没有跨模型、跨任务通用的最佳数值，应在相同数据划分和评测集上比较若干候选，同时记录显存、吞吐、质量和回归。LoRA 也不保证等价于全量微调，更不自动消除幻觉、扩展上下文或解决错误标签。',
      ],
      keyPoints: [
        'LoRA 冻结基础权重并学习低秩增量，减少可训练参数、相关优化器状态和 adapter 存储。',
        'LoRA 不是独立训练目标；rank 与 target modules 必须围绕任务质量、成本和回归共同实验。',
      ],
      callout: {
        kind: 'boundary',
        title: '参数高效不等于系统零成本',
        body: '基础模型、前向激活、训练数据、评测和部署仍然存在；LoRA 节省的是特定参数更新与适配存储负担，不是整个训练服务栈。',
      },
      sourceIds: ['res-rasbt', 'res-hf-llm', 'res-happy-llm'],
    },
    {
      id: 'data-quality-bias-and-evaluation',
      title: '数据质量、偏差与评测：训练前先定义证据链',
      paragraphs: [
        '问题：为什么“更多数据”可能让结果更差？训练语料要经过抽取、格式解析、质量过滤、隐私信息处理和去重。精确去重容易解释却可能漏掉改写或模板近似，模糊去重能发现更多近重复，也可能误删有价值的相似样本；阈值本质上是在去重 precision 与 recall 之间取舍。任何过滤器都可能改变领域、语言和群体分布，因此要记录规则、版本和移除比例并抽样审查。',
        '内容之外，样本格式、长度和风格同样是信号。若训练回答普遍冗长、总以固定套话开头，模型可能学习这些模式；若偏好候选来自单一模型或标注群体，系统性偏差会随目标被放大。隐私处理还要覆盖可识别信息的检测、删除或受控使用，而不是把公开抓取等同于可无条件训练。上线风险评估应把数据来源、授权、敏感性和可删除性与模型效果放在同一张记录里。',
        '评测必须从目标反推。先定义成功、拒答与严重失败，再建立任务、语言、长度、群体、时间、权限和高风险切片；训练前跑 baseline，训练后比较目标收益、原能力回归、事实与引用、延迟和成本。对于构造的教学表，任何“高、低、中”都只是待验证假设，不是来源 benchmark；真实决策还需记录样本量、置信区间或重复试验不确定性，并设置未达门槛时的停止与回滚条件。离线通过后仍要用受控流量监测分布漂移和未知失败，且不能用线上平均值掩盖高风险小切片。',
      ],
      keyPoints: [
        '清洗、隐私处理与精确或模糊去重都会改变数据分布，阈值选择必须抽样和切片验证。',
        '训练方案要用 baseline、目标切片、回归、风险、成本与延迟共同评测，教学等级不能冒充 benchmark。',
      ],
      sourceIds: ['res-stanford-cs336', 'res-hf-llm', 'res-openai-cookbook'],
    },
    {
      id: 'rag-finetuning-decision-lab',
      title: '决策实验：为三个业务需求选择可验证路线',
      paragraphs: [
        '问题：怎样完成训练方案决策表而不是凭流行度投票？先为每个用例填写五维：数据是否可获得且可授权，训练计算加部署是否可承受，知识更新频率，是否必须给出可核验出处，错误或偏差风险。再分别写清预训练或继续预训练优化语言建模，SFT 优化示范行为，偏好优化塑造相对倾向，LoRA 以低秩增量承载参数更新，RAG 在推理时提供外部证据。每个格子还应同时写“判断依据、验证指标、失败后动作”，例如可引用性不能只写高或低，而要说明怎样核对引用是否真正支持结论；更新频率也要连接文档发布、索引刷新和旧版本撤回流程。所有成本和效果等级都标“教学假设，待实测”。',
        '三个用例可以形成可验证起点。客服语气先用明确 prompt 与格式校验建立基线，若在多语言、长对话和投诉切片中仍稳定失败，再比较 SFT 或 LoRA 承载的 SFT；每日更新且必须引用的制度问答优先 RAG，因为资料、版本、出处和权限可留在外部系统；稳定的专业分类在标签清楚且样本质量足够时，可比较 prompt baseline 与 SFT/LoRA。这里不预设 GPU 数、固定数据量或收益，最终选择由同一评测集决定。',
        'RAG 与微调也可以组合，而非二选一。RAG 提供可更新证据，SFT 或 LoRA 可训练模型按规定使用证据、输出引用格式并在证据不足时拒答；但 RAG 仍可能切分不当、漏召回、排序错误或把恶意文本带入上下文，微调也不会天然保证引用支持结论。恶意检索内容属于工程安全风险，需要隔离不可信材料、限制材料中的指令影响，并用对抗样例验证。组合方案必须分别测检索命中、证据支持、最终回答、提示注入与权限隔离，才能知道故障发生在检索还是生成。',
        '最终交付一张“目标—成本—用例”表和三条选择证据链。每条都按“失败样例与目标→prompt baseline→候选方法→五维假设→离线切片与回归→小流量观察→上线或回滚”填写，并明确回答三个追问：偏好数据可能怎样受群体、长度或风格影响；LoRA 的 rank 与 target modules 如何通过实验选择；什么场景同时使用 RAG 与微调。若还能解释为什么每日制度题优先 RAG、为什么 LoRA 仍需数据评测，就满足两道测验和本课完成标准。',
      ],
      keyPoints: [
        '行为与格式先建立 prompt 基线，稳定行为瓶颈再评估 SFT/LoRA；频繁更新、引用、版本和权限优先 RAG。',
        'RAG 与微调可以组合，但检索、证据使用、引用、拒答、注入和权限都要分别评测。',
      ],
      callout: {
        kind: 'example',
        title: '构造决策表不是来源基准',
        body: '表中的高低判断只是教学假设。不要填入虚构的 GPU、数据量、成本或质量数字；先定义测量方法，再用自己的模型、数据和流量验证。',
      },
      sourceIds: ['res-ms-genai', 'res-openai-cookbook', 'res-hf-llm', 'res-rasbt'],
    },
  ],
  misconceptions: [
    {
      claim: '只要从已有 checkpoint 继续训练，就都属于 SFT。',
      correction: '分类应看目标与样本结构：继续优化连续文本的语言建模目标是继续预训练，使用输入—期望输出对塑造行为才是 SFT；两者都可能加载 checkpoint。',
    },
    {
      claim: 'SFT 或继续预训练能把制度可靠写入参数，并自动提供最新版本、出处和权限隔离。',
      correction: '参数训练不天然提供可审计检索接口；频繁更新、引用和权限需求通常应先验证版本化 RAG，并对召回与答案分别评测。',
    },
    {
      claim: '所有偏好方法都是同一套 RLHF，DPO 也必须先训练显式奖励模型再做 PPO。',
      correction: '经典 RLHF 常含偏好对、显式奖励模型与 PPO 类优化；DPO 直接用 chosen、rejected 和 reference policy 构造目标，不需要 DPO 算法内的独立奖励模型与同一种 on-policy rollout。',
    },
    {
      claim: 'LoRA 是一种独立训练阶段，不需要基础模型、数据、GPU 或验证，而且一定等价于全量微调。',
      correction: 'LoRA 是低秩参数更新方式，可承载 SFT 或偏好目标；它减少部分训练与存储负担，但基础模型、前向激活、数据、评测和部署仍在，效果需按 rank 与目标模块实测。',
    },
    {
      claim: 'RAG 与微调只能二选一，只要组合使用就能同时保证召回、正确引用和安全。',
      correction: '两者可组合：RAG 供可更新证据，微调塑造使用证据的行为；但漏召回、错误排序、无依据生成、提示注入和权限错误仍需独立控制与评测。',
    },
    {
      claim: '偏好标注代表客观真值，持续优化同一偏好指标只会让模型全面变好。',
      correction: '标注群体、候选来源、长度与风格都会形成系统偏差；过度优化可能损害多样性、校准和原任务表现，必须用切片与回归集共同约束。',
    },
  ],
  recap: [
    '先按基础能力、稳定行为、可更新证据和系统控制定位失败，再选择训练或应用方案。',
    '预训练与继续预训练优化语言建模目标；是否从 checkpoint 开始不能单独区分继续预训练和 SFT。',
    'SFT 用输入—期望输出对塑造指令、格式、语气和任务映射，主要解决“怎样回答”，不是可审计知识库。',
    '偏好优化使用相对选择信号；经典奖励模型加 PPO 与 DPO 的直接偏好目标属于不同管线，不能混写。',
    'LoRA 冻结基础权重并学习低秩增量，是参数高效更新方式，可承载不同训练目标但不消除数据、评测和部署成本。',
    '数据抽取、过滤、隐私和精确或模糊去重都会改变分布；样本格式、长度和标注群体也是训练信号。',
    '行为或格式先测 prompt 基线，频繁更新、出处、版本与权限先测 RAG；二者与 SFT/LoRA 可组合但要分层评测。',
    '训练方案决策表的高低项只是教学假设，真实上线需要统一评测集、关键切片、回归、成本延迟和回滚门槛。',
  ],
  nextStep: '本章建立了从语言建模、SFT、偏好优化到 LoRA 与 RAG 的目标坐标系，也说明任何参数更新都必须以数据和评测闭环收束。下一课回到模型已训练完成后的生成路径：从 logits 经 softmax、temperature 与 top-p 选择下一个 token，比较 prefill 与逐 token decode，并理解 KV Cache 为什么能减少重复计算、又为何会把长上下文和高并发变成显存与延迟权衡。',
};
