export const llm02Note = {
  readingMinutes: 28,
  introduction: '神经网络的“学习”并不是模型突然理解了答案，而是一条可以逐步检查的数值链：带形状的张量进入带参数的层，前向计算产生预测，损失把预测与标签的差距变成训练信号，反向传播计算梯度，优化器才据此更新参数。本章沿一轮训练迭代拆开这条链，同时把表达能力、梯度传播、学习率、数据划分和上线判断放回各自的位置。学完后，你应能完整口述一次参数更新，解释梯度、学习率与激活函数各自解决什么问题，并知道训练损失下降为什么还不能充当发布证据。',
  sections: [
    {
      id: 'training-loop-and-tensor-shapes',
      title: '先看全局：训练循环与张量形状',
      paragraphs: [
        '问题：一批原始数字怎样最终变成一次参数变化？张量是按一个或多个维度排列的数值数组，“形状”记录每个维度的长度。例如二十条样本、每条八个特征，可以写成形状为“批次二十、特征八”的输入；与它相乘的权重必须在对应维度上兼容。形状不仅是存储细节，还说明批次、特征或输出等轴在运算中扮演什么角色。',
        '完整训练循环按因果顺序运行：从训练数据取一个批次，前向计算得到预测，将预测与标签送入损失函数，反向传播得到相关参数的梯度，优化器应用更新，再处理下一批。前向传播负责从输入算到目标值，反向传播负责沿已发生的计算求导；二者之间的损失是连接任务目标与参数敏感度的节点。任何一步缺失，都不能声称完成了一轮学习。',
        '实际排查应先在关键边界记录形状、数值范围与批次语义：样本轴是否意外和特征轴互换，标签是否能与预测逐项对齐，输出是否满足损失函数的输入约定。一个能运行的广播运算也可能悄悄复制数字而偏离任务含义。工程上的第一条因果链因此是“形状正确，才有可解释的前向；前向可解释，后续梯度才值得诊断”。',
      ],
      keyPoints: [
        '张量的形状同时约束运算是否合法，以及每个维度在任务中的语义。',
        '一次训练迭代依次包含取批次、前向、损失、反向传播、优化器更新和重复。',
      ],
      sourceIds: ['res-d2l-zh', 'res-karpathy', 'res-fastai'],
    },
    {
      id: 'linear-layers-and-activation',
      title: '线性层与激活：网络为何不只会画直线',
      paragraphs: [
        '问题：多堆几层加权求和，是否自然就能表达复杂关系？线性层在工程语境中通常指仿射变换：输入乘以权重矩阵，再加偏置，写成“输出等于输入乘权重加偏置”。权重决定各输入怎样组合，偏置允许整体平移。严格数学术语会区分线性与仿射，但代码和课程常把带偏置的模块也称为线性层，理解时要看实际计算。',
        '若相邻层之间没有非线性激活，多个仿射变换的复合仍能合并成一个仿射变换；层数增加了，却没有获得相应的非线性表达能力。这直接回答本课测验：完全没有激活时，网络不会因此无法求梯度，而是整体仍受单个仿射映射的表达边界限制。激活函数对中间值做非线性变换，使网络可以形成更复杂的决策边界。',
        '激活选择还会改变梯度如何穿过深层链条。某些区域导数很小，反复相乘可能使早期层收到的梯度接近零；导数或权重尺度持续放大，则可能出现梯度爆炸。激活不是消除所有训练困难的万能开关，初始化、数据尺度和网络结构也共同影响传播。工程上应同时观察激活分布与分层梯度尺度，再决定换激活或调整其他环节。',
      ],
      keyPoints: [
        '带偏置的“线性层”实质是仿射变换，负责学习输入特征的加权组合。',
        '没有非线性激活的多层仿射网络可折叠为一个仿射映射；激活也会影响梯度传播。',
      ],
      callout: {
        kind: 'boundary',
        title: '表达能力与可训练性是两个问题',
        body: '非线性激活让网络能表达更复杂的函数，但不保证深层网络一定容易优化；看到梯度消失时，还要联合检查初始化、尺度与结构。',
      },
      sourceIds: ['res-google-ml', 'res-d2l-zh', 'res-fastai'],
    },
    {
      id: 'loss-as-training-proxy',
      title: '损失：可优化目标不等于业务成功',
      paragraphs: [
        '问题：预测已经产生，模型靠什么知道“这次有多错”？损失函数把预测与真实标签的差异映射为数值，数值越小通常表示更符合所选训练目标。二分类可使用交叉熵，回归可使用平方误差，但选择哪种损失取决于任务与输出含义。损失只度量设计者写进目标的部分，未被表示的事实性、安全性、成本或用户体验不会自动受到保障。',
        '单个样本、一个批次或序列中每个 token 的损失都可能先形成张量；为了进行一次常见的梯度更新，训练通常对这些值求和、求平均或加权归约，得到一个标量目标再求导。归约方式决定不同样本或位置对更新贡献多少，因此“损失必须从一开始就是一个数字”并不准确，而“优化常围绕一个归约后的标量目标进行”才是稳妥边界。',
        '训练损失是代理指标：它适合驱动参数变化，却不是发布许可。面试被问“损失下降是否可上线”时，应继续检查与训练隔离的验证或测试数据、任务级代理指标、真实业务结果以及语言、长度、人群与高风险请求等重要切片；还要确认数据划分没有让同一用户、近重复样本或未来信息跨集合泄漏。工程桥梁是把损失当训练仪表，而不是把它当产品验收单。',
      ],
      keyPoints: [
        '损失把预测与标签的差距编码为可优化信号，但只能覆盖显式写入的代理目标。',
        '逐样本或逐 token 损失可以是张量，训练常通过归约形成标量目标后计算梯度。',
      ],
      sourceIds: ['res-google-ml', 'res-d2l-zh', 'res-3b1b-nn'],
    },
    {
      id: 'computation-graph-chain-rule-backprop',
      title: '计算图、链式法则与反向传播',
      paragraphs: [
        '问题：一个最终损失怎样把责任分给网络中许多参数？计算图把一次计算表示为节点和依赖边：节点保存输入、参数或中间结果，运算节点产生新值，损失位于依赖链末端。局部导数描述某个运算的输出对直接输入的微小敏感度；链式法则再把相邻局部导数沿依赖路径组合，得到损失对更早变量的梯度。',
        '反向传播是高效应用链式法则的过程。动态计算图可在执行前向运算时记录实际依赖，然后从标量损失开始，以与拓扑依赖相反的顺序传播梯度；一个值若经多条路径影响损失，各路径贡献需要相加。Karpathy 的 micrograd 用标量节点展示这一机制，适合看清局部导数与逆序遍历，但它是教学引擎，不能据此推断生产张量框架的全部实现。',
        '这也划清测验边界：反向传播直接产生的是相关变量或参数的梯度，不是新参数、训练数据或部署接口。若在中间节点检查导数，可以逐段定位错误来自前向公式、局部导数还是路径累加；得到梯度后仍需交给优化器。工程上的责任分工是“计算图保留依赖，反向传播计算敏感度，优化器决定怎样行动”。',
      ],
      keyPoints: [
        '链式法则把局部导数沿计算依赖组合，反向传播按依赖逆序高效计算梯度。',
        '反向传播的直接结果是梯度；参数是否以及如何改变由随后的优化步骤决定。',
      ],
      sourceIds: ['res-d2l-zh', 'res-karpathy', 'res-3b1b-nn'],
    },
    {
      id: 'optimizer-learning-rate-and-zero-grad',
      title: '优化器、学习率与梯度清理边界',
      paragraphs: [
        '问题：有了梯度，参数为什么通常沿负方向移动？某参数的梯度表示在当前点附近，参数微小增加时损失上升最快的局部敏感方向，因此梯度本身指向局部最陡上升；梯度下降取它的负方向。最基本更新可写成“新参数等于旧参数减去学习率乘梯度”。这是局部近似，既不保证每一步实际损失都下降，也不保证到达全局最优。',
        '优化器读取梯度并应用更新规则，学习率控制步长。学习率过小常表现为损失下降很慢、有限预算内几乎不动；过大可能越过较低区域，使损失来回震荡、突然增大或数值不稳定。因此面试追问完整更新链时要说清：反向传播只算梯度，优化器才结合学习率等状态修改参数；“更大的学习率总会更快”没有成立的机制基础。',
        '还要把数学机制与框架行为分开。在 PyTorch 的常见默认行为中，多次反向计算会把梯度累加到已有梯度字段，所以普通逐批更新通常在合适时机清零；有意做梯度累积时则会跨若干小批次保留并合并贡献。何时调用 zero_grad 或 zero_ 属于具体框架与训练方案的约定，不是微积分定律。工程上应把清零、反向与更新的顺序写进循环并用日志验证，而不是只清一次后凭感觉训练。',
      ],
      keyPoints: [
        '梯度是当前点附近的最陡上升敏感方向，梯度下降沿负梯度更新且没有全局最优保证。',
        '反向传播计算梯度，优化器应用更新；PyTorch 梯度累积与清零是框架行为。',
      ],
      sourceIds: ['res-d2l-zh', 'res-google-ml', 'res-fastai', 'res-karpathy'],
    },
    {
      id: 'generalization-and-training-diagnosis',
      title: '泛化与训练诊断：看哪条曲线、哪些切片',
      paragraphs: [
        '问题：训练曲线变好时，怎样判断模型是在学习规律还是只记住训练样本？训练集用于计算更新，验证集用于训练期间选择方案，测试集应留到较少次数的最终估计。若训练损失和验证损失都高，可能是优化、容量或数据表示不足；若训练损失继续下降而验证损失转差，常见解释是过拟合，但曲线只是线索，仍要检查实现和数据。',
        '可靠诊断要分层切片。数据切片检查来源、时间、标签质量、重复与难例；梯度切片按层观察接近零、异常放大或非有限值；业务切片按语言、人群、输入长度、流量场景和风险等级比较任务指标。单一平均值会掩盖少数但重要场景的失败，训练日志也无法替代部署后的延迟、成本、分布漂移与质量监控。',
        '验证集必须按未来使用方式隔离：同一实体或近重复内容不应跨集合，时间预测不能让未来信息进入训练，反复用测试结果调参也会把测试集变成事实上的验证集。面试回答上线判断时，应给出“训练损失—无泄漏验证—业务代理与关键切片—部署监控”的证据链。工程结论是，优化成功只回答模型是否贴近训练目标，泛化与发布要由独立证据继续回答。',
      ],
      keyPoints: [
        '训练、验证和测试承担不同职责，数据泄漏会让泛化估计失真。',
        '训练诊断应联合损失曲线、数据切片、分层梯度、业务切片与部署监控。',
      ],
      sourceIds: ['res-google-ml', 'res-d2l-zh', 'res-karpathy'],
    },
    {
      id: 'worked-binary-parameter-update',
      title: '手算一次二分类参数更新',
      paragraphs: [
        '问题：能否用一个透明数字例子，把输入一路追到新参数？以下是专门构造的教学例子，不是任何来源的基准结果。设只有一个样本，输入 x=2，真实标签 y=1，权重 w=0.2、偏置 b=0；模型先算 z=wx+b=0.4，再用 sigmoid 激活得到预测概率 p≈0.599，并采用二元交叉熵损失 L=-ln(p)≈0.513。因为正类标签希望 p 更接近 1，所以当前损失仍有下降空间。',
        '对 sigmoid 与二元交叉熵的这组组合，dL/dz=p-y≈-0.401；链式法则给出 dL/dw=(p-y)x≈-0.802，dL/db≈-0.401。取学习率 0.1，优化器执行负梯度更新后，w 变为约 0.280，b 变为约 0.040；同一输入的新 z 增大，正类概率随之提高。这个算例只说明当前点的一步局部更新：学习率太大会越过合适区域甚至震荡，太小则会在许多步后仍变化有限。',
        '练习交付时依次完成三步：给出输入、参数、预测、标签和损失变化理由；反推一个权重的梯度方向并说明优化器更新；补充学习率过大与过小的可观察现象。可直接改写这份约二百至四百字模板：“我选择一个二分类样本，输入为___，标签为___。模型用参数___先做仿射计算，再经过___激活得到预测___；我采用___损失，因为预测与标签___，所以损失为___。沿计算图反向求导后，权重梯度为___，它表示权重在当前点增加时损失将___。反向传播只产生梯度，优化器以学习率___执行___方向更新，参数由___变为___。若学习率过大，我会观察损失震荡、上升或数值不稳；若过小，损失下降缓慢。训练损失下降后，我仍会用无泄漏验证集与关键业务切片检查泛化。”能不看资料讲清这条链，并解释梯度、学习率和激活各自作用，才满足本课完成标准并能应对面试追问。',
      ],
      keyPoints: [
        '透明算例必须声明数据、激活、损失、梯度与学习率，不能把教学数字冒充来源基准。',
        '完成标准是能完整口述前向到更新，并解释梯度、学习率、激活与泛化检查的边界。',
      ],
      sourceIds: ['res-google-ml', 'res-d2l-zh', 'res-karpathy', 'res-fastai'],
    },
  ],
  misconceptions: [
    {
      claim: '反向传播完成后，参数已经自动更新。',
      correction: '反向传播直接计算梯度；只有优化器读取梯度并执行更新规则后，参数才改变。',
    },
    {
      claim: '梯度就是新参数，或者梯度天然指向损失下降方向。',
      correction: '梯度是当前点附近的局部最陡上升敏感方向，不是参数值；梯度下降取负方向，也不保证全局最优。',
    },
    {
      claim: '多层网络即使完全没有激活函数，也会因为层数增加而自动变得非线性。',
      correction: '多个仿射变换仍可合并为一个仿射映射；非线性激活才打破这种折叠。',
    },
    {
      claim: '训练损失持续下降，就足以说明模型已经可以上线。',
      correction: '训练损失只是代理目标的拟合信号，还要检查无泄漏验证、业务指标、重要切片以及部署监控。',
    },
    {
      claim: '训练开始前把梯度清零一次，之后所有批次都可以直接反向传播。',
      correction: '在 PyTorch 的常见默认行为中梯度会累加，普通逐批更新要按循环清理；有意累积时机则由训练方案决定，这不是数学定律。',
    },
    {
      claim: '学习率越大，模型一定学得越快。',
      correction: '过大的步长可能越过较低区域并造成震荡或发散；过小会缓慢，需要根据曲线与数值状态诊断。',
    },
  ],
  recap: [
    '张量形状决定运算是否兼容及各维度语义；形状错误会让后续预测和梯度失去任务含义。',
    '仿射层负责加权组合；若没有非线性激活，多层仿射映射仍会折叠成一个仿射映射。',
    '损失把预测与标签的差异变成训练代理，逐项损失常经归约形成标量目标后再求梯度。',
    '计算图记录依赖，链式法则组合局部导数，反向传播按依赖逆序得到参数梯度。',
    '梯度指向局部最陡上升，优化器沿负方向应用更新；学习率决定步长但不提供全局最优保证。',
    'PyTorch 中常见的梯度累积与清零属于框架行为，必须与有意梯度累积方案区分。',
    '训练损失下降只说明更贴近代理目标；无泄漏验证、关键业务切片和部署监控才继续提供泛化与发布证据。',
  ],
  nextStep: '本章把数字张量如何经过参数、损失和梯度完成更新讲清了。下一课将转向语言输入：文本如何被切成 token，离散 token 如何映射为 embedding 向量，以及有限上下文怎样影响模型实际可见的信息与应用预算。',
};
