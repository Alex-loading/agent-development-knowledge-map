const sections = Object.freeze([
  Object.freeze({
    id: 'map-the-field',
    title: '先画地图：AI 不是某一种模型',
    paragraphs: Object.freeze([
      '学习大语言模型时，第一步不是记住一串缩写，而是先判断每个词在回答什么问题。人工智能（AI）描述的是让机器完成感知、预测、决策或生成等任务的宽泛目标；它既包含从数据学习的方法，也包含搜索、规划、规则推理等不依赖训练神经网络的方法。因此，AI 不能与聊天机器人、神经网络或某一家模型画等号。',
      '机器学习和深度学习属于“如何实现能力”的方法轴：机器学习让系统从数据中学习规律，深度学习则主要用多层神经网络学习表示。生成式 AI 属于“系统产出什么”的能力轴，关注产生文本、图像等新内容；大语言模型（LLM）是面向语言建模的一类大型模型，现代 LLM 通常用深度学习实现。两条轴可以交叉，却不该被压成一条没有分叉的包含链。',
      '画关系图时，可以先写“AI”这个大范围，在其中标出机器学习和深度学习的方法关系；再单独画一条生成能力轴，把语言生成对应到 LLM，并在旁边注明视觉、语音等深度学习系统并不都是 LLM。这样既能回答“LLM 通常是基于深度学习的生成模型”这道判断题，也能在面试追问时说明：AI 还有非学习方法，深度学习也远不只处理语言。',
    ]),
    keyPoints: Object.freeze([
      'AI 是能力目标的宽泛集合，不等于机器学习、深度学习或聊天产品。',
      '方法轴与产出轴相互交叉，LLM 只是现代 AI 系统中的一种模型。',
    ]),
    callout: Object.freeze({
      kind: 'boundary',
      title: '不要背一条错误的箭头链',
      body: '“AI→机器学习→深度学习”可表达方法上的包含关系，但“生成式 AI→LLM”描述的是能力与模型类别；两组关系应在图中交叉说明。',
    }),
    sourceIds: Object.freeze(['res-ms-ai', 'res-ms-genai', 'res-hf-llm']),
  }),
  Object.freeze({
    id: 'minimal-learning-loop',
    title: '机器如何从数据中学习',
    paragraphs: Object.freeze([
      '把模型学习压缩成最小闭环，需要五个角色。任务先规定希望系统完成什么；数据提供可观察的样例；参数保存模型当前学到的可调整规律；目标函数把任务要求变成可计算的方向，损失则度量当前预测与目标之间的差距；优化过程依据这个差距反复调整参数。这里不必先掌握公式，但必须看清因果顺序：没有明确任务和数据，参数更新就没有可靠目标。',
      '一次训练迭代通常先让当前参数根据输入产生预测，再计算损失，随后确定哪些参数变化可能降低损失，最后由优化步骤实施更新。下一课会进一步解释梯度和反向传播如何完成“分配责任”，本课只保留边界：损失是任务目标的代理，不是业务价值本身；优化让训练样例上的损失下降，也不自动保证模型面对新输入仍然有效。',
      '泛化指模型把训练中学到的规律用于未见样例的能力。开发者因此要把训练样例与独立验证样例分开，并用真实任务切片检查模型是否只记住表面模式。这个闭环也解释了为什么应用故障不能一律归因于“模型没训练好”：输入数据、任务定义、评测标准或应用流程同样可能是瓶颈，必须先定位再决定修改哪一层。',
    ]),
    keyPoints: Object.freeze([
      '学习闭环由任务、数据、参数、目标或损失以及优化共同构成。',
      '训练损失下降不等于业务目标达成，泛化必须由独立样例验证。',
    ]),
    sourceIds: Object.freeze(['res-ms-ai', 'res-hf-llm']),
  }),
  Object.freeze({
    id: 'from-generation-to-llm',
    title: '从生成模型到大语言模型',
    paragraphs: Object.freeze([
      '判别式任务通常要在给定候选中输出标签或分数，例如判断一封邮件属于哪个类别；生成式任务则要产生新的内容。语言模型把生成问题写成“在已有文本条件下，下一个语言单位有多可能”。这种目标不会让模型内置一个事实数据库，而是让它学习文本中可用于预测的模式，所以流畅、相关与事实正确是需要分别评测的性质。',
      '文本进入现代 LLM 前会先被 tokenizer 切成 token，并映射成数字 ID。token 是模型词表中的处理单位，可能是词、子词、标点、汉字或字节片段，不能简单理解为自然语言中的一个词。自回归生成会根据当前全部可见 token 计算下一个 token 的概率分布，选出结果后把它追加到上下文，再重复同一过程，直到满足停止条件。',
      '“大语言模型”中的“大”提示了模型、数据和计算规模，但规模本身不是严格能力保证。对 Agent 开发者更有用的边界是：LLM 接收上下文并生成语言或结构化候选，它擅长从模式中归纳、转换和起草，却可能受训练数据偏差、上下文长度、资源限制和概率生成影响。不要因为输出像解释，就推断模型拥有人的理解，也不要用未经核验的参数量给模型能力排序。',
    ]),
    keyPoints: Object.freeze([
      '语言模型学习条件 token 概率，token 与自然语言中的词不是同一概念。',
      '自回归生成按依赖顺序逐 token 延长上下文，流畅度不是事实验证器。',
    ]),
    sourceIds: Object.freeze(['res-ms-genai', 'res-hf-llm', 'res-ms-ai']),
  }),
  Object.freeze({
    id: 'training-versus-inference',
    title: '训练与推理是两个不同阶段',
    paragraphs: Object.freeze([
      '训练阶段的目标是改变模型。预训练可以从大量文本中的自监督任务学习通用模式，微调则用更聚焦的数据改变模型在特定任务或行为上的表现；无论是哪一种，只有进入训练管线、计算训练目标并执行优化，模型参数才会被更新。训练需要管理数据、计算资源、验证和回归风险，它与用户发起一次请求不是同一件事。',
      '标准推理阶段的目标是使用已经部署的模型：输入被编码后，模型依据当前参数计算输出，用户提供的新文档不会因为出现在一次 prompt 中就自动写入参数。推理可先分成两个概念阶段理解：prefill 处理已有输入上下文，decode 再按顺序生成新 token。decode 的下一步依赖前一步刚生成的结果，所以一次回答内部不能把所有未来 token 预先独立算完；缓存细节留到后续推理课再讲。',
      '这个边界能澄清 RAG。检索增强生成（RAG）是在推理时先从外部资料中找到相关内容，再把内容放入模型上下文；它改变的是本次计算可见的信息，不是用优化器改写模型参数。若系统另外收集数据并启动微调，那才进入训练阶段。面试回答可以据此抓住核心：训练通过目标和优化更新参数，推理使用已部署模型完成当前输入的计算。',
    ]),
    keyPoints: Object.freeze([
      '预训练和微调属于参数更新流程，标准推理使用已部署参数处理当前输入。',
      'RAG 改变推理上下文而不是训练模型，prefill 与 decode 只是推理阶段的进一步拆分。',
    ]),
    callout: Object.freeze({
      kind: 'warning',
      title: '把文档放进 prompt 不叫训练',
      body: '文档只在本次上下文中可见；除非另有训练管线执行目标计算和参数更新，否则模型本身没有因此学会这份文档。',
    }),
    sourceIds: Object.freeze(['res-ms-genai', 'res-hf-llm', 'res-ms-ai']),
  }),
  Object.freeze({
    id: 'model-and-application-boundary',
    title: '模型开发与应用开发的职责边界',
    paragraphs: Object.freeze([
      '模型是包含架构、参数、tokenizer 和配置的计算工件；模型服务在它外面增加推理运行环境、接口、伸缩、监控和安全能力；应用再把服务接入业务数据、用户交互、权限规则与确定性代码。分清三层后，团队才能准确描述问题：一次回答错误，可能来自基础模型能力，也可能来自检索漏文档、提示歧义、工具返回异常或应用校验缺失。',
      '模型开发者主要处理训练数据、目标、架构、训练或微调效率、模型评测与部署工件。应用开发者主要处理需求拆解、模型选择、prompt 与上下文、RAG、工具接口、结构校验、失败降级、延迟、成本和可观测性。两者会协作，但不能因为应用调用了模型 API，就说应用团队在训练模型；也不能把本应由权限和程序校验保证的事情交给模型猜测。',
      '遇到故障时先建立可复现样例和业务评测，记录输入、检索结果、工具调用与最终输出，再按层定位。事实缺失可能先检查资料和检索，格式不稳可检查提示与结构校验，计算错误可交给确定性工具，只有评测显示现有模型在充分上下文和正确流程下仍缺少必要能力，才把换模型或微调列为候选。这正是第二道测验选择“先定位错误来源”的理由。',
    ]),
    keyPoints: Object.freeze([
      '模型、模型服务与应用是三层对象，失败应沿数据和调用链定位。',
      '应用可靠性来自评测、工具、校验和降级，不能全部外包给模型。',
    ]),
    sourceIds: Object.freeze(['res-ms-genai', 'res-hf-llm', 'res-openai-agents']),
  }),
  Object.freeze({
    id: 'agent-learning-depth',
    title: 'Agent 开发者应该学到哪一层',
    paragraphs: Object.freeze([
      'Agent 不是“更大的 LLM”，而是把模型放进可行动系统。现代 Agent 通常让 LLM 根据指令和上下文产生下一步候选，再由会话状态、工具、知识来源、权限、护栏与追踪组件支持执行和检查。LLM 负责概率性判断或生成，应用代码负责暴露允许的工具、验证参数并控制副作用；没有这些系统部件，裸模型只能返回内容，不能可靠地改变外部世界。',
      '学习顺序应由证据驱动。先定义任务成功标准并建立评测样例，再学习模型选择、prompt、上下文组织和结构化输出；需要新鲜或私有事实时验证 RAG，需要外部动作时增加最小权限工具和结果校验。若这些手段仍不能达到指标，再判断是换模型，还是用足够、高质量且与目标匹配的样例进行微调。RAG 用于提供运行时证据，微调用于改变较稳定的行为模式，不能互相冒名。',
      '本课只要求掌握能做正确工程决策的深度：知道模型怎样从数据学习、怎样按 token 推理，以及应用层怎样评测和约束它。Attention 的内部计算留给架构课，微调算法留给训练课，Harness 的会话、工具调度、追踪和恢复留给后续模块。面试追问“什么证据让你微调”时，应回答：替代方案已有基线、目标行为稳定、训练样例充足，并且收益经评测能够覆盖成本与回归风险。',
    ]),
    keyPoints: Object.freeze([
      'Agent 是由模型、状态、工具和控制边界组成的系统，不是裸 LLM。',
      '先评测，再尝试 prompt、上下文、RAG 与工具，最后才考虑模型层改动。',
    ]),
    sourceIds: Object.freeze(['res-ms-genai', 'res-ms-agents', 'res-hello-agents', 'res-openai-agents']),
  }),
  Object.freeze({
    id: 'research-assistant-example',
    title: '用资料助理串起全章',
    paragraphs: Object.freeze([
      '假设要做一个“资料助理”：用户提出问题，系统必须只依据团队文档回答，标出出处，并在证据不足时明确拒答。这里的业务目标不是“生成一段像答案的文字”，而是“给出可追溯且权限正确的答案”。第一步应收集一组可复现问题，覆盖有答案、无答案、文档冲突和越权请求，再分别检查回答正确性、引用是否支持结论以及拒答是否恰当。',
      '如果错误来自模型没有看到正确文档，应先修复文档版本、切分、检索或上下文组装，而不是从头训练模型；如果引用格式不稳定，可通过明确提示、结构化输出和程序校验修复；如果问题需要查询数据库，则提供只读、参数受控的工具。只有当充分证据已经进入上下文、应用流程也通过检查，而模型仍反复不能执行一个稳定任务模式时，才用评测基线比较换模型或微调。',
      '当资料助理进一步成为 Agent，LLM 可以提出“检索资料—比较证据—生成草稿”的下一步，Harness 则保存会话状态、调用获准工具、记录轨迹并在敏感动作前阻止或确认。这个闭环展示了职责边界：模型产生候选，检索和工具连接外部事实，可信代码验证引用与权限，评测判断系统是否真的改进。工具执行成功也不代表最终答案正确，整个链路都要可观察。',
      '把这个例子落实为练习时，先提交一张双轴关系图，正确标出 AI、机器学习、深度学习、生成能力与 LLM；再分别列出训练基础模型和调用现成模型构建资料助理所需的至少四项工作；最后写一份两周计划，把评测、prompt、RAG、工具和验证列为当前学习项，把从头预训练与训练算法细节列为暂缓项。交付物是一张图和不超过三百字、说明取舍依据的应用开发路径。',
    ]),
    keyPoints: Object.freeze([
      '资料助理先以可复现评测定位失败，再选择检索、提示、工具或模型层方案。',
      '模型生成候选，系统提供事实、权限、执行、验证和追踪，二者共同构成 Agent。',
    ]),
    callout: Object.freeze({
      kind: 'example',
      title: '决策顺序',
      body: '样例与指标 → 定位失败层 → 修复资料、上下文或工具 → 比较模型方案 → 用回归评测验证，而不是从“要不要训练”开始。',
    }),
    sourceIds: Object.freeze(['res-ms-genai', 'res-ms-agents', 'res-hello-agents', 'res-openai-agents']),
  }),
]);

const misconceptions = Object.freeze([
  Object.freeze({
    claim: 'AI、机器学习、深度学习、生成式 AI 和 LLM 只是同一个东西的不同叫法。',
    correction: '它们回答的层次和分类轴不同。把它们混成同义词会让团队无法判断问题属于任务、方法、模型还是应用系统。',
  }),
  Object.freeze({
    claim: '把新文档放进 prompt 或 RAG 后，模型就完成了训练并永久记住文档。',
    correction: 'prompt 和 RAG 改变本次推理可见的上下文；只有单独的训练或微调管线执行优化和参数更新，模型参数才会改变。',
  }),
  Object.freeze({
    claim: '应用回答错误时，最彻底的第一步总是换更大模型或从头训练。',
    correction: '先用可复现样例定位数据、检索、提示、工具、校验或模型能力中的失败层，否则高成本模型改动可能无效并引入新回归。',
  }),
  Object.freeze({
    claim: 'LLM 能生成计划，所以它天然拥有工具权限、长期记忆和可靠行动能力。',
    correction: '这些能力来自 Agent 系统给模型连接的状态、工具和控制边界；权限、参数校验与副作用控制必须由可信应用代码落实。',
  }),
  Object.freeze({
    claim: '语言流畅就说明模型理解了事实，而且同一问题会稳定得到正确答案。',
    correction: 'LLM 依据上下文生成概率较高的 token，可能产生流畅但无依据的内容；事实、稳定性和任务质量需要分别评测。',
  }),
]);

const knowledgeNote = Object.freeze({
  readingMinutes: 25,
  introduction: '如果把 AI、机器学习、深度学习、生成式 AI、LLM 和 Agent 当成一串同义词，后续学习很容易只记住术语，却不知道故障发生在哪一层。本章从一张多轴地图出发，建立“机器如何学习—语言模型如何生成—模型如何被应用调用—Agent 如何在模型外形成行动系统”的依赖链。学完后，你应能回答本课两道测验与三道面试题，完成资料助理的学习边界练习，并能依据评测证据选择应用层方案或模型层方案。',
  sections,
  misconceptions,
  recap: Object.freeze([
    'AI 是宽泛能力目标；机器学习和深度学习是实现方法，生成式 AI 是能力类别，LLM 是面向语言的一类模型。',
    '模型学习的最小闭环包含任务、数据、参数、目标或损失、优化以及对未见样例的泛化检查。',
    '现代 LLM 把文本编码为 token，并按已有上下文自回归地产生下一个 token；连贯生成不等于事实验证。',
    '训练或微调通过优化改变参数，标准推理使用已部署模型；RAG 提供推理时证据，不是训练。',
    '模型、模型服务与业务应用是不同层，应用团队要负责上下文、工具、校验、权限、观测和降级。',
    'Agent 开发应先建立评测，再依次验证 prompt、上下文、RAG 与工具，只有证据充分时才换模型或微调。',
    '资料助理把全章串成一个闭环：模型生成候选，外部资料提供证据，工具执行动作，可信代码守住边界，评测验证结果。',
  ]),
  nextStep: '下一课将放大本章的“机器如何学习”闭环，沿一次神经网络参数更新依次观察张量、线性层、激活、损失、梯度、反向传播和优化器。你不需要先学会训练大模型，但需要理解参数为什么会改变、损失为何只是代理目标，以及训练表现与泛化能力为什么必须分开检查。',
});

export const llmFoundationNotes = Object.freeze({
  'llm-01': knowledgeNote,
});
