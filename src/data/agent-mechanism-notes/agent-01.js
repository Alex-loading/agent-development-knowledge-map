const sections = Object.freeze([
  Object.freeze({
    id: 'control-authority-spectrum',
    title: '控制权连续谱：先问谁决定下一步',
    paragraphs: Object.freeze([
      '判断一个系统是普通 LLM 应用、Workflow 还是 Agent，最有用的问题不是“界面像不像聊天”或“有没有调用工具”，而是谁依据什么信息决定下一步。普通调用里，宿主代码准备输入、调用模型并接收一次输出；模型提供生成能力，却不接管应用控制流。即使它输出 JSON、总结很长或被调用数次，只要后续路径仍由确定性代码固定，它仍是普通 LLM 应用。',
      'Workflow（工作流）把顺序、分支、重试和审批路径预先写进程序，模型可以在某个节点分类、抽取或起草，但节点之间怎样移动由代码决定。Agent 则把有限的下一步选择权交给模型：模型读取目标、当前状态与新的环境观察，在宿主允许的动作空间内提出下一动作。这里的“有限”很重要，Agent 不是获得任意权限，而是在明确边界中动态选路。',
      '三类系统因此构成 agency（行动自主度）连续谱，而不是互斥品牌。一个固定审批流程可以只在“补充哪些公开资料”节点授予模型选择权，其余阶段仍由规则推进；这种混合设计往往比全流程自治更容易审计。自治增加时，模型调用次数、长尾延迟、成本、权限暴露和不可复现轨迹通常也会增加，所以团队应让动态选择的业务价值为新增复杂度提供证据。',
    ]),
    keyPoints: Object.freeze([
      '系统分类取决于下一步控制权及其依据，而不是产品名称、聊天界面或是否使用模型。',
      '普通调用、Workflow 与 Agent 是连续谱，控制权可以只在确有价值的局部节点开放。',
    ]),
    callout: Object.freeze({
      kind: 'intuition',
      title: '把控制流看成方向盘',
      body: '普通调用和 Workflow 的方向盘主要在代码手里；Agent 让模型在画定车道内选择转向，但刹车、路权和护栏仍属于宿主系统。',
    }),
    sourceIds: Object.freeze(['res-agent-anthropic-effective', 'res-agent-openai-guide', 'res-agent-hf-course']),
  }),
  Object.freeze({
    id: 'minimal-action-loop',
    title: '最小闭环：从目标走到可验证出口',
    paragraphs: Object.freeze([
      '最小 Agent 首先要有可操作目标，而不是一句无法检查的愿望；还要有保存进展的工作状态，以及宿主明确声明的动作或工具集合。模型读取目标与状态后只能提出一个候选动作，宿主再验证动作名称、参数、权限和风险，执行通过的动作，并把环境返回的 observation（观察）写回状态。下一轮决策必须看到新观察，否则系统只是重复猜测。',
      '闭环可以压缩为“读目标与状态—选择动作—宿主校验和执行—接收观察—更新状态—检查出口”。模型负责在上下文内作下一步决策，宿主程序负责真实执行、权限、状态持久化、预算计数和确定性检查；两者不能混写成“模型调用了数据库”。模型生成一个动作提案不等于动作已获授权，更不等于环境已经成功改变。',
      '出口至少要覆盖 done、blocked、budget-exhausted 与 handoff。done 表示外部或可计算的完成证据已满足；blocked 表示缺少继续所需的信息、权限或资源；budget-exhausted 表示步数、时间、成本或重试上限已到；handoff 表示风险、歧义或专业判断必须移交人或其他受控流程。若系统只有“继续思考”和“模型说完成”，它就没有可验证、可终止的行动闭环。',
    ]),
    keyPoints: Object.freeze([
      '最小 Agent 包含目标、状态、受限动作、环境观察、状态更新循环和显式终止出口。',
      '模型提出决策，宿主执行并验证；观察必须回填，否则后续动作无法依据环境变化。',
    ]),
    callout: Object.freeze({
      kind: 'boundary',
      title: '闭环不是“模型加工具”',
      body: '没有状态更新、完成证据和停止条件的工具调用器，不具备可治理的 Agent 闭环；RAG、长期记忆和多 Agent 则都不是最小必要组件。',
    }),
    sourceIds: Object.freeze(['res-agent-openai-guide', 'res-agent-hello-agents', 'res-agent-lihongyi']),
  }),
  Object.freeze({
    id: 'action-space-and-environment',
    title: '动作空间与环境：自治只发生在边界之内',
    paragraphs: Object.freeze([
      '动作空间是系统允许 Agent 选择的候选动作集合，例如读取一个获准目录、查询订单状态、请求澄清、生成草稿或提交人工审批。动作空间不是模型在文字中能想象出的所有行为，而是宿主确实实现、能够校验并愿意授权的接口。集合越小、语义越清晰，越容易解释失败和控制权限；将“运行任意命令”当成一个万能工具，会把大量风险藏进看似简单的动作名。',
      '环境是动作真正产生效果并返回观察的外部世界，可以是文件系统、数据库、网页、用户回复或审批服务。观察应描述已发生的可见事实，例如“查询返回三条记录”“审批状态仍为 pending”或“读取权限被拒绝”，而不是模型对结果的自我评价。宿主把观察与调用标识、时间和错误类型关联后写回状态，模型才可决定重试、换路、澄清或退出。',
      '若去掉 observation 回填，具体故障不是抽象的“表现变差”，而是下一轮不知道动作是否成功：它可能重复发起同一审批、继续使用已失效链接、把权限拒绝误当成空数据，或声称文件已经生成。相反，观察存在也不自动保证可靠；宿主仍要判断返回是否新鲜、完整且属于当前任务。动作空间限定“可以提什么”，环境观察证明“实际发生了什么”。',
    ]),
    keyPoints: Object.freeze([
      '动作空间由宿主声明和实现，模型只能在被授权、可校验的候选集合中提议动作。',
      '观察是环境结果而非模型自评，并须关联本次动作后回填到工作状态。',
    ]),
    sourceIds: Object.freeze(['res-agent-hf-course', 'res-agent-ms-course', 'res-agent-hello-agents', 'res-agent-lihongyi']),
  }),
  Object.freeze({
    id: 'termination-evidence',
    title: '终止证据：停止是一项控制器职责',
    paragraphs: Object.freeze([
      '终止条件要在运行前定义为可检查谓词，而不能等模型感觉“差不多了”。固定格式摘要的 done 可以是输出通过必填字段、长度和来源校验；审批流的 done 可以是最终审批状态由审批服务确认；开放调查的 done 可以是问题清单均有可追溯证据、冲突已标注且交付文件通过检查。模型可以建议任务已完成，但宿主必须依据外部状态或确定性校验作最终判断。',
      'blocked 与 handoff 解决的是“现在不能安全继续”，但含义不同。blocked 记录具体缺口和解除条件，例如缺少文档读取权限或用户尚未回答关键范围；handoff 则把当前状态、证据、尝试过的动作和风险交给有权决定的人。不可逆动作、政策冲突或高风险歧义不应靠多问模型几次解决，而应进入审批或人工路径，并保留可恢复的交接材料。',
      '预算出口把成本与失败变成控制变量。可以同时设置最大步数、工具调用数、墙钟时间、模型费用和同类错误重试次数；任一上限触发时，宿主停止新动作，保存可用产物与失败原因，再选择 budget-exhausted 或 handoff。预算不是追求越小越好，而是防止没有新观察、没有进展的循环无限消耗，并让运营方能预测最坏情况。',
    ]),
    keyPoints: Object.freeze([
      'done 由任务外部证据或可计算谓词决定，模型的完成声明只能成为待验证候选。',
      'blocked、budget-exhausted 与 handoff 各自记录原因、恢复条件和已有状态，不能都折叠成模糊失败。',
    ]),
    callout: Object.freeze({
      kind: 'warning',
      title: '先写出口，再开放循环',
      body: '没有 done 证据、失败预算和人工交接的循环，不应因为模型能持续生成下一步就上线运行。',
    }),
    sourceIds: Object.freeze(['res-agent-anthropic-effective', 'res-agent-openai-guide', 'res-agent-ms-course']),
  }),
  Object.freeze({
    id: 'selection-thresholds',
    title: '选型门槛：用动态价值支付自治成本',
    paragraphs: Object.freeze([
      '选型先检查路径确定性：输入到输出若能由普通代码可靠计算，选择确定性程序；只需要一次抽取、分类或起草，选择普通 LLM 调用并在宿主校验。若路径由少量已知分支构成、审批顺序固定、错误恢复规则清楚，Workflow 通常更合适。只有任务步骤难以预先穷举，而且新观察确实会改变下一步时，才进入 Agent 候选。',
      '第二层检查反馈与风险。Agent 必须能获得足够可靠的环境反馈，并能把动作限制在最小权限内；若关键结果不可观察，就无法判断进展和完成。对于资金、合规、生产写入等高代价动作，可以让 Agent 收集材料、比较选项或起草建议，却把最终执行收回确定性规则、固定审批或人工。高风险并不总是排除 Agent，但会压缩它拥有的控制权。',
      '第三层用真实样例比较收益。团队应在普通调用、Workflow 和有限 Agent 之间建立相同任务集，观察成功证据、错误类型、步数、延迟、成本、人工介入和可复现性，而不是把“更先进”当成选择理由。如果任务大部分固定、只有一个开放节点，就固定外围路径，仅在该节点提供受限动作与预算；这种局部自治直接回答了面试追问，也避免为少量不确定性支付全流程复杂度。',
    ]),
    keyPoints: Object.freeze([
      '路径稳定或可确定求解时优先普通代码；已知分支适合 Workflow；观察驱动的开放选路才值得评估 Agent。',
      '选型还必须同时满足可靠反馈、最小权限、显式预算和可验证收益，自治本身不是进步指标。',
    ]),
    sourceIds: Object.freeze(['res-agent-anthropic-effective', 'res-agent-openai-guide', 'res-agent-ms-course']),
  }),
  Object.freeze({
    id: 'three-case-decision-table',
    title: '三案例决策表：把判断写成可审计设计',
    paragraphs: Object.freeze([
      '案例一“固定格式摘要”选择普通调用：路径只有“输入文档—生成指定字段—程序校验—返回”，环境反馈只用于一次校验，不需要模型动态探索。拒绝 Workflow，是因为额外节点和分支没有带来实质控制价值；拒绝 Agent，是因为开放下一步会增加成本和失败面。done 是字段、长度与引用检查全部通过；blocked 是输入缺失或无读取权限；预算是一次生成加有限格式修复；仍失败则 handoff 给人工编辑。',
      '案例二“审批流”选择 Workflow：提交、经理审批、合规复核和最终执行的顺序及角色由政策预先规定，模型最多在节点内提取信息或起草说明。拒绝普通调用，是因为一次生成不能持久化多阶段状态与审批证据；拒绝 Agent，是因为模型不应动态改写法定顺序或自行越过审批。done 是审批服务确认最终状态；blocked 是材料或审批人缺失；预算限制提醒与重试；政策冲突、超时或拒绝进入 handoff。',
      '案例三“开放资料调查”选择受限 Agent：来源路径无法预先穷举，新发现会改变查询、比较和补证顺序，因此需要状态驱动的动态动作。拒绝普通调用，是因为单次上下文难以吸收逐步环境反馈；拒绝固定 Workflow，是因为固定分支会漏掉随证据出现的新问题。动作只允许只读搜索、读取获准来源、记录证据和请求澄清；done 是问题均被证据覆盖且冲突显式保留，blocked 是核心来源不可访问，预算限定查询数与时间，高风险判断或证据仍冲突时 handoff 给领域专家。',
      '把三行写进交付表时，每行固定包含“路径是否稳定、是否依赖环境反馈、错误代价、可验证证据、推荐方案、拒绝另外两种方案、done、blocked、budget、handoff”。这张表不是给方案贴标签，而是让评审者能从同一组证据重放决策。能用控制权而非名称解释三类系统，并能画出目标—状态—动作—观察—更新—出口闭环，才满足本课两个完成条件。',
    ]),
    keyPoints: Object.freeze([
      '固定摘要用普通调用，固定审批路径用 Workflow，观察驱动的开放调查用受限 Agent。',
      '每个选择都要同时写推荐依据、拒绝替代方案的理由，以及 done、blocked、budget 与 handoff 出口。',
    ]),
    callout: Object.freeze({
      kind: 'example',
      title: '交付物字段顺序',
      body: '案例｜路径稳定性｜反馈依赖｜错误代价｜完成证据｜推荐控制方式｜拒绝其他方案｜done｜blocked｜budget｜handoff。',
    }),
    sourceIds: Object.freeze(['res-agent-anthropic-effective', 'res-agent-openai-guide', 'res-agent-hello-agents']),
  }),
]);

const misconceptions = Object.freeze([
  Object.freeze({
    claim: '只要应用使用大模型、聊天界面或调用过工具，就可以称为 Agent。',
    correction: '这些只是组件或界面特征。应检查模型是否依据状态和新观察，在受限动作空间内动态决定下一步；固定路径仍可能只是普通调用或 Workflow。',
  }),
  Object.freeze({
    claim: 'Agent 比 Workflow 更先进，所以自治越多，系统能力一定越强。',
    correction: '自治会扩大成本、延迟、权限和不可预测失败面。只有动态选择带来的可验证收益超过这些代价时，增加 agency 才合理。',
  }),
  Object.freeze({
    claim: '最小 Agent 只需要模型、Prompt 和几个工具。',
    correction: '工具只是动作入口；若缺少工作状态、环境观察、状态更新、完成证据和终止出口，系统无法形成可验证、可恢复的行动闭环。',
  }),
  Object.freeze({
    claim: '模型输出了合法动作名称，就说明该动作已经安全执行。',
    correction: '模型只提出候选动作。宿主仍须校验参数、权限和风险，执行真实操作，再把关联观察写回状态；提案、授权和执行是不同阶段。',
  }),
  Object.freeze({
    claim: '模型说任务完成，控制器就可以结束循环。',
    correction: '完成必须由外部状态或确定性谓词验证；证据不足时应继续、blocked、预算退出或 handoff，不能把自然语言自信当作事实。',
  }),
]);

export const agent01Note = Object.freeze({
  readingMinutes: 28,
  introduction: '在上一模块里，LLM 被看作接收上下文并生成候选输出的模型组件。本章把它放进应用控制流：先用“谁决定下一步”建立普通调用、Workflow 与 Agent 的控制权连续谱，再补齐目标、状态、动作、观察和显式终止出口组成的最小闭环。学完后，你应能推导两道测验的答案，完整回答三组面试追问，并为固定摘要、审批流和开放资料调查提交一张包含选型证据与退出设计的决策表。你不需要先选定某个框架；本章关注的是任何实现都必须回答的控制、反馈与停止问题。',
  sections,
  misconceptions,
  recap: Object.freeze([
    '普通 LLM 调用由宿主决定路径；Workflow 预写步骤与分支；Agent 让模型在边界内依据状态和观察动态选择下一步。',
    '三者是 agency 连续谱，局部开放一个决策节点通常比无差别扩大自治更可控。',
    '最小 Agent 的闭环是目标与状态、受限动作、宿主校验执行、环境观察、状态更新以及显式出口。',
    '模型负责提出候选决策，宿主负责权限、真实执行、状态持久化、预算和完成检查。',
    '动作空间限定可以提议什么，观察证明环境实际发生了什么；没有观察回填会导致重复、误判和虚假完成。',
    'done、blocked、budget-exhausted 与 handoff 需要不同证据、原因和恢复条件，不能合并为模糊的停止。',
    '固定摘要选择普通调用，固定审批流选择 Workflow，观察驱动的开放调查选择受限 Agent；每项都必须解释为何拒绝另外两种方案。',
  ]),
  nextStep: '下一课将把本章的“目标、状态与完成证据”写成 task contract：把用户请求拆成硬约束、软偏好、事实、假设和未知项，并把 done 从一句模型声明改造成可检查的成功谓词。这样，Agent 才知道自己正在完成什么、哪些信息不能猜、何时应澄清或 blocked。',
});
